# Districo — Contesto di progetto

Questo file va aggiornato a ogni decisione importante presa nel progetto (nome, funzionalità, architettura, scelte di design, ecc.), così che chiunque riprenda il progetto — umano o assistente — abbia il contesto aggiornato. Aggiungere una riga alla tabella "Decisioni prese" per ogni scelta fatta, senza cancellare la cronologia precedente.


> **Per la cronologia completa delle decisioni storiche, vedi [CLAUDE-ARCHIVIO.md](CLAUDE-ARCHIVIO.md).** Questo file contiene solo lo stato attivo/corrente del progetto e le decisioni più recenti/rilevanti per chi riprende oggi il lavoro. In archivio: le decisioni e gli sprint dal 16/7 al 19/7 (primo modello dati Attività/Fasi e prima versione del modello "a stella", superati dalla revisione strutturale del 25/7); l'intera revisione strutturale del 25/7 (Sprint A: lo schema satellite di allora — stati testuali, `RevisionabileChain`, ecc. — è stato riscritto quasi per intero dalla "Revisione satelliti"/Sprint "fondamenta" di agosto); i round di implementazione/test/fix del 26/7–31/7 (Sprint B/C/D originali, KPI a target/semaforo poi sostituiti dai KPI dello Sprint E, redesign Dashboard/Header/Footer/Fornitore/Clienti, vari "Key learnings" infrastrutturali); la gran parte delle voci della tabella "Decisioni prese" dal 2/8 al 6/8 che documentano passaggi intermedi poi superati nello stesso periodo (es. stati testuali di Acquisto poi sostituiti dal flag `ordinato`, creazione automatica limitata al solo Briefing poi estesa a Preventivo/Chiusura); e le voci dal 29/7 all'8/8 spostate nello split del 13/8 (Revisione satelliti Fase 1, Sprint "fondamenta"/B/"allegati modale"/C/D, rimozione trigger post-accettazione, Sprint E, Combobox, flag `ordinato`, prima versione del restyling modale Appuntamento — poi diventata il template UI-4 e infine superata dal nuovo template Briefing dell'10/8 — Sprint UI-1/UI-2/UI-3/UI-4, audit iOS/Modal di test, sessioni di affinamento UI Android) — **nessun contenuto è stato perso o modificato, solo riorganizzato**. Split più recente eseguito il 2026-08-13 (il file aveva superato 300K caratteri); lo split precedente (2026-08-06) copriva fino a 275K caratteri. Stesso giorno, un secondo taglio più piccolo ha spostato altre 4 righe non più citate altrove (indicatore "acconto incassato" Dashboard, caselle testo Verifica misure, nota testuale Noleggio, prima versione delle classi Data+Ora poi sostituita nella sessione immediatamente successiva) per fare di nuovo spazio sotto i 150K. Il 2026-08-14 spostate altre 2 righe non più citate altrove (verifica/allineamento dimensioni Modal condivisa, mascheratura valuta) per lo stesso motivo. Il 2026-08-16 spostate altre 9 righe (il file aveva superato di nuovo i 150K, a 173,5K): origine del template Briefing/`PilloleSalvaAnnulla` e fix scrollbar/contenitore (10/8), restyling Preventivo/Progetto (11/8), nuova Attività "Acconto" (11/8), restyling Campionatura (12/8), restyling Costruzione con `sessioni_lavoro` (12/8), proporzioni Data/Ora 60/40 (12/8), promozione di Montaggio a tipo autonomo (12/8) — tutte sessioni di restyling/propagazione-template ormai completate e stabili, il loro esito resta riassunto in "Prossimi passi aperti"; il Refactor Parallel/Intercepting Routes e la "ripetibilità Attività, elenco consolidato" (entrambi 12/8) restano invece qui, ancora attivamente citati/di riferimento (dettaglio del travaso in CLAUDE-ARCHIVIO.md). Il 2026-08-17 spostate altre 5 righe, stavolta non per anzianità ma perché rese fattualmente obsolete da sessioni successive: bug fix "provenienza Dettaglio Lavoro" (13/8) e metà della sessione "origine navigazione + icona Visualizza" (14/8, l'icona resta viva e invariata) — il meccanismo "sezione di origine" che descrivono è stato rimosso per intero il 16/8; "ordinamento liste Dashboard e Conclusi" (13/8) — la sua logica è stata interamente sostituita dalla sessione di unificazione del 16/8; restyling Acquisto+catalogo Referenze e la sua gestione standalone in Impostazioni (entrambe 14/8) — corrette esplicitamente dalla sessione del 17/8 su due punti (creazione al volo rimossa, Categorie/Referenze spostate da Impostazioni a una sezione "Catalogo" dedicata); il resto di quelle due sessioni (schema, migration 0048) resta valido e non duplicato altrove (dettaglio in CLAUDE-ARCHIVIO.md). Il 2026-08-19 spostate altre 7 righe (il file aveva raggiunto 157,5K dopo la sessione "tariffe orarie e costo manodopera"), tutte del 13/8 — bug fix "Aggiungi attività"/Dashboard, rifinitura visiva grassetto Chiusura Lavoro, bug fix ordine campi indirizzo Sede Fornitore, sessione di audit completo, correzione due fix dal Livello 2 dell'audit, "Correzione di rotta" `lavoro_pronto_per_montaggio()` (il principio che stabilisce resta comunque qui, già duplicato per intero in "Principi architetturali"), cinque problemi emersi da test manuali post-audit (il punto sugli allegati è anch'esso già duplicato in "Principi architetturali") — tutti fix/rifiniture ormai stabili, mai più citati per nome da una sessione successiva (verificato con una ricerca mirata prima di spostarli, dettaglio del travaso in CLAUDE-ARCHIVIO.md). Stesso giorno, un secondo taglio più piccolo (dopo la sessione "Codice SDI/PEC + cambio password", il file aveva superato di nuovo i 150K, a 153,8K) ha spostato altre 2 righe non più citate altrove per nome (correzione posizione "Attività non preventivate" nell'elenco, 13/8; unificazione bottoni Modifica/Riapri lavoro su Lavoro concluso, 15/8 — quest'ultima non va confusa con l'unificazione Dashboard+Conclusi del 16/8, un refactor distinto e non toccato).

## Cos'è

Districo è un'app gestionale pensata per gli artigiani, per seguire un singolo lavoro dall'inizio alla fine attraverso tutte le sue fasi. Pensata per artigiani di mestieri diversi (non un solo settore), con un ruolo di amministratore della piattaforma che non entra nel merito del lavoro dei singoli ma dispone di statistiche aggregate.

## Funzionalità principali (dal brief iniziale)

- Anagrafica utenti: ogni artigiano vede i propri lavori; due artigiani possono condividere un lavoro svolto "a quattro mani".
- Anagrafica clienti: ogni lavoro è associato a un cliente specifico.
- Anagrafica fornitori: per acquisti di materiali e lavorazioni esterne.
- Percorso di svolgimento del lavoro: trattativa (attività libere) → esecuzione (fasi, personalizzabili, vedi sotto).

## Principi architetturali

- **Nessun vincolo di prerequisito reale tra Attività** (esplicitato il 2026-08-13, dopo aver rimosso `lavoro_pronto_per_montaggio()`, l'unico tentativo di gate simile mai esistito nell'app — dettaglio della rimozione in CLAUDE-ARCHIVIO.md): Districo non impone un ordine né blocca un'Attività in base allo stato di un'altra. L'artigiano decide — può fare un preventivo senza misure confermate, acquistare materiale senza acconti incassati, procedere senza consenso esplicito su una modifica — l'app si limita a **tracciare** cosa è stato fatto e cosa resta da fare (i semafori rosso/giallo/verde), mai a **imporre**. L'unico vero gate di tutta l'app resta quello di Chiusura Lavoro (tutte le Attività verdi per poter chiudere il Lavoro) — deliberato, non un pattern da estendere per analogia ad altre Attività.
- **Noleggio = noleggio di un furgone/mezzo di trasporto** (chiarito il 2026-08-13, mai specificato esplicitamente prima): per gli artigiani privi di un mezzo idoneo sempre disponibile — non un'attività di noleggio di materiale/attrezzatura del lavoro stesso.
- **Gli allegati vivono fuori dal ciclo bozza/Salva/Annulla** (chiarito il 2026-08-13, per design fin dallo Sprint "allegati modale" del 2/8, mai esplicitato prima): un file è già fisicamente scritto su disco/storage nel momento in cui viene caricato, non una bozza in memoria — trattarlo come "non salvato" fino a un click su Salva creerebbe un'ambiguità reale su cosa fare di un file già scritto se l'utente preme Annulla. Upload/eliminazione di un allegato restano quindi sempre immediati e auto-salvanti su tutti gli 8 satelliti, indipendentemente dal dirty-state degli altri campi — non un'incoerenza da correggere, un confine deliberato.

## Modello dati — dove trovarlo

Non esiste più un unico schizzo statico del modello dati: dopo la "Revisione satelliti" di agosto (vedi tabella sotto, a partire dal 2026-08-01) lo schema delle Attività (ex "satelliti") è stato riscritto più volte in sprint successivi, ciascuno documentato dove è stato deciso. Per capire lo stato **attuale** dello schema, la fonte primaria sono le migration in `supabase/migrations/` (l'ultima applicata fa fede) insieme alle voci più recenti della tabella "Decisioni prese" qui sotto, in particolare: Revisione satelliti Fase 1 (2026-08-01, base di preventivo/progetto/acquisti/campione), Sprint "fondamenta" (creazione automatica, ordine delle attività), Sprint B/C/D di agosto (Appuntamento, Progetto, Preventivo, Campionatura, Acquisto, Costruzione, Noleggio), e "Nuova attività Chiusura Lavoro" (2026-08-03, l'attuale — e unico — meccanismo di chiusura del Lavoro). Il vecchio modello Attività/Fasi e la prima versione del modello satellite (25/7) sono in CLAUDE-ARCHIVIO.md e **non riflettono più lo schema reale**.

## UI / Stile — linee guida

- Mobile-first, totalmente responsive, navigazione via menu hamburger a scomparsa.
- Palette: bianco/nero/grigio come base; colori "a LED" (rosso/giallo/verde) riservati agli stati; palette bottoni limitata e uniforme in tutta l'app.
- Tipografia: serif (stile logo) solo nel logo/marchio; sans-serif ovunque nell'interfaccia.
- Schermata più critica da disegnare per prima: dettaglio Lavoro, deve rendere evidente "cosa manca per andare avanti" (Attività aperte/bloccate/SLA superati, Fasi non concluse).

### Note tecniche emerse in fase di implementazione

- `sla_attivita`: PostgreSQL non ammette colonne nullable in una PRIMARY KEY, neanche con COALESCE nella definizione. Soluzione adottata: `id UUID PRIMARY KEY` surrogato + `CREATE UNIQUE INDEX` con espressione `COALESCE(artigiano_id, '00000000-...')` — funziona perché gli expression index supportano COALESCE, le PK no.
- `NEXT_PUBLIC_SUPABASE_URL` deve essere la base dell'URL senza path (es. `https://xxx.supabase.co`), non includere `/rest/v1/`.
- Admin RLS: nessun accesso diretto alle tabelle operative. Solo funzioni/view SQL con `SECURITY DEFINER` esporranno metriche aggregate. Il guard in `app/(admin)/layout.tsx` legge `is_admin` dalla tabella `artigiano`.
- **Ambiente dev**: Node.js 18 non è compatibile con Next.js 15/16 (richiede ≥20). Installato Node.js 20 via nvm (`nvm use 20`). Dev server: `npm run dev -- --port 3456`.
- **Fix Tailwind/Turbopack**: `@tailwindcss/oxide` non trova il binding nativo in modalità Turbopack. Fix: copiare `node_modules/@tailwindcss/oxide-linux-x64-gnu/tailwindcss-oxide.linux-x64-gnu.node` dentro `node_modules/@tailwindcss/oxide/`. Va rifatto se si cancella `node_modules`.
- Nessuna credenziale di connessione diretta Postgres (connection string) salvata in locale: le migration vengono applicate a mano via SQL Editor Supabase, non con `supabase db push`.

### 4) Convenzione asterisco sui campi obbligatori
Rimosse tutte le etichette testuali "(opz.)" (21 occorrenze in 11 file)
in favore di un **asterisco rosso** (`<span className="text-red-500">*</span>`)
accanto all'etichetta dei soli campi **obbligatori**; i facoltativi
restano impliciti per assenza di marcatura. Applicata in modo coerente
a tutti i form dell'app: registrazione, invito "a quattro mani", login,
password dimenticata/reset, Cliente, Fornitore/Sede/Contatto, Lavoro
(nuovo/standalone/modifica), e tutti i satelliti (Appuntamento,
Progetto/Preventivo/Campione, Ordine, Costruzione, Noleggio).

**Colore scelto (rosso, non un grigio neutro)**: coerente con l'uso
già esistente del rosso per i messaggi di errore nei form
(`text-red-600`), non introduce quindi una terza semantica di colore —
resta comunque distinto dal rosso "a LED" riservato agli stati dei
satelliti (un asterisco è un simbolo tipografico accanto a un'etichetta,
non un pallino di stato, il rischio di confusione è marginale e diverso
da quello già accettato per il logo/brand nella decisione del 19/7).

**Criterio usato per stabilire obbligatorio/facoltativo**: campi con
validazione client esplicita che blocca il submit se vuoti (o vincoli
`NOT NULL` a DB senza fallback) → asterisco; campi liberi/nullable →
nessun marcatore. **Select con un valore di default sempre presente**
(es. Nazione, Prefisso telefono, Sicurezza SMTP) non hanno ricevuto
asterisco: non possono essere lasciati vuoti dall'utente, quindi non
c'è ambiguità da segnalare. **Eccezione**: il campo Specializzazione
(registrazione e invito) **ha** l'asterisco pur essendo una `<select>`,
perché la sua opzione di default è "Seleziona..." (valore vuoto), non
un valore valido — può essere lasciato vuoto e la validazione lo
richiede esplicitamente.

### Note tecniche / infrastrutturali — riferimento rapido (dettaglio completo in CLAUDE-ARCHIVIO.md)

- **Cloudflare (proxy attivo su districo.it) blocca falsamente le Server Actions su `/lavori/`**: una regola WAF gestita per CVE-2025-55183 genera falsi positivi contro richieste legittime (Districo non è vulnerabile, gira su Next.js oltre la versione patchata). Fix applicato lato Cloudflare (non nel codice): Custom Rule "Skip - All managed rules" per i path contenenti `/lavori/`. Se un'azione server fallisce in produzione in modo intermittente e senza traccia nei log del container/Nginx, sospettare per primo un blocco a livello Cloudflare/CDN.
- **Porta SMTP 465 bloccata in uscita da apphub** (Hetzner o rete a monte, non l'OS del VPS) — usare sempre 587/STARTTLS, mai 465/SSL diretto, per SMTP di sistema o credenziali personali degli artigiani.
- **Docker Compose interpreta `$$` → `$` anche nei valori letti da `env_file:`** (non solo in `environment:`): una password con caratteri `$` letterali va scritta raddoppiando ogni `$` (`$` singolo → `$$`) per sopravvivere intatta a runtime.
- **`ignoreTLS` necessario per l'opzione "Nessuna cifratura" SMTP**: `secure:false`+`requireTLS:false` da soli non disabilitano lo STARTTLS opportunistico di nodemailer — se il server lo annuncia con un certificato non valido, l'invio fallisce comunque. `lib/email/send-email-personale.ts` imposta `ignoreTLS` esplicito quando l'utente sceglie "Nessuna". Preset provider comuni (Aruba, Gmail/Workspace, Register.it, Microsoft 365, Libero, hosting generico) precompilano host/porta/sicurezza in Profilo/Impostazioni.
- **Upload allegati**: due limiti Next.js indipendenti da alzare insieme (`experimental.serverActions.bodySizeLimit` e `proxyClientMaxBodySize`, quest'ultimo per via di `middleware.ts` attivo su ogni richiesta) — entrambi a 20MB, coerenti con `client_max_body_size` di Nginx. Le immagini caricate vengono ridimensionate server-side con `sharp` (lato massimo 1920px, qualità 82, EXIF applicato e poi rimosso) — i PDF restano invariati byte-per-byte.
- **Le note "non ancora applicata a Supabase Cloud" in questo file possono diventare stale**: se l'utente esegue una migration manualmente dallo SQL Editor senza che questo venga registrato in una sessione successiva, la nota scritta qui non riflette più la realtà — verificare lo stato reale del DB (query REST con la service role key) prima di trattare una migration come non ancora live, specialmente se questo condiziona *come* si scrive un fix (editare una migration già applicata è sbagliato: se ne aggiunge una nuova che corregge la precedente).
- `cloneElement`/`isValidElement` su un elemento passato da un Server Component a un Client Component **non è affidabile** (Next.js può serializzarlo come riferimento "lazy" non risolto) — se serve mostrare la stessa istanza di un componente in due modalità diverse (es. sola lettura vs modifica) a seconda di un'interazione client-side, costruire entrambe le varianti lato server invece di un override a runtime.

## Prossimi passi aperti

- **Landing page**: prossima fase del ciclo di restyling UI (dopo UI-1 → UI-4, chiuso il 2026-08-06) — non ancora iniziata, in attesa di conferma esplicita dell'utente prima di partire.
- **Ordine delle Attività di trattativa personalizzabile per mestiere**: oggi fisso in codice (`lib/lavori/attivita-ordine.ts`), uguale per tutti gli artigiani — da affrontare solo se emergerà un bisogno reale (non ipotetico), riusando lo stesso pattern già previsto per `Fase_Template` nel brief originale (vedi riga 2026-08-03 "Nota architetturale" nella tabella sotto per il dettaglio).
- **Propagazione del template Briefing (2026-08-10) COMPLETA**: tutti e 9 i satelliti sono ora su `PilloleSalvaAnnulla` — Preventivo e Progetto restylati l'11/8, Campionatura e Costruzione il 12/8, Chiusura Lavoro il 13/8 (effetto collaterale del restyling calcoli economici), Montaggio e "Attività non preventivate" nati direttamente sul nuovo standard, Noleggio migrato durante l'audit del 13/8, **Acquisto restylato il 14/8** (vedi tabella sotto, sessione "restyling Acquisto + catalogo Referenze") — era l'ultimo rimasto su `SalvaFlottante`. Le dimensioni/margini della Modal condivisa (`components/modal.tsx`) restano allineate alla Modal di test dall'11/8, nessun intervento necessario per nessuno di questi restyling. **Anche la Modal di test** (`components/test/modal-test.tsx`) resta indietro su un punto specifico: ha solo una pillola "Salva" (nessun "Annulla" affiancato) — notato e lasciato intoccato come richiesto esplicitamente in più sessioni, non è nello scope di una sessione che restyla un satellite reale.
- **Gestione standalone del catalogo Referenze — COMPLETATA il 14/8** (vedi tabella sotto, stessa giornata): non più un gap aperto.
- Fix tastiera mobile (Modal di test, 7/8): verificato finora solo su Android, da verificare anche su iOS prima di considerarlo definitivo — invariato da questa sessione.
- **"Aggiungi attività" non route-ificata nel refactor Parallel/Intercepting Routes del 12/8** (vedi tabella sotto): decisione autonoma per contenere lo scope, resta stato locale come prima — il tasto Back non la chiude (nessuna regressione, non la chiudeva nemmeno prima). Follow-up naturale a basso rischio se emergerà il bisogno: stesso pattern già pronto (`AttivitaModaleRoute`/`bloccaBackConModifiche`), ma qui non serve alcuna protezione dirty-state (è un selettore di creazione, non un form).
- **Revisioni del Preventivo non raggiungibili dalla UI**: la modale Preventivo restylata (11/8, sul template Briefing) non espone alcuna interfaccia per creare o consultare nuove revisioni — `revisione_di` esiste ancora a schema e `costruisciCatena()` mostra eventuali catene storiche precedenti alla revisione satelliti dell'1/8, ma non c'è alcun modo per l'utente di generarne una nuova dall'interfaccia attuale. Da affrontare in una sessione dedicata quando si deciderà come gestire le revisioni del preventivo in interfaccia (non ancora pianificato).
- **Futuro: app a pagamento con Stripe** (segnalato dall'utente il 2026-08-19, nessuna implementazione ancora): la struttura a tab di Impostazioni (`ImpostazioniTabs`, vedi riga 19/8 nella tabella sotto) è stata pensata apposta per reggere una futura sezione "Abbonamento/Fatturazione" senza ristrutturare il layout — basterà un nuovo elemento nell'array `TABS`. **Codice SDI/PEC — ora raccolti in Profilo** (vedi riga 19/8 "Codice SDI/PEC + cambio password" nella tabella sotto, colonne `artigiano.codice_sdi`/`.pec`, migration `0056`): resta aperto solo il collegamento vero e proprio con Stripe/fatturazione elettronica (regola di obbligatorietà — "almeno uno dei due" — non ancora decisa/implementata, i due campi sono per ora liberamente opzionali e senza vincoli incrociati) — da decidere con l'utente quando si pianificherà davvero l'integrazione, non prima.
- Per lo storico dei passi ormai completati (migration da applicare, deploy in sospeso, credenziali da aggiornare — tutti risolti nel frattempo), vedi CLAUDE-ARCHIVIO.md.

## Decisioni prese

| Data | Decisione |
|---|---|
| 2026-08-12 | **Refactor architetturale — modali satellite migrate a Next.js Parallel/Intercepting Routes**, sessione senza supervisione diretta con piena autorizzazione a decidere autonomamente sui punti delicati. Obiettivo: il tasto Back (mobile e desktop) chiude la modale attiva e torna al Dettaglio Lavoro con lo scroll ripristinato — prima le modali erano puro stato locale (`apertoSatelliteId` in `LavoroSatelliteTabella`), senza alcuna integrazione con la history del browser. **Discrepanza corretta rispetto al prompt, senza bisogno di fermarsi**: la versione reale di Next.js in uso è **16.2.10** (verificato in `package.json`), non "15" come indicato nel prompt — l'analisi di stabilità delle API di navigazione è stata condotta sulla versione realmente installata. **Adattamento di percorso non esplicitato nel prompt**: le route di esempio (`app/lavori/[id]/...`) sono state mappate al vero percorso del progetto, `app/(app)/lavori/[id]/...` (route group esistente dal redesign Dashboard/Header/Footer di luglio), non un percorso separato. **Struttura file nuova**: `app/(app)/lavori/[id]/layout.tsx` (nuovo, prima assente per questo segmento — accoglie gli slot `children`+`modal`, nessun wrapper posizionale necessario dato che `Modal` è già un portal verso `document.body`); `app/(app)/lavori/[id]/@modal/default.tsx` (nessuna modale quando l'URL non corrisponde); `app/(app)/lavori/[id]/@modal/(.)attivita/[attivitaId]/page.tsx` (route intercettata, overlay, Server Component); `app/(app)/lavori/[id]/attivita/[attivitaId]/page.tsx` (pagina piena di fallback per apertura diretta dell'URL — refresh, link condiviso — stesso contenuto ma senza overlay/backdrop/portal, un link "← Torna al Lavoro" al posto della history). **Refactor dati/contenuto, necessario per evitare duplicazione tra le vecchie tre righe forEach di `page.tsx` e le due nuove route**: nuovo `lib/lavori/dettaglio-lavoro-data.ts` (`caricaDatiLavoroSatelliti()`, estrae verbatim il blocco di fetch — lavoro/isOwner/satelliti/allegati/righe/fornitori/categorieAcquisto/preventivoCatena/costiSostenuti — che prima viveva solo in `page.tsx`) e `lib/lavori/satelliti-render.tsx` (`costruisciVociAttivita()` per il riepilogo tabellare senza JSX, usata solo da `page.tsx`; `costruisciContenutoAttivita()` per il contenuto di UN satellite, stesso identico switch per tipo/sottotipo che prima viveva nei forEach — **contenuto delle modali già implementate non toccato**, solo spostato/parametrizzato) — entrambe le nuove route chiamano queste stesse funzioni, nessuna divergenza possibile. **Scelta deliberata di non ottimizzare il fetch**: ogni route (inclusa quella di un solo satellite) richiama l'intera `caricaDatiLavoroSatelliti()` (tutti i satelliti del Lavoro, non solo quello richiesto) — `lavoro_satellite` per un Lavoro è sempre una tabella piccola, e più tipi (Preventivo: catena di revisioni; Chiusura: riepilogo costi aggregato su tutti gli Acquisti/Noleggi) hanno comunque bisogno dell'intero insieme; una query mirata per-satellite avrebbe comunque richiesto gli stessi dati per quei due casi, con la complessità aggiuntiva di due percorsi di fetch diversi. `page.tsx` drasticamente semplificato (da 536 a ~85 righe): non costruisce più `RigaSatellite.contenutoModifica/contenutoLettura`, solo il riepilogo per la tabella. **`RigaSatellite`** (in `lavoro-satelliti-tabella.tsx`) diventa un alias di `VoceAttivita` (satelliteId/nome/colore/statoLabel/posizione) — persi i due campi JSX e `titoloConPallino` (quest'ultimo era `true` per ogni riga reale fin dallo Sprint UI-4 del 6/8, nessuna eccezione rimasta da distinguere: le nuove route compongono sempre pallino+nome incondizionatamente, tramite nuovo `titoloConPallino()` condiviso in `satelliti-render.tsx`). **`LavoroSatelliteTabella`**: click sul nome/matita diventa `router.push()` verso `/lavori/[id]/attivita/[satelliteId]` (sola lettura, default) o `?vista=modifica` (matita) — un solo file di route serve entrambe le viste, la query string decide quale `isOwner` effettivo passare al componente satellite esistente (invariato). Nessuna `<Modal>` di dettaglio satellite più in questo componente (vive nella nuova route) — resta però, invariata, la `<Modal>` locale per "Aggiungi attività". **Decisione autonoma, "Aggiungi attività" NON route-ificata in questa sessione** (il prompt la presentava come "valuta se applicare", non mandatorio): resta stato locale (`mostraAggiungi`), il tasto Back non la chiude — nessuna regressione (non la chiudeva nemmeno prima, essendo pura modale in-memory), motivato dal contenere lo scope di un refactor già ampio: è un selettore di creazione senza dirty-state da proteggere, l'obiettivo esplicito della sessione era la modale di *dettaglio* di un'attività. `creaEApri()` (dopo la creazione riuscita) naviga comunque verso la route di dettaglio del nuovo satellite in modifica (`router.push`, stessa UX "si apre da sola" di prima, ora una navigazione reale) — segnalato come follow-up naturale a basso rischio per una sessione futura, se emergerà il bisogno. **Punto delicato #1 — blocco navigazione con modifiche non salvate, il cuore tecnico della sessione**. Verificato leggendo il sorgente del router (`node_modules/next/dist/client/components/app-router.js`, non solo la documentazione): l'App Router non offre alcun blocco nativo per le navigazioni client-side — `<Link onNavigate>` esiste ma copre solo i click su `<Link>`, non il tasto Back; su `popstate`, se lo `state` della entry non ha `__NA` (marker interno di Next), scatta un `window.location.reload()` incondizionato. Lo stesso file rivela però la soluzione: `window.history.pushState`/`replaceState` sono monkey-patchati per copiare automaticamente `__NA`/l'albero di rendering interno (`copyNextJsInternalHistoryState`) in **qualsiasi** stato passato da codice esterno — un `pushState` "esterno" è quindi già riconosciuto e sicuro. **Tecnica scelta: "voce di history fantasma"** (implementata come opt-in `bloccaBackConModifiche?: boolean` su `Modal`, default `false` — solo la nuova route di dettaglio attività lo passa `true`, "Aggiungi attività" resta invariata). Quando la guardia esistente (`guardiaRef`, già usata per X/backdrop/Esc) passa da assente a presente (dirty diventa vero), si pusha una voce di history duplicata con la **stessa URL** corrente. Un primo tasto Back fisico pop-a quella voce senza alcun cambio di URL/route (Next non ha nulla da fare, la vede identica) — il nostro listener `popstate` lo intercetta e mostra lo stesso dialog di conferma già esistente (`DialogConferma`, invariato), la Modal **resta montata con tutto lo stato del form intatto** (mai smontata — a differenza di un tentativo "previeni poi ripristina", che arriverebbe sempre troppo tardi dato che React/Next possono già aver iniziato la transizione prima che un handler esterno reagisca). Se l'utente conferma di uscire, `history.go(-2)` (fantasma + voce reale, un solo passo indietro "vero", il resto viene comunque gestito da Next in autonomia) porta via in un colpo solo — `go(-2)` invece di due `back()` sequenziali per evitare rischi di ordinamento asincrono tra le due chiamate. **Bug reale scoperto e corretto durante il testing** (non ipotizzato in anticipo): il primo tentativo falliva in modo bizzarro — cliccare "Annulla" nel dialog (che nel satellite è **solo stato locale**, `setConfermaUscitaAperta(false)`, nessuna chiamata a `ctx.onChiudi`) causava comunque una navigazione fino alla Dashboard. Diagnosticato con log temporanei: `useProteggiChiusuraModal`'s effect dipende da `[ctx, dirty, onTentativoChiusura]` — ma `onTentativoChiusura` è quasi sempre una arrow function inline nei satelliti (`() => setConfermaUscitaAperta(true)`), una referenza **nuova ad ogni render** — con la vecchia `registraGuardia` (solo `guardiaRef.current = guardia`, nessun effetto collaterale) questo era innocuo; con la nuova `registraGuardia` che ha effetti collaterali sulla history, lo stesso schema di cleanup+re-run ad ogni render (non solo alle vere transizioni di `dirty`) generava push/pop fantasma spuri che disallineavano il conteggio della history nel giro di pochi render. **Fix nell'hook condiviso** (`useProteggiChiusuraModal` in `modal.tsx`, non nei satelliti — non toccati, come richiesto): pattern "latest ref" già in uso altrove nello stesso file per `richiediChiusuraRef` — l'effect ora dipende solo da `[ctx, dirty]`, la guardia esposta legge sempre la versione più recente di `onTentativoChiusura` tramite un ref aggiornato in un effect separato. **Secondo affinamento necessario, scoperto testando la sequenza "Back → Annulla (resta) → Back di nuovo"**: il fantasma consumato dal primo Back fisico va **ri-armato immediatamente**, prima ancora di sapere cosa deciderà l'utente nel dialog — se sceglie di restare (Annulla, stato locale interno al satellite, Modal non ne viene mai a conoscenza), un secondo Back deve trovare di nuovo un fantasma pronto ad assorbirlo, non passare "a vuoto" dritto alla navigazione reale; se invece conferma l'uscita, `chiudiConPulizia()` lo consuma comunque lui con `go(-2)`, coerente in entrambi gli esiti. **Punto delicato #2 — scroll restoration**: verificato esplicitamente (non dato per scontato, come richiesto) prima di scrivere qualunque fallback manuale. Lo slot `children` del nuovo `layout.tsx` (la pagina Lavoro sottostante) **non smonta mai** quando lo slot parallelo `@modal` cambia — è la proprietà fondante delle parallel routes: rami indipendenti dello stesso layout, un cambio in uno non tocca l'altro. Dato che la pagina sottostante non si smonta né si ri-renderizza, il suo scroll (a livello di documento/`window`, nessun contenitore scrollabile interno in questa pagina) **non viene mai toccato** dall'apertura/chiusura della modale (un overlay `position: fixed`, che non influenza affatto lo scroll del documento) — nessun fallback manuale (`sessionStorage`) necessario, verificato con un test dedicato (scroll a 396/347px, apertura+chiusura modale via Back, scarto ≤2px). **Testing end-to-end estensivo** (Supabase locale, porte offset +1000, Lavoro di test con tutti e 8 i tipi di satellite, sia dev server Turbopack sia build di produzione `next start`, ambiente smontato a fine sessione): 50 controlli su script Playwright standalone, ripetuti identici in entrambe le modalità (dev e produzione) — navigazione base (click riga → URL/overlay, pagina sottostante mai smontata, Back chiude); scroll restoration; **sequenza completa modifiche non salvate**: Back fisico → dialog, dati intatti, modale ancora montata → Annulla (resta) → dati ancora intatti → secondo Back fisico → dialog di nuovo (fantasma riarmato) → Esci senza salvare → naviga via, dati **non** persistiti (verificato a query diretta); stessa sequenza con "Salva ed esci" → dati persistiti (verificato a query diretta, `costo=777.00`); chiusura via X con dirty (invariata, nessun Back coinvolto) → stesso `go(-2)`; apertura diretta dell'URL (pagina piena di fallback, nessun backdrop, link "← Torna al Lavoro", campi editabili con `?vista=modifica`); smoke test su tutti gli 8 tipi di satellite (overlay si apre correttamente per ciascuno); "Aggiungi attività" (creazione Progetto → naviga alla route in modifica, invariata); eliminazione (nessuna modale coinvolta, invariata); viewport mobile 390×844 (nessun overflow orizzontale, lista e overlay, Back chiude correttamente); 404 su un `attivitaId` inesistente; **Lavoro `completato`**: matita disabilitata in tabella, e — verificato esplicitamente il bypass diretto dell'URL — `?vista=modifica` forzato manualmente sulla pagina piena **resta comunque sola lettura** (nessun campo editabile), perché `isOwnerEffettivo` (che già incorpora `completato`) è calcolato server-side una sola volta nel data loader condiviso, non fidandosi della query string da sola. Zero errori console/pageerror in tutta la sequenza, in entrambe le modalità. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione) puliti. **Tutti i test superati → proceduto a commit, push, deploy su apphub e aggiornamento di questo file, come da istruzione esplicita** (nessun blocco riscontrato che richiedesse di fermarsi con l'ambiente pulito). **File nuovi**: `lib/lavori/dettaglio-lavoro-data.ts`, `lib/lavori/satelliti-render.tsx`, `components/attivita-modale-route.tsx` (wrapper client minimale, unico scopo fornire `router.back()`/`bloccaBackConModifiche` alla route intercettata, Server Component), `components/attivita-pagina-piena-contesto.tsx` (equivalente per la pagina piena, naviga verso il Lavoro invece di `router.back()` — nessuna history di cui fidarsi su un'apertura diretta), `app/(app)/lavori/[id]/layout.tsx`, `app/(app)/lavori/[id]/@modal/default.tsx`, `app/(app)/lavori/[id]/@modal/(.)attivita/[attivitaId]/page.tsx`, `app/(app)/lavori/[id]/attivita/[attivitaId]/page.tsx`. **`components/modal.tsx` esteso** (non riscritto): nuovo prop opt-in `bloccaBackConModifiche`, nuova `ModalContestoStatico` (Provider "senza meccaniche" per la pagina piena — i satelliti esistenti chiamano comunque `useProteggiChiusuraModal` internamente, senza un Provider nel loro albero i loro bottoni "Annulla"/"Salva ed esci" chiuderebbero il dialog interno senza portare da nessuna parte), fix del bug di dipendenze in `useProteggiChiusuraModal`. Nessuna migration. |
| 2026-08-12 | **Sessione di correzione — ripetibilità Attività, elenco consolidato "Aggiungi attività"**. **Punto 1 (nessuna modifica)**: confermato che Briefing resta correttamente ripetibile pur essendo auto-creato alla nascita del Lavoro (comodità di partenza) — l'utente può crearne di successivi (es. manca un partecipante, servono nuove condizioni per procedere), comportamento "Briefing 1"/"Briefing 2" già corretto, nessun bug. **Punto 2**: Costruzione e Montaggio passano da ripetibili a non ripetibili — decisione presa quando entrambi hanno guadagnato la gestione di sessioni multiple al proprio interno (12/8, stesso giorno): non serve più creare istanze separate, una sola istanza copre già più sessioni di lavoro discontinue. `RIPETIBILE_ATTIVITA.costruzione`/`.montaggio` (`lib/lavori/attivita-ordine.ts`) passati a `false`. **Propagazione del filtro "esiste già → escludi dall'elenco"**, stesso pattern già in uso per Progetto/Preventivo/Chiusura (`esistente: Partial<Record<ChiaveAttivita, boolean>>` in `lavoro-satelliti-tabella.tsx`): nuovi flag `costruzioneEsiste`/`montaggioEsiste` aggiunti a `DatiLavoroSatelliti` (`lib/lavori/dettaglio-lavoro-data.ts`, stesso calcolo `satelliti.some((s) => s.tipo === '...')` già usato per `chiusuraEsiste`) e filati come nuovi prop attraverso `LavoroDettaglioSezioni` fino a `LavoroSatelliteTabella` — nessuna nuova migrazione, nessuna nuova query (i satelliti del Lavoro sono già caricati per intero da `caricaDatiLavoroSatelliti()`). **Elenco consolidato di ripetibilità** (per riferimento futuro): non ripetibili (singola istanza per Lavoro, escluse da "Aggiungi attività" appena esiste già una) — Progetto, Preventivo, Costruzione, Montaggio, Chiusura Lavoro; ripetibili (istanze multiple sempre proposte) — Briefing, Campionatura, Acconto, Verifica misure, Noleggio. **Acquisto lasciato esplicitamente invariato** (resta `true`/ripetibile) — decisione rimandata a una sessione futura dedicata, fuori scope qui. **Verificato end-to-end** (Supabase locale, porte offset +1000, dev server + 2 script Playwright standalone, ambiente smontato a fine sessione — Lavoro "Cucina Test Costruzione" riusato, con 3 Costruzioni/2 Montaggi/1 Briefing già esistenti dalle sessioni precedenti, più un secondo Lavoro nuovo creato via SQL diretto per testare il ciclo completo "presente → creato → sparito"): 20 controlli — su un Lavoro con Costruzione/Montaggio già esistenti, entrambe assenti da "Aggiungi attività" (Briefing/Noleggio/Campionatura/Acconto/Verifica misure tutte ancora presenti, invariate); creazione di un secondo Briefing → numerazione "Briefing 1"/"Briefing 2" corretta, "Briefing" resta comunque proposto in "Aggiungi attività" dopo la creazione (ripetibile, invariato); su un Lavoro nuovo senza Costruzione/Montaggio, entrambe le opzioni presenti finché non create, spariscono immediatamente ciascuna subito dopo la propria creazione (verificato passo-passo, non solo allo stato finale); tabella con una sola riga "Costruzione"/"Montaggio" ciascuna, nessuna numerazione residua (coerente con singola istanza non ripetibile); zero errori console/pageerror in entrambi gli script. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione) puliti. **Committato e deployato su apphub**. Nessuna migration (solo application code). |
| 2026-08-13 | **Nuova Attività "Attività non preventivate" + restyling calcoli economici/ciclo di vita di "Chiusura Lavoro"**, implementate insieme perché la seconda dipende dalla prima. **PARTE 1 — Attività non preventivate**: tipo interno `spesa_non_preventivata`, etichetta UI "Attività non preventivate" (stessa convenzione già in uso per campione/"Campionatura", acquisti/"Acquisto" — il tipo DB resta un identificatore tecnico singolare). Stesso schema di Acconto: Importo riusa `valore_complessivo`, Descrizione riusa `descrizione_libera` (entrambi già generici, nessuna colonna nuova); Data e il flag "accettata" sono dedicati (`spesa_data`/`spesa_accettata`, migration `0043`), stesso pattern di `acconto_data`/`acconto_incassato` — nessuna colonna condivisa tra tipi diversi con nomi prefissati, principio già stabilito da Costruzione/Montaggio con `sessioni_lavoro`. **Discrepanza corretta senza bisogno di fermarsi**: la richiesta descriveva Acconto come avente "pattern data+ora 60/40", ma `satellite-acconto.tsx` ha in realtà un solo campo Data (nessun campo Ora, nessuno split) — replicato Acconto così com'è realmente (l'istruzione "identica alla modale Acconto già implementata" prevale sulla frase discordante nello stesso paragrafo). Semaforo dedicato (`coloreSpesaNonPreventivata`/`labelStatoSpesaNonPreventivata`, non riusa funzioni di altri tipi): rosso finché Data e Importo non sono entrambi valorizzati, giallo quando entrambi presenti ma non accettata, verde quando accettata=true — stesso pattern di Acconto. Ripetibile (mai auto-creata), posizionata in `ORDINE_ATTIVITA` subito prima di Chiusura (nessuna posizione esplicitamente richiesta — scelta perché alimenta direttamente il calcolo "Valore complessivo" di Chiusura). **PARTE 2 — Chiusura Lavoro**. **Verifica preliminare 1 (dati reali)**: confermato via REST/service role su Supabase Cloud — 5 righe reali con `chiusura_conclusa=true`+`chiusura_data` popolata (Lavori realmente completati) e **1 riga reale con `chiusura_acconti` popolato** (€40 su "Rif. Antonelli") — non solo dati di test. Nessuna ambiguità: il prompt autorizzava esplicitamente il pensionamento di `chiusura_acconti` come fonte ("non droppare, semplicemente non leggerla/scriverla più"), quindi nessun bisogno di fermarsi — la perdita di visibilità futura di quell'unico acconto storico è una conseguenza accettata e prevista dalla richiesta stessa. **Verifica preliminare 2 (de-accettazione)**: confermato leggendo `impostaPreventivoDecisione()` — è l'unico meccanismo esistente che porta `lavoro.stato` fuori da `'accettato'` tramite un'azione utente diretta (checkbox Preventivo: annullare "Accettato" → `opportunita`; spuntare "Rifiutato" → `rifiutato` diretto). Nessuna ambiguità reale su nessuno dei due punti, proceduto senza fermarsi. **Ciclo di vita**: Chiusura non è più auto-creata alla creazione del Lavoro (rimossa da `crea_satelliti_iniziali()`, che torna a creare solo Briefing+Preventivo) — creata invece quando `lavoro.stato` entra in `'accettato'`, rimossa quando ne esce **senza passare da `'completato'`** (l'unica altra condizione in cui deve continuare a esistere — quella transizione passa comunque da un'altra funzione, `aggiornaChiusuraFlags()`, mai da `impostaPreventivoDecisione()`). Due nuovi helper (`assicuraChiusuraAllAccettazione`/`rimuoviChiusuraSeNonAccettato`, `lib/lavori/satelliti.ts`) chiamati dentro `impostaPreventivoDecisione()` (unico punto interessato — `riapriLavoro()` non necessita di queste chiamate, motivato con un commento dedicato lì: `completato→accettato` non tocca mai una Chiusura già esistente, `rifiutato→opportunita` non ha mai `'accettato'` come target). **Notifica non bloccante**: nuovo campo opzionale `info?: string` sul tipo `AzioneResult` locale di `satelliti.ts` (retrocompatibile, tutti i chiamanti esistenti restano `{ok:true}` senza `info`) — `impostaPreventivoDecisione()` lo valorizza con "Chiusura Lavoro rimossa perché il lavoro non è più accettato." quando la rimozione avviene davvero; `SatellitePreventivo` lo mostra con un semplice `alert()` dopo il successo dell'azione principale (stessa primitiva UI già in uso per gli errori, nessuna nuova conferma bloccante — l'azione è già avvenuta, è solo un avviso). **Migration `0043` — coerenza dati one-off** (verificato su Supabase Cloud prima di scrivere la migration): 2 Lavori reali `opportunita` avevano comunque una Chiusura residua del vecchio modello (entrambe vuote, nessun dato perso) → rimosse; 3 Lavori reali (2 `accettato`, 1 `completato`) non avevano alcuna Chiusura (gap storico mai colmato dal 3/8) → backfillate — il caso `completato` riceve `chiusura_conclusa`/`chiusura_incassata=true` e `chiusura_data=completato_at` (coerente con uno stato già raggiunto nella realtà), i 2 casi `accettato` ricevono una riga vuota (equivalente alla creazione automatica che sarebbe scattata al momento della loro accettazione). **Nuovo flag `chiusura_incassata`** (checkbox "Contrassegna tutti gli importi come incassati.") — **non ambiguo nonostante la richiesta lo segnalasse come tale**: la richiesta stessa assegna esplicitamente `chiusura_conclusa` alla checkbox "Contrassegna il lavoro come chiuso." (punto 4 della struttura), quindi la checkbox "Incassati" (punto 3) non può riusare la stessa colonna — due booleani indipendenti richiedono per forza due colonne, nessuna vera scelta da segnalare. `lavoro.stato` diventa `'completato'` solo quando **entrambi** i flag sono true (era: solo `chiusura_conclusa`) — backfillato `chiusura_incassata=true where chiusura_conclusa=true` sulle 5 righe reali già `completato`, preservando il loro stato verde/gate-passing (stesso principio di backfill già seguito per Campione 2/8, Acquisto 3/8, Campionatura 12/8). Semaforo (`coloreChiusura`/`labelStatoChiusura`, SOSTITUISCE il vecchio binario): rosso se nessuno dei due flag è true, giallo se almeno uno, verde se entrambi. **5 campi calcolati** (sola lettura, mai salvati — calcolati server-side in `dettaglio-lavoro-data.ts`, ogni componente coalesce-ato a 0 individualmente prima di combinarli): Valore complessivo = Preventivo rilevante + Attività non preventivate accettate; Spese complessive (rinominato da `costiSostenuti`, stessa formula invariata: Acquisti `ordinato=true` + Noleggi `prenotazione_effettuata=true`, verificato che questa fosse davvero la condizione verde per entrambi prima di riusarla); Margine = Valore complessivo − Spese complessive; Acconti complessivi = somma Acconto con `acconto_incassato=true` (sostituisce `chiusura_acconti` come fonte, come richiesto — colonna non droppata); Importo da incassare = Valore complessivo − Acconti complessivi (nessun avviso di incoerenza se non zero, come richiesto — gli Acconto tracciano solo pagamenti parziali). **Struttura modale riscritta interamente** (`components/satellite-chiusura.tsx`): SOSTITUISCE Data+Acconti liberi+singola checkbox+allegati con i 5 campi calcolati + 2 checkbox, `PilloleSalvaAnnulla` (migra Chiusura dal vecchio `SalvaFlottante`) — entrambi i flag parte del dirty-state tracciato da Salva (non auto-salvanti, stesso trattamento di "Concluso" in Costruzione/Montaggio). **Nessuna riga allegati** (rimossa, come da struttura richiesta esplicitamente — verificato prima **zero allegati reali** su satelliti Chiusura in produzione, nessun dato perso). `chiusura_data` resta silenziosamente valorizzata alla prima transizione a "entrambi true" (stesso pattern "leggi poi scrivi" di prima) pur non essendo più mostrata in UI — un audit trail interno a basso costo. Nuova `aggiornaChiusuraFlags()` sostituisce `aggiornaChiusura()`+`impostaChiusuraConclusa()` (nessun altro chiamante). **KPI Dashboard**: "Importo lavori accettati" rinominato **"Importi da incassare"** — nuova formula (per Lavoro `accettato`: Valore complessivo − Acconti complessivi, sommato sul perimetro invariato). Nome colonna SQL `importo_lavori_accettati` **non rinominato** (stesso principio già seguito per `valore_preventivo_accettato` il 2/8 — solo un'etichetta interna, l'unico cambio visibile è nel componente React). **Verificato end-to-end** (Supabase locale, porte offset +1000, dev server + 5 script Playwright standalone, ambiente smontato a fine sessione): migration — 4 scenari di coerenza dati (opportunita+chiusura stale→rimossa; accettato senza chiusura→backfillata vuota; completato senza chiusura→backfillata verde con `chiusura_data=completato_at`; completato con `chiusura_conclusa=true` preesistente→`chiusura_incassata` backfillata) tutti confermati corretti via query diretta, 2 Lavori reali già accettati con Chiusura preesistente confermati invariati; **ciclo di vita completo** — Lavoro nuovo (opportunita, nessuna Chiusura) → Preventivo accettato → Chiusura creata automaticamente (verificato a query diretta) → annullamento accettazione (uncheck "Accettato") → notifica "Chiusura Lavoro rimossa..." mostrata, `lavoro.stato` torna a `opportunita`, Chiusura rimossa; stesso esito (Chiusura rimossa) anche nella transizione diretta `accettato→rifiutato`; **Riapri lavoro** (`completato→accettato`) verificato **non tocca** la Chiusura esistente, flag preservati invariati; **Attività non preventivate** — opzione presente in "Aggiungi attività", creazione→rosso, Data+Importo→giallo, Accettata→verde, label "Descrizione"/testo checkbox/testo allegati tutti corretti, valori persistiti verificati a query diretta; **5 campi calcolati** — Lavoro con Preventivo=5000+Spesa non preventivata accettata=800 (Valore complessivo atteso 5800), Acquisto ordinato=1200+Noleggio prenotato=300 (Spese complessive attese 1500), Acconto incassato=1500 (Acconti complessivi attesi 1500) → tutti e 5 i valori mostrati in UI esattamente come attesi (€ 5.800/€ 1.500/€ 4.300/€ 1.500/€ 4.300), nessuna sezione "Acconti ricevuti"/allegati residua; **doppio flag** — un solo flag true→`lavoro.stato` resta `accettato` (non basta uno solo)+semaforo giallo, secondo flag→`lavoro.stato='completato'`+`completato_at` valorizzato+**Lavoro confermato assente da `lavori_dashboard()`** via RPC diretta; **KPI** — `kpi_dashboard()` verificato via RPC autenticata su un Lavoro `accettato` con Preventivo=6200/Acconto non incassato: `importo_lavori_accettati=6200.00` esatto (acconto non incassato correttamente non sottratto); **regressione** — Acconto/Costruzione esistenti (satelliti non toccati direttamente da questa sessione) ancora aperti/funzionanti correttamente, "Aggiungi attività" continua a escludere Costruzione/Montaggio già esistenti. 60 controlli totali su 5 script, zero errori console/pageerror in tutta la sequenza. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione) puliti. **Committato e deployato su apphub**. **Migration `0043` non ancora applicata su Supabase Cloud**. |
| 2026-08-13 | **Sessione di implementazione — vincolo su "Contrassegna il lavoro come chiuso."**: la checkbox è ora abilitabile solo se **tutte** le Attività esistenti del Lavoro (qualsiasi tipo, incluse le ripetibili, Chiusura Lavoro esclusa) hanno semaforo verde. **Ambiguità segnalata dalla richiesta, risolta chiedendo esplicitamente prima di procedere** (come richiesto): cosa fare del flag `chiusura_conclusa` già salvato `true` se, nel frattempo (Lavoro ancora `accettato`, non `completato` — quindi ancora scrivibile — perché `chiusura_incassata` non era ancora true), un'altra Attività smette di essere verde (es. una nuova "Attività non preventivate" appena creata, ancora rossa). **L'utente ha scelto**: reset automatico a `false` (non solo blocco per nuove modifiche) — il checkbox torna non spuntato e disabilitato non appena la condizione precedentemente soddisfatta smette di esserlo, coerente col fatto che rappresenta "tutte le attività sono verdi ORA", non uno stato storico immutabile. **Design — due nuove funzioni condivise in `satelliti-meta.ts`** (leaf module, nessun rischio di import circolare tra `dettaglio-lavoro-data.ts` e `satelliti-render.tsx`, che si importano a vicenda tipo/funzione): `coloreQualsiasiSatellite(s, {haAllegati, haRighe})` — dispatcher per `tipo` che delega alle singole funzioni `colore*` già esistenti (nessuna logica nuova, un solo punto richiamabile con un satellite qualsiasi); `attivitaRilevantiPerChiusura(satelliti)` — esclude Chiusura stessa e le righe Preventivo storiche superate da una revisione più recente (stesso criterio "rilevante" già in uso in `lavori_dashboard()`/`kpi_dashboard()`/`costruisciVociAttivita`, che mostra sempre e solo la revisione corrente). **Calcolo primario in `dettaglio-lavoro-data.ts`** (dove allegatiById/righePerSatellite, necessari per Progetto/Acquisti, sono già disponibili senza query aggiuntive): nuovi campi `tutteAttivitaVerdi`/`attivitaNonVerdiCount` su `DatiLavoroSatelliti`, passati a `SatelliteChiusura` tramite `costruisciContenutoAttivita()` (`satelliti-render.tsx`). **Reset automatico implementato come side-effect al caricamento dati** (non al salvataggio di un'altra azione, che avrebbe richiesto agganciare ogni singola funzione `aggiorna*`/`crea*`/`elimina*` di ogni tipo satellite — rischio alto di dimenticarne una): se `chiusura.chiusura_conclusa===true` ma `!tutteAttivitaVerdi`, `caricaDatiLavoroSatelliti()` esegue una UPDATE correttiva (`chiusura_conclusa=false`) e muta in-place l'oggetto già dentro l'array `satelliti` (stesso riferimento restituito da `.find()`, i consumatori a valle — tabella, contenuto del satellite — vedono già il valore corretto senza una seconda query). **Gated su `isOwner`**: un ospite in sola lettura non avrebbe comunque permesso di scrittura via RLS — degrado accettabile e limite esplicitamente documentato nel codice, dato che il vincolo che conta davvero (impedire `lavoro.stato='completato'` con attività non verdi) resta comunque ri-verificato in modo indipendente e autoritativo al momento del salvataggio, non dipende da questo side-effect. **Scrittura best-effort** (un errore di rete transitorio qui non deve mai bloccare il caricamento della pagina). **UI** (`satellite-chiusura.tsx`): checkbox "Chiuso" `disabled={!tutteAttivitaVerdi}`; testo esplicativo sotto, stile grigio secondario già in uso nell'app (`text-xs text-gray-500`) — "Disponibile quando tutte le attività del lavoro sono concluse (N attività non ancora conclusa/e)." (singolare/plurale corretto), mostrato solo quando disabilitato. Checkbox "Incassato" **non toccata**, resta sempre abilitata (il vincolo si applica solo a "Chiuso", come richiesto esplicitamente). **Ri-verifica difensiva lato server** in `aggiornaChiusuraFlags()` (`lib/lavori/satelliti.ts`): quando `fields.conclusa===true`, un fetch dedicato (non riusa `caricaDatiLavoroSatelliti()`, troppi campi/query non necessari per questa sola verifica) ricalcola `tutteAttivitaVerdi` con le stesse due funzioni condivise — se non tutte verdi, l'azione fallisce con un errore esplicito, indipendentemente dal valore del `disabled` lato client (stesso principio "guardia UX primaria + ri-verifica server autoritativa" già in uso per il controllo "lavoro accettato" nella stessa funzione). **Verificato end-to-end** (Supabase locale, porte offset +1000, dev server + 3 script Playwright standalone, ambiente smontato a fine sessione): checkbox disabilitato su un Lavoro con molte attività reali non verdi (conteggio "6" mostrato correttamente, testo esplicativo presente); Lavoro nuovo minimale (solo Briefing+Preventivo) portato a "tutte verdi" (Briefing concluso, Preventivo accettato) → checkbox abilitato, nessun testo esplicativo, salvataggio riuscito (`chiusura_conclusa=true` in DB); creazione di una nuova "Attività non preventivate" (nasce rossa) → **reset automatico confermato a query diretta** (`chiusura_conclusa` tornato `false` nel DB senza alcuna azione esplicita dell'utente su quel satellite), checkbox mostrato non spuntato e di nuovo disabilitato; **bypass client-side deliberato** (rimozione manuale dell'attributo `disabled` via DOM, poi tentativo di salvataggio) → **bloccato dalla ri-verifica server-side**, messaggio d'errore "Non tutte le attività del lavoro sono ancora concluse" mostrato correttamente; checkbox "Incassato" confermato sempre abilitato indipendentemente dal vincolo; i 5 campi calcolati ancora presenti e funzionanti (nessuna regressione). 27 controlli totali su 4 script, zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione) puliti. **Committato e deployato su apphub**. Nessuna migration (solo application code). |
| 2026-08-15 | **Sessione di correzione — modale Acquisto, un decimale per Quantità e Prezzo**. **Quantità**: `<input type="number" step="0.001">` (frecce native, permettevano variazioni non intenzionali) sostituito da `<input type="text" inputMode="decimal">` — le frecce spariscono per costruzione (proprietà del solo `type="number"`), nessun CSS necessario. Nuovo `filtraQuantita()` (`satellite-ordine.tsx`) rianalizza l'intero testo ad ogni digitazione (non carattere-per-carattere): accetta **sia virgola sia punto** come separatore decimale, un solo separatore, al più una cifra dopo — nessuna ambiguità possibile qui perché, a differenza di Prezzo, il campo non ha mai una mascheratura/formattazione live che inietti un punto delle migliaia da confondere con un decimale digitato. **Prezzo**: `InputValuta` (`components/input-valuta.tsx`) esteso con una prop opt-in `decimali?: 1` (assente = comportamento di sempre, **zero regressione verificata** sugli altri 5 campi che la usano senza specificarla — Acconto, Preventivo, Noleggio, Spesa non preventivata, Referenze — "decisione esplicita col 10/8, nessun decimale" resta intatta per loro). Con `decimali={1}`, **solo la virgola** è un separatore decimale valido in digitazione, non il punto — non una scelta stilistica: il punto compare già nel testo mostrato come separatore delle migliaia iniettato dalla formattazione stessa (es. "1.234"), e un parsing che accettasse anche quello digitato dall'utente perderebbe la distinzione non appena l'utente rimuove la virgola con Backspace (**bug riprodotto empiricamente durante l'implementazione, non ipotizzato**: digitando "1.234,7" e premendo Backspace due volte per tornare a "1234", un parsing "ultimo separatore trovato" nel testo mascherato reinterpreta il punto delle migliaia residuo come decimale, producendo "1,2" — corretto restringendo la digitazione alla sola virgola, mai iniettata dalla formattazione finché non c'è una parte decimale, quindi mai ambigua). **Due tentativi di implementazione scartati dopo verifica prima di arrivare a quello finale**: (1) un parsing carattere-per-carattere (diff tra testo prima/dopo ogni tasto) risolveva l'ambiguità ma si rompeva con "seleziona tutto e digita" (pattern comune col tap su un campo compilato, specialmente mobile) — un test dedicato lo ha confermato prima di scartarlo; (2) un parsing "a blocco" che accettasse anche il punto come decimale riproduceva il bug Backspace sopra descritto. La versione finale (a blocco, solo virgola) evita entrambi. `formattaValuta()` (`lib/formato-valuta.ts`) esteso con un secondo parametro opzionale `decimali` (default 0, stesso principio opt-in) — `minimumFractionDigits: 0` anche con `decimali>0`: un valore che risulta intero si mostra "€ 250", non "€ 250,0", il decimale compare solo quando c'è davvero. **Valore complessivo** (Acquisto): passato a `decimali={2}` in visualizzazione (non 1) — è il prodotto di Prezzo×Quantità, entrambi a 1 decimale, quindi può avere matematicamente fino a 2 decimali (es. 12,5 × 3,5 = 43,75). **Arrotondamento esplicito a 2 decimali** aggiunto sia lato client (`totaleRighe()`, satellite-ordine.tsx) sia lato server (`valoreComplessivoRighe()`, lib/lavori/satelliti.ts, stessa formula) — la moltiplicazione in virgola mobile di due valori a 1 decimale può introdurre artefatti oltre i 2 decimali attesi (es. 12,3 × 3,7 → 45.510000000000005 in JS), mai visibile prima d'ora perché Prezzo/Quantità erano sempre interi. **Ripple non esplicitamente richiesto ma necessario, applicato per evitare un bug reale**: il campo "Prezzo indicativo" del catalogo Referenze standalone (`components/profilo-referenze-form.tsx`, Profilo/Impostazioni) modifica la stessa colonna `ultimo_prezzo` che ora può legittimamente contenere un decimale (sincronizzata da un Acquisto) — lasciato a `decimali` assente (0 decimali) avrebbe **arrotondato silenziosamente e perso il decimale** alla prima modifica di una Referenza da quella pagina; esteso anch'esso a `decimali={1}` (creazione, modifica, visualizzazione). Nessuna migration: le colonne DB (`prezzo_unitario numeric(12,2)`, `quantita numeric(10,3)`, `valore_complessivo numeric(12,2)`, `referenza.ultimo_prezzo numeric(12,2)`) accettavano già più precisione di quella ora esposta in UI. **Verificato end-to-end** (Supabase locale, porte offset +1000, dev server + script Playwright standalone, ambiente smontato a fine sessione con verifica hash SHA-256 di `.env.local`, stessa disciplina delle sessioni precedenti — un `tsc --noEmit` post-smontaggio ha inizialmente fallito su un file generato `.next/dev/types/validator.ts` corrotto dal kill brusco del dev server, non un problema reale: risolto con `rm -rf .next` + rebuild pulita, tsc/eslint/build torna pulito): 22 controlli — Quantità: nessuna freccia nativa (`type="text"` confermato), intera, con virgola, con punto, troncata a 1 decimale da un input a 3 cifre; Prezzo: intero, con virgola, **punto digitato correttamente ignorato** (verificato che produca il comportamento documentato, non un crash), migliaia+virgola, **sequenza Backspace attraverso la virgola verificata esplicitamente** (torna a "€ 1.234", non al bug "€ 1,2"), **seleziona-tutto-e-digita verificato esplicitamente** (sostituisce il valore intero, non lo altera parzialmente); salvataggio con Prezzo=12,5/Quantità=3,5 → Valore complessivo live "€ 43,75" corretto, persistito a query diretta (`prezzo_unitario=12.5`, `quantita=3.5`, `valore_complessivo=43.75`, `referenza.ultimo_prezzo=12.5`); **regressione Acconto verificata esplicitamente** (stesso componente, decimali non richiesto: virgola digitata ignorata come sempre, comportamento invariato); 0 errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). Nessuna migration (solo application code). |
| 2026-08-16 | **Sessione di correzione — tabella Attività su mobile (overflow) + etichette Acquisto (numerazione/parentesi)**. **Punto 1 — overflow mobile**: la colonna Stato (`components/lavoro-satelliti-tabella.tsx`) mostra ora solo il pallino semaforo sotto `sm:` (l'etichetta testuale — "Concluso", "In attesa", ecc. — è avvolta in `hidden sm:inline`), invariata da `sm:` in su (pallino + etichetta come oggi). `title={statoLabel}` aggiunto sul contenitore per non perdere del tutto l'informazione testuale su mobile (tooltip al tocco prolungato/hover, stesso principio già in uso per le icone Occhio/Matita/Cestino della colonna Azioni). **Punto 2 — etichette Acquisto**: verificato prima di procedere, come richiesto — la numerazione (`nomeNumerato()`, satelliti-render.tsx) è condivisa da tutti i tipi ripetibili (Briefing/Acconto/Campionatura/Verifica misure/Costruzione/Noleggio/Montaggio/Attività non preventivate) ed è rimasta **invariata per tutti loro**; le parentesi invece venivano da `satelliteTipoLabelBreve()` (satelliti-meta.ts), usata **di fatto solo per Acquisto** in entrambi i suoi punti di chiamata nonostante fosse scritta in modo generico (rami per Appuntamento/Campione mai raggiunti — **scoperta incidentale**: quei rami, insieme alla mappa `TIPO_SATELLITE_LABEL_BREVE` e a `SOTTOTIPO_APPUNTAMENTO_LABEL`, erano già codice morto **prima** di questa sessione, non introdotto da essa — rimossi insieme, stesso principio di pulizia già applicato nelle sessioni precedenti). Nuova `labelAcquisto(s)` (satelliti-render.tsx, sostituisce entrambe le chiamate a `nomeNumerato(g.acquisti, ...)`/`satelliteTipoLabelBreve(...)` per questo tipo): `"Acquisto"` senza categoria, `"Acquisto Categoria"` (spazio, non parentesi) con categoria — **mai numerata**, a differenza di ogni altro tipo ripetibile. Istanze diverse con la stessa categoria condividono quindi la stessa etichetta in elenco: accettato esplicitamente dall'utente, si distinguono aprendole. Applicata in entrambi i punti in cui il nome di un'istanza Acquisto viene costruito — la riga della tabella (`costruisciVociAttivita`) e l'header della modale di dettaglio (`costruisciContenutoAttivita`, `titoloConPallino`) — per coerenza tra i due (prima potenzialmente disallineabili solo in teoria, essendo generati dalla stessa funzione, ma verificato comunque esplicitamente in entrambi i punti). **Verificato end-to-end** (Supabase locale, porte offset +1000, dev server + script Playwright standalone — **incidente in questa sessione**: uno script diagnostico ad-hoc ha causato un `git stash`/timeout che ha temporaneamente svuotato la working tree; rilevato subito dal `grep` di verifica post-fatto (funzione attesa assente), recuperato con `git stash pop` senza perdita di lavoro, `tsc`/`eslint` e l'intera suite Playwright rieseguiti da capo per conferma dopo il recupero — ambiente smontato a fine sessione con verifica hash SHA-256 di `.env.local`, stessa disciplina delle sessioni precedenti): 17 controlli — nessun overflow orizzontale (`scrollWidth − clientWidth ≤ 0`) a 360/390/412px (range S24/S26) né a 1280px desktop; etichetta di stato assente su mobile ma pallini sempre presenti; etichetta di stato **ancora visibile** su desktop (regressione esplicita); `"Acquisto"` (nessuna categoria, nessun numero), `"Acquisto Ferramenta metallica lavorata"` su **2 istanze con la stessa categoria** (stessa etichetta esatta su entrambe, nessuna parentesi/numero residuo verificato con un pattern-match negativo su tutta la tabella); **regressione Acconto verificata esplicitamente** (`Acconto 1`/`Acconto 2`, numerazione invariata su un tipo diverso da Acquisto); 0 errori console/pageerror. Rilevato anche, incidentalmente durante la verifica ESLint post-incidente, che `eslint` senza scoping esplicito include `supabase/.temp/` (bundle minificato dell'edge-runtime locale, generato da `supabase start`) — già correttamente escluso da git (`supabase/.gitignore`) ma non dal flat config ESLint del progetto (che non legge `.gitignore` automaticamente), quindi visibile **solo** in una sessione con un ambiente Supabase locale attivo, mai in CI/produzione: nessuna azione presa (rumore locale innocuo, richiederebbe toccare `eslint.config.mjs` per un beneficio marginale, fuori scope). `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). Nessuna migration (solo application code). |
| 2026-08-16 | **Unificazione Dashboard + Conclusi in un'unica vista `/lavori` con filtri + ricerca cliente**, elimina alla radice l'intero meccanismo "sezione di origine" introdotto il 13-14/8 (esisteva solo per distinguere due sezioni ora fuse in una). **Verifica preliminare** (come richiesto, prima di scrivere codice): (1) il badge rosso sulla voce "Dashboard" conta gli **appuntamenti scaduti** (`appuntamenti_scaduti_count()`, calcolato nel root layout, indipendente da qualunque filtro) — non un contatore di riga; (2) le due liste erano già strutturalmente simili — Dashboard su `lavori_dashboard()` (satelliti rossi/gialli/verdi, valore, `where stato in ('opportunita','accettato')` **hardcoded**), Conclusi su una query diretta più povera (niente satelliti/valore/azioni) — la vera duplicazione da consolidare era proprio quel filtro hardcoded, non due componenti separati; (3) il `Combobox` di "Nuovo lavoro" (`cercaClienti()`, `ilike '%q%'`, non fuzzy) è un **selettore** (click su un'opzione svuota la query e chiude la tendina) — comportamento sbagliato per "filtra la lista mentre digiti". **4 ambiguità poste esplicitamente prima di procedere, come richiesto, tutte risolte con l'utente**: (1) **ricerca cliente** — non il Combobox letterale (conflitto con "filtra mentre digiti"), ma un campo di testo semplice che riusa la stessa logica non-fuzzy, **client-side** (i nomi cliente delle righe già caricate sono già in memoria, nessuna query aggiuntiva per carattere digitato) con sincronizzazione dell'URL debounced (500ms, `router.replace`) solo per la condivisibilità richiesta esplicitamente ("ricerca come query string"); (2) **KPI per filtro** (opzione scelta dall'utente, non quella "KPI fissi"): 1° KPI = conteggio del filtro corrente (non più da `kpi_dashboard()`, semplicemente `lavori.length` della lista già caricata — nessuna nuova query); "Importi da incassare" → **"Valore totale generato"** su Conclusi (nuovo, stessa formula di "Valore complessivo" di Chiusura Lavoro ma sommata sui soli `completato`) e **nascosto** su Rifiutati (nessun valore economico sensato per quel sottoinsieme); "Tempo medio preventivo"/"completamento" restano su In corso/Conclusi/Tutti, **entrambi nascosti** su Rifiutati (nessuna media isolata per quel solo sottoinsieme, mostrare la media globale sarebbe fuorviante) — Rifiutati mostra quindi **un solo** KPI (il conteggio); (3) **ordinamento Rifiutati** — confermato con l'utente che `impostaPreventivoDecisione()` è l'**unico** punto del codice che porta `lavoro.stato` a `'rifiutato'` (grep mirato, nessun altro percorso), sempre nello stesso momento in cui `preventivo_rifiutato=true` viene scritto sullo stesso satellite: `data_ultimo_cambio_stato` del Preventivo rilevante è quindi un proxy affidabile della data di rifiuto (un Lavoro rifiutato non ha quasi mai una Chiusura, rimossa quando esce da `accettato` senza diventare `completato` — `chiusura_data` non sarebbe stato un proxy valido lì); (4) **badge menu** — **resta un alert "appuntamenti scaduti" indipendente dal filtro**, NON diventa un contatore: l'utente ha corretto la mia stessa proposta iniziale ("il badge riflette il filtro attivo"), motivando che è un segnale SLA più importante di un conteggio, che comunque resta visibile nel 1° KPI della pagina. **Migration `0049`**: `lavori_dashboard()` droppata e ricreata con `p_filtro text default 'in_corso'` (`'in_corso'|'conclusi'|'rifiutati'`, qualunque altro valore = nessun filtro/"Tutti") — stesso corpo/join laterali di prima (`0044`), solo il `where` parametrizzato con un `case`; nuova colonna d'output `data_decisione_preventivo` (dalla stessa lateral già usata per `valore_preventivo_accettato`, nessuna query aggiuntiva). `kpi_dashboard()` droppata e ricreata con una colonna nuova, `valore_totale_completati` (riusa le CTE `preventivo_corrente`/`spesa_non_preventivata_somma` già presenti, sommate sui soli `stato='completato'`, senza sottrarre gli acconti a differenza di "Importi da incassare" — qui interessa quanto generato in totale, non quanto resta da incassare). **Ordinamento centralizzato in TS** (`lib/lavori/lista-filtri.ts`, nuovo — `ordinaLavori()`), non nel `order by` SQL (che differisce per filtro e dipende da dati di due tabelle diverse): `created_at asc` per In corso/Tutti (invariato), `chiusura_data desc` per Conclusi (stessa query mirata su `lavoro_satellite` già in uso nella vecchia pagina Conclusi, eseguita solo per quel filtro), `data_decisione_preventivo desc` per Rifiutati — in entrambi i casi "desc" un Lavoro privo della data rilevante finisce in fondo, mai in cima con una posizione arbitraria (stesso principio già in uso per Conclusi da sempre). **Componenti nuovi**: `components/filtro-lavori-chip.tsx` (`FiltroLavoriChip`, Server Component, Link+searchParams — stesso pattern già in uso nella vecchia pagina Conclusi, chip più larghe/thumb-friendly `px-4 py-2 text-sm` invece di `px-3 py-1 text-xs`, esplicitamente richiesto "niente hamburger da aprire"); `components/lavori-lista-filtrata.tsx` (`LavoriListaFiltrata`, Client Component — assorbe l'intera resa card/tabella che prima viveva in `page.tsx`, aggiunge solo il campo di ricerca e il filtro locale). `components/kpi-dashboard.tsx` riscritto: costruisce un array di card in base al `filtro` invece di 4 card fisse, griglia dinamica (`GRID_PER_COLONNE`, 1→4 colonne). **Rimozione completa del meccanismo "sezione di origine"** (introdotto il 13/8, esteso il 14/8): `lib/nav/origine-sezione.ts`/`.server.ts` e `components/origine-link.tsx` **eliminati**; `middleware.ts` non scrive più alcun cookie su `/lavori`/`/statistiche` (resta solo un redirect di cortesia `/statistiche` → `/lavori?filtro=conclusi` per bookmark vecchi); `app/layout.tsx`/`components/app-nav.tsx` non leggono/propagano più `origineSezione`; i link "← Dashboard/Conclusi" tornano ad essere `Link` server-renderizzati hardcoded — "← Lavori" nel dettaglio Lavoro, "← Clienti"/"← Fornitori" su Cliente/Fornitore (prima "← {origine}" per via della catena, ora un semplice ritorno al proprio elenco: più diretto, dato che l'unica ragione per seguire l'origine — due sezioni da distinguere — non esiste più). Come conseguenza, anche il workaround `prefetch={false}` sui Link di menu (introdotto il 14/8 contro la corruzione del cookie da prefetch automatico) non serve più — rimosso, i Link tornano al comportamento di prefetch di default. **Menu**: voce "Conclusi" rimossa; "Dashboard" rinominata **"Lavori"** (scelta stilistica, non vincolata dalla richiesta — più diretta, coerente col path `/lavori` e con "Lavori associati" già in uso altrove nell'app). **Voce "Test" rimossa dal menu** (richiesta aggiunta a metà sessione): bottone/`ModalTest`/stato `apertoTest` tolti da `components/app-nav.tsx` (desktop e mobile) — **`components/test/modal-test.tsx` NON eliminato**, lasciato sul filesystem: è un ambiente di iterazione rapida sul design tuttora citato come riferimento storico in una dozzina di commenti sparsi nel codice (Data+Ora affiancate, fix tastiera mobile, dimensioni Modal condivisa…), non una pagina di prova usa-e-getta — resta raggiungibile solo da codice/dev, non più da UI. **Segnalato esplicitamente come richiesto**: nessun'altra utility di test/debug reale trovata nella sezione "Test" oltre a questa Modal — nessun'altra ambiguità aperta su cosa tenere/buttare. **Verificato end-to-end** (Supabase locale, porte offset +1000, dev server + 2 script Playwright standalone, dati di test scaglionati per tutti e 4 gli ordinamenti — 3 Lavori in-corso a 10/5/1 giorni fa, 3 completati con chiusura a 20/10/2 giorni fa, 2 rifiutati con decisione preventivo a 15/3 giorni fa **backdatata in due UPDATE separati** per non farla sovrascrivere da `set_satellite_data_ultimo_cambio_stato` — bug di test scoperto e aggirato, non un bug applicativo —, ambiente smontato a fine sessione con verifica hash SHA-256 di `.env.local`): 53 controlli — i 4 filtri (conteggio e ordine riga-per-riga esatti su ciascuno, incluso Rifiutati con date di decisione volutamente invertite rispetto a `created_at` per isolare l'ordinamento corretto dal caso banale); KPI dinamici per ciascun filtro (label/valore esatti, inclusi i 4→1 su Rifiutati e i due nuovi "Valore totale generato"/conteggio); ricerca cliente combinata con ogni filtro (incluso un cliente con lavori in più stati contemporaneamente, verificato che compaiano entrambi sul filtro giusto), KPI **non** influenzato dalla ricerca, sincronizzazione URL post-debounce, persistenza su reload, preservazione della ricerca al cambio filtro, messaggio "nessun risultato"; badge menu **invariato** (mostra "1" per un appuntamento scaduto di test, mai il conteggio "3"/"8" del filtro attivo, verificato su due filtri diversi); redirect `/statistiche`; link "← Lavori"/"← Clienti"; **Back/scroll non regrediti** (stessa garanzia del refactor Parallel Routes del 12/8, non toccato da questa sessione); mobile 390px (nessun overflow, vista card, chip ≥32px di altezza). Zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. **Deploy inizialmente sospeso** (il codice chiama `lavori_dashboard(p_filtro)` incondizionatamente — deployarlo prima della migration avrebbe rotto `/lavori` per intero, stesso rischio già gestito per Costruzione/Montaggio il 12/8 e Referenze il 14/8; nessuna credenziale di connessione diretta Postgres disponibile né in locale né sul server per applicarla da sola) — **migration `0049` applicata dall'utente su Supabase Cloud durante la stessa sessione**, verificata indipendentemente via introspezione dello schema OpenAPI di PostgREST (`GET /rest/v1/`, il parametro `p_filtro` risultava presente nella definizione di `/rpc/lavori_dashboard` solo dopo l'applicazione — nessun accesso Postgres diretto necessario per questo tipo di verifica), poi **deploy eseguito** (stesso accesso SSH `root@178.105.199.29`): `git pull`+`docker compose build`+`up -d`, container ricreato, verificato `200` su `/login` (porta diretta `3002` e `https://districo.it`) e `307`→`/login` su `/lavori`/`/statistiche` (atteso, nessuna sessione — nessun errore 500), log del container puliti. |
| 2026-08-17 | **Rinomina filtro "Conclusi"→"Completati" + fix bug conteggio rosso/verde in elenco Lavori**, due correzioni indipendenti sulla stessa RPC. **Punto 1 — rinomina**: il filtro "Conclusi" (completato+rifiutato) era ridondante col chip "Rifiutati" già esistente — ora "Completati" filtra solo `stato='completato'`. Rinominata la chiave in tutto lo stack (`FiltroLavori`, `lib/lavori/lista-filtri.ts`): `'conclusi'→'completati'` in URL/param SQL/etichette (`FILTRO_LABEL`, `FILTRO_LABEL_CONTEGGIO`: "Totale lavori conclusi"→"Totale lavori completati"), `components/kpi-dashboard.tsx`, `components/filtro-lavori-chip.tsx`, `components/lavori-lista-filtrata.tsx` (messaggio vuoto), `app/(app)/lavori/page.tsx` (query mirata `chiusura_data`). **Verificato prima di toccare la formula**, come richiesto: "Valore totale generato" (`kpi_dashboard()`, CTE `valore_generato`) era **già** filtrata su `stato='completato'` fin dalla sua introduzione il 16/8 — i Rifiutati non vi avevano mai contribuito, nessun cambio di formula necessario, solo la rinomina della chiave che ne controlla la visibilità. Redirect di cortesia `/statistiche` aggiornato da `?filtro=conclusi` a **`?filtro=tutti`** (non `completati`): il vecchio "Conclusi" era un superset completato+rifiutato, "Tutti" resta l'equivalente più vicino per un bookmark legacy. **Punto 2 — bug conteggio rosso/verde**: riprodotto sui due Lavori segnalati (verificato via query dirette su Supabase Cloud, non ipotizzato) — "Manutenzione armadio"/Lucchesi Milena e "Sistemazione ante Achille"/Cogefrin spa hanno entrambi un'Attività Montaggio con `concluso=true` ma `sessioni_lavoro='[]'` (flusso legittimo: nulla obbliga a registrare una sessione prima di spuntare "Concluso"). **Causa esatta**: `coloreSessioniLavoro()` (satelliti-meta.ts, fonte di verità usata dal Dettaglio Lavoro) dà priorità a `conclusa` (verde) indipendentemente dalle sessioni — ma il predicato `rosso` per costruzione/montaggio in `lavori_dashboard()` (SQL, usata per l'aggregato in elenco) era `jsonb_array_length(sessioni_lavoro) = 0` **senza** il guard `not concluso` che hanno invece tutti gli altri tipi (appuntamento/preventivo/chiusura/acconto/spesa_non_preventivata/progetto, ciascuno con un `not <flag verde>` esplicito nel proprio predicato rosso) — quindi rosso e verde potevano risultare **entrambi true sulla stessa riga**, contata in **entrambi** `satelliti_rossi` e `satelliti_verdi` contemporaneamente (da qui "1 rosso, 5 verdi" in elenco mentre il Dettaglio mostrava tutto verde). **Stessa identica classe di bug trovata per ispezione mirata (non per analogia cieca) anche su Campione**: rosso = `campione_data_consegna is null` senza guard `not campione_consegnato` — la Data è liberamente opzionale in UI, nulla impedisce di spuntare "Consegnato" senza mai compilarla; corretto per lo stesso motivo anche se non è la causa dei due casi segnalati (nessuno dei due Lavori ha un'istanza Campione). **Verificato ed ESCLUSO** dallo stesso problema: Acquisti (`ordinato` verde, fornitore/righe mancanti rosso, nessun guard simmetrico) è comunque strutturalmente sicuro — `impostaOrdinatoAcquisto(true)` richiede fornitore+referenze già persistiti, e `lavoro_satellite.fornitore_sede_id` referenzia `fornitore_sede(id)` **senza** `on delete cascade/set null` (0009, FK RESTRICT di default) quindi non può tornare null dopo che `ordinato=true`; tutti gli altri 7 tipi hanno già il guard esplicito. **Non un bug del gate di Chiusura Lavoro**: il gate (`attivitaRilevantiPerChiusura`+`coloreQualsiasiSatellite`, satelliti-meta.ts) usa la stessa `coloreSessioniLavoro()`/`coloreCampione()` corrette già utilizzate dal Dettaglio — non ha mai valutato questi satelliti come rossi, ha sempre permesso la chiusura correttamente; il bug era isolato all'aggregato SQL separato di `lavori_dashboard()`, confermato **prima di procedere al fix** come richiesto (non ha richiesto di fermarsi). Migration `0050` (`create or replace`, nessun `drop` necessario: return type invariato) — aggiunto il guard mancante (`not coalesce(<flag verde>, false)`) ai predicati rosso di campione/costruzione/montaggio; stesso corpo per il resto, incluso il case `p_filtro` rinominato `'conclusi'→'completati'` con nuova semantica `stato = 'completato'` (era `in ('completato','rifiutato')`). **Verificato end-to-end** (Supabase locale, porte offset +1000, stesso ambiente/dati di test della sessione precedente riusati, dev server + 2 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di `.env.local`): **diagnosi confermata empiricamente con un confronto diretto prima/dopo** — stesso Lavoro sintetico (Montaggio `concluso=true`+sessioni vuote, Campione `consegnato=true`+data nulla, su un Lavoro `completato`) interrogato dentro una transazione con la funzione pre-fix (0049, poi rollback) → `satelliti_rossi=2, satelliti_verdi=5`; stessa query con la funzione post-fix (0050) → `satelliti_rossi=0, satelliti_verdi=5`, che riproduce esattamente il sintomo segnalato e conferma la correzione; 17 controlli totali — chip "Completati" presente/"Conclusi" assente; filtro Completati mostra solo `stato='completato'` (Rifiutati esclusi, verificato che non compaiano più insieme); KPI "Totale lavori completati"/"Valore totale generato" con valori esatti; filtro Rifiutati invariato; **elenco Lavori: nessun pallino rosso sul Lavoro di test, pallino verde "5" corretto**; Dettaglio Lavoro dello stesso Lavoro: 5 righe attività, nessun pallino rosso/giallo (coerenza elenco↔dettaglio confermata); ricerca cliente combinata col filtro rinominato; redirect `/statistiche`→`?filtro=tutti`. Zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. **Migration `0050` applicata dall'utente su Supabase Cloud durante la stessa sessione** (nessuna verifica indipendente possibile stavolta: a differenza della `0049`, questa non cambia la firma della funzione, quindi non c'è nulla di nuovo da osservare nello schema OpenAPI di PostgREST — accettata la conferma dell'utente, stesso principio già in uso altrove in questo file per le migration verificabili solo "a riscontro applicativo"). **Deploy eseguito** (stesso accesso SSH `root@178.105.199.29`): `git pull`+`docker compose build`+`up -d`, container ricreato, verificato `200` su `/login` (porta diretta `3002` e `https://districo.it`) e `307`→`/login` su `/lavori` (nessuna sessione, atteso), log del container puliti. |
| 2026-08-17 | **Catalogo Referenze standalone + revisione modale Acquisto (selezione-only, niente propagazione prezzo) + testo mail configurabile** — 4 parti indipendenti nella stessa sessione, correggono due decisioni prese il 14/8. **PARTE A — nuova sezione di menu "Catalogo"**: Categorie/Referenze spostate da Profilo/Impostazioni a `/catalogo` (voce propria nel menu, non dentro Impostazioni né dentro Fornitori — le Referenze sono legate a una Categoria personale, non a un Fornitore, vedi CLAUDE.md 14/8). Categorie invariate nel comportamento, solo spostate (`ProfiloCategorieAcquistoForm`→`CatalogoCategorieForm`, stesso file rinominato). Referenze passano a **soft delete**: nuova colonna `referenza.attiva boolean not null default true` (migration `0051`) — `eliminaReferenzaCatalogo()` ora fa un `update({attiva:false})` invece di un `delete()`; `cercaReferenze()` (usata dalla modale Acquisto) e il fetch di `/catalogo` filtrano entrambi `attiva=true`, quindi una Referenza eliminata sparisce sia dalle scelte per nuovi Acquisti sia dalla lista del Catalogo, ma resta a schema — gli Acquisti passati che la referenziano restano collegati alla riga originale (non solo a una copia congelata), a differenza del vecchio hard delete (`on delete set null`, 0048) che ora non scatta più in pratica. **PARTE B — Acquisto: solo selezione, mai creazione al volo**: CORREGGE la sessione del 14/8 (avvenuta durante una disconnessione) che aveva introdotto `ComboboxCreabile` per creare una Referenza al volo dentro l'Acquisto — rimossa (componente `combobox-creabile.tsx` eliminato, zero altri chiamanti), sostituita dal `Combobox` generico esistente (già in uso per Cliente/Fornitore). **PARTE C — prezzo per Acquisto, nessuna propagazione**: CORREGGE la stessa sessione — selezionare una Referenza precompila il Prezzo da `ultimo_prezzo` (proposta di default, invariato), ma salvare l'Acquisto non scrive più indietro su `ultimo_prezzo` (prima lo faceva ad ogni salvataggio, `salvaRigheOrdine()` in `lib/lavori/satelliti.ts`) — quel campo si modifica ora **solo** da `/catalogo`. **Verifica non ovvia fatta prima di restringere i tipi** (non "alla cieca"): interrogata Supabase Cloud prima di scrivere `RigaOrdineInput` come union — 6 righe reali in produzione con `referenza_id null` e descrizione libera (Acquisti creati prima della revisione del 14/8) su Acquisti **non ancora ordinati** (editabili): un tipo che rendesse `referenzaId` sempre obbligatorio le avrebbe fatte sparire silenziosamente al primo Salva successivo (il delete+insert di `aggiornaOrdine()` non le avrebbe più incluse). `RigaOrdineInput` resta quindi un'unione — `{referenzaId: string; ...}` per una riga scelta dal catalogo (server legge descrizione/colore da lì, non si fida più del client) o `{referenzaId: null; descrizione; coloreFinitura; ...}` per una riga storica preservata così com'è — ma **mai più costruibile lato client per una riga nuova** (nessun input libero rimasto in UI, `salvaComeReferenza` rimosso). Righe storiche/nuove mostrate identicamente in sola lettura nella modale, "Cambia" resta l'unico modo per agganciare anche una riga storica a una Referenza reale. **PARTE D — testo mail ordine, Apertura/Congedo configurabili**: nuovi campi `artigiano.mail_ordine_apertura`/`mail_ordine_congedo` (migration `0051`, nullable — null = usa il default applicativo). **Struttura del corpo mail chiarita con l'utente prima di implementare, come richiesto esplicitamente** (unica ambiguità posta in questa sessione): Apertura **sostituisce interamente** il vecchio saluto fisso "Ciao {nome contatto}," e include già il lead-in ("Avrei bisogno di:" o equivalente) — non due pezzi separati; Congedo **include anche la firma dell'artigiano** (non più una riga "Buon lavoro"/nome a parte). L'utente ha fornito i 4 testi di default esatti (non indovinati): Apertura informale "Ciao {nome contatto},\nAvrei bisogno del seguente materiale\n", Apertura formale "Buongiorno {nome contatto},\nLe scrivo per ordinare il seguente materiale\n", Congedo informale "Buon lavoro\n{nome artigiano}", Congedo formale "Resto a sua disposizione per ogni chiarimento.\nCordiali saluti.\n{nome e cognome artigiano}" — costanti in nuovo `lib/lavori/mail-ordine-testo.ts` (condiviso tra `ProfiloTestoMailForm`, il selettore Formale/Informale che pre-compila i due campi restando comunque liberamente modificabili dopo, e `ordini-email.ts`, che li usa come fallback quando l'artigiano non ha mai personalizzato). Placeholder testuali (`{nome contatto}`, `{nome artigiano}`, `{nome e cognome artigiano}`) sostituiti a runtime con gli stessi dati già letti per contatto/artigiano, nessun fetch aggiuntivo — `artigiano.cognome` aggiunto alla `select` di `inviaOrdineSatellite()` (prima leggeva solo `nome`). **Bug reale scoperto e corretto durante il testing** (non ipotizzato): sia `aggiornaTestoMail()` sia la lettura in `ordini-email.ts` facevano `.trim()` sul valore **effettivo** salvato/usato (non solo per decidere se fosse vuoto) — i default terminano deliberatamente con un `\n` (riga vuota prima dell'elenco referenze), un `.trim()` incondizionato lo perdeva silenziosamente ad ogni salvataggio/invio; riprodotto round-trip (digita→salva→reload, l'ultimo carattere `\n` spariva) prima di correggere entrambi i punti (trim solo per il controllo di vuoto, valore salvato/usato sempre quello originale). **Verificato end-to-end** (Supabase locale, porte offset +1000, `SMTP_CREDENZIALI_KEY` di test generata al volo — assente in locale, causa di un 500 diagnosticato e risolto durante il testing, nessun impatto sul codice applicativo — SMTP personale dell'artigiano di test puntato realmente a Mailpit locale tramite `smtp_port` scommentato in `config.toml`, dev server + 4 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di `.env.local`): CRUD Categorie/Referenze su `/catalogo`; soft delete verificato (referenza sparisce da Catalogo e dalla ricerca Acquisto, resta in DB); riga storica "ad hoc" precaricata (simulata identica alle 6 reali di produzione) verificata **sola lettura** (nessun input editabile) e **preservata intatta** dopo un Salva che aggiunge anche una riga nuova selezionata dal catalogo; nessuna affordance "Crea nuova referenza" nella modale; prezzo precompilato da `ultimo_prezzo` (€0,5), modificato per il singolo Acquisto (€9,90) — **`ultimo_prezzo` sulla Referenza verificato invariato a query diretta dopo il salvataggio** (unico punto davvero critico di questa sessione); creazione di un Acquisto interamente nuovo (`creaOrdine()`, non solo `aggiornaOrdine()`) verificata senza errori; **invio ordine reale end-to-end** (Mailpit): corpo mail ricevuto ispezionato byte per byte — Apertura con nome contatto sostituito, lead-in personalizzato, elenco referenze corretto (storica + nuova), **nessun prezzo nel testo** (invariato, non da comunicare al fornitore), Congedo con nome e cognome artigiano sostituiti, **nessun segnaposto `{...}` residuo**. 26 controlli totali, zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. **Migration `0051` applicata dall'utente su Supabase Cloud durante la sessione stessa** (comunicato a metà lavoro, prima ancora del testing), verificata indipendentemente via REST (`referenza.attiva`/`artigiano.mail_ordine_apertura`/`mail_ordine_congedo` tutte interrogabili, `200`) — poi **deploy eseguito** (stesso accesso SSH `root@178.105.199.29`): `git pull`+`docker compose build`+`up -d`, container ricreato, verificato `200` su `/login` e `307`→`/login` su `/catalogo` (nessuna sessione, atteso), log del container puliti. |
| 2026-08-17 | **Mail d'ordine ai fornitori in HTML con banner responsive**, completa la sessione precedente (Apertura/Congedo configurabili) — l'intera mail (prima solo testo semplice via 3 `<p>`) diventa un documento HTML completo con banner cliccabile, tabella referenze con prezzi, layout table-based compatibile client mail. **Verifica preliminare fatta come richiesto**: asset grafici esistenti in `public/` (loghi SVG vettoriali puri, `districo_logo.svg`/`districo_logo_payoff.svg`, e un precedente `public/email-assets/districo-logo-payoff.png` già usato nei template Supabase Auth "Confirm signup"/"Reset password", CLAUDE-ARCHIVIO.md 19/7) — stessa cartella riusata per i nuovi asset. **Discrepanza corretta senza bisogno di fermarsi**: il prompt indicava "Python + cairosvg + PIL, già usato per asset grafici" — verificato che non è vero per questo progetto (cairosvg non installato, nessuno script Python nel repo); il toolchain realmente già in uso per questo stesso scopo (stesso identico caso d'uso: SVG logo → PNG per email) è **`sharp`** (Node, già dipendenza del progetto per il resize degli allegati immagine) — usato quello, coerente col precedente reale invece che con quanto indicato nel prompt. **Bug di fedeltà scoperto e risolto durante la generazione, non ipotizzato**: il marchio "distrìco" richiede un accento grave in combinazione (`&#x0300;`) su una "ı" (i senza puntino) — con i font fallback Georgia/Arial (assenti su questo host, sostituiti da fontconfig con DejaVu/Liberation) quella combinazione non compone in un accento pulito ma produce un cerchio spezzato, visivamente rotto. **Non ridisegnato come testo**: il vero logo (`public/districo_logo_payoff.svg`) è già vettorializzato in puri `<path>` (nessun `<text>`, commit storico "Logo come vettori puri: fix accento fuori posto", verificato nel repo) proprio per evitare questo tipo di problema — riusato quello via composizione raster (`sharp .composite()`, non `<image>`/SVG-nested: librsvg non risolve in modo affidabile riferimenti a file esterni), non ridisegnato. Le due SVG dei banner (`public/email-assets/ordine-banner-orizzontale.svg`/`-impilato.svg`, versionate) contengono quindi solo il "frame" (sfondo, linea divisoria, testo tagline in Arial — questo sì reso bene dal fallback, nessun problema) — il logo viene composto sopra a runtime di generazione, non nel markup SVG stesso. PNG finali (`ordine-banner-orizzontale.png` 1360×300, `ordine-banner-impilato.png` 1360×400, entrambi 2x come richiesto) generati con uno script Node one-off (non versionato, stesso principio già in uso per il logo email del 19/7). **Swap responsivo**: due `<img>` (uno per banner, entrambi dentro lo stesso `<a href="{siteUrl}">` cliccabile — nuovo `siteUrl()` esportato da `lib/email/templates.ts`, prima privata, riusata qui per link+URL assoluti dei PNG, le mail non possono referenziare percorsi relativi), `<style>` in `<head>` con `@media (max-width: 480px)`: orizzontale `display:block` di default (fallback per Outlook desktop e altri client che ignorano le media query, esplicitamente richiesto), impilato `display:none` di default, invertiti sotto i 480px. **Tabella referenze — INVERTE la decisione del 14/8** ("il prezzo resta fuori dall'email... non un dato da comunicare al fornitore"): colonne Descrizione/Quantità/Prezzo unitario/Totale riga, riga finale con il Totale complessivo — richiesto esplicitamente in questa sessione, trattato come reversal deliberato (non un'ambiguità: la struttura delle colonne era già specificata per intero) — una mail d'ordine è a tutti gli effetti un ordine d'acquisto, il prezzo è informazione attesa lì. Totale complessivo dal campo persistito `valore_complessivo` (già calcolato server-side, non ricalcolato). Righe storiche prive di prezzo (dato pre-esistente, vedi sessione precedente sulla preservazione delle righe "ad hoc") mostrano "—" invece di un numero fuorviante. Nuovi moduli: `lib/lavori/ordine-mail-html.ts` (puro, banner+tabella+documento completo — `escapeHtml()` su ogni valore testuale prima di iniettarlo, inclusi Apertura/Congedo dopo la sostituzione dei placeholder) — `lib/lavori/ordini-email.ts` ridotto a orchestrazione (fetch dati, poi chiama `corpoMailOrdine()`), `righeTesto`/il vecchio corpo a 3 `<p>` rimossi. **Verificato end-to-end** (Supabase locale, porte offset +1000, `smtp_port` di Mailpit scommentato in `config.toml` — stesso artigiano/Acquisto di test della sessione precedente, ancora con SMTP puntato a Mailpit — dev server + 2 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di `.env.local`): 16 controlli sul markup HTML ricevuto via API Mailpit (documento completo, media query presente, ordine dei due `<img>` corretto, stato `display` di riposo corretto per entrambi, banner dentro il link, Apertura/lead-in/Congedo con placeholder sostituiti, intestazioni tabella, righe con quantità/prezzo/totale corretti, totale complessivo, nessun segnaposto residuo) — **ispezione visiva via screenshot della vista Mailpit reale** (non solo markup) a due viewport, 1000px (banner orizzontale visibile, tabella pulita, logo con accento corretto) e 400px (**swap confermato**: banner impilato mostrato, orizzontale nascosto — la tabella si restringe ma resta leggibile, nessun overlap); **caso limite verificato esplicitamente**: una riga con `prezzo_unitario null` (dato storico) mostra "—" su prezzo e totale invece di un valore inventato, nessun crash. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. Nessuna migration (solo asset statici + application code). **Deploy eseguito** (stesso accesso SSH `root@178.105.199.29`): `git pull`+`docker compose build`+`up -d`, container ricreato; verificato non solo `200` su `/login` ma anche, esplicitamente, che i due PNG del banner rispondano `200` da `https://districo.it/email-assets/...` — un ordine reale inviato subito dopo il deploy avrebbe altrimenti mostrato immagini rotte (stessa attenzione già documentata per `districo-logo-payoff.png` il 19/7: un asset nuovo va deployato, non basta il commit). |
| 2026-08-18 | **Sessione di correzione — Acquisto allineato allo standard, mail d'ordine senza prezzi con banner in calce, verifica Formale/Informale, report schema Referenza**. Quattro punti indipendenti + una verifica pura, nessuna migration. **1) Modale Acquisto — allineamento allo standard**: era rimasta indietro su 3 punti rispetto a ogni altro satellite dal template Briefing (10/8) — Data ora editabile (`aDateLocal()`/NOT NULL validata client-side con asterisco rosso, stesso pattern esatto di Preventivo/Progetto, verificato riga per riga); testo standard allegati aggiunto, `"Puoi allegare foto e documenti inerenti all'ordine (file di immagine e PDF)."` (corretto anche l'errore di battitura "al ordine"→"all'ordine" della richiesta originale, segnalato dall'utente); checkbox rietichettata `"Contrassegna l'acquisto come effettuato."` (era "Ordinato"). **Quarta deviazione trovata durante la verifica sistematica richiesta** ("segnala altri scostamenti anche se non li correggi"): mancava il wrapper `pb-24` quando `dirty` — le `PilloleSalvaAnnulla` (fixed) potevano coprire l'ultima riga di contenuto, stesso bug già noto e corretto altrove (Acconto) — corretto anche questo, non solo segnalato, essendo diretta conseguenza dello stesso allineamento. Nessun'altra deviazione trovata (confronto riga per riga con Acconto/Preventivo/Progetto: struttura pillole, DialogConferma, useDirtyForm, useProteggiChiusuraModal tutti già allineati). `aggiornaOrdine()` (`lib/lavori/satelliti.ts`) estesa con `dataCreazione`. **2) Mail d'ordine — nessun prezzo al fornitore**: tabella referenze (`ordine-mail-html.ts`) perde le colonne Prezzo unitario/Totale riga/Totale complessivo — CORREGGE la sessione del 17/8 che le aveva introdotte: il prezzo resta un dato interno (modale Acquisto, per Spese complessive/Margine di Chiusura Lavoro), non da comunicare al fornitore. Solo Descrizione+Quantità in tabella. `ordini-email.ts` non seleziona/passa più `prezzo_unitario`/`valore_complessivo` per l'email. **3) Banner mail — spostato in calce, centrato in entrambe le varianti (richiesta corretta a metà sessione dall'utente)**: la richiesta iniziale era stata fraintesa come "elimina lo swap responsivo, usa solo la versione impilata" — l'utente ha corretto esplicitamente durante l'implementazione: lo swap a due immagini (orizzontale su schermi larghi/impilata sotto 480px, via media query, invariato dal 17/8) **resta**, l'unico intervento reale è il centraggio esplicito di **entrambe** le varianti (prima solo l'impilata aveva `margin:0 auto`, l'orizzontale si affidava solo a `width:100%` per riempire la cella — non un vero centraggio, a rischio con client basati sul motore Word di Outlook desktop) — aggiunto `margin:0 auto` anche all'orizzontale **e** `align="center"`/`text-align:center` sulla `<td>` contenitore (tecnica bulletproof standard per le mail). Banner spostato dalla testa (prima riga della card) al fondo (dopo il Congedo, nuova `<tr>` con lo stesso padding orizzontale 28px del testo — l'orizzontale passa da 600px pieno-bleed a 544px, la larghezza utile della colonna di testo, per restare visivamente allineato sotto il Congedo invece che a filo bordo). Link cliccabile verso districo.it invariato. **4) Selettore Formale/Informale — verificato, NON corretto (in attesa di conferma)**: come richiesto esplicitamente, verificato lo stato reale prima di intervenire — **non è un bug**, `applicaTono()` (`components/profilo-testo-mail-form.tsx`) funziona correttamente (click su "Formale"/"Informale" precompila davvero Apertura/Congedo con il preset corrispondente, verificato dal vivo: "Formale"→"Buongiorno...", "Informale"→"Ciao..."). **Il problema è di visibilità/chiarezza**, confermato via screenshot della sezione "Testo mail ordine" in Profilo/Impostazioni: i due bottoni "pillola" compaiono subito sotto il paragrafo descrittivo della sezione, ma quel paragrafo parla solo di cosa sono Apertura/Congedo — **non menziona affatto i due bottoni** né spiega che sono scorciatoie di precompilazione; inoltre **nessuno stato "attivo" visibile** dopo il click (nessun `aria-pressed`, nessuna classe distintiva) — l'unico segnale che qualcosa sia cambiato è il testo nelle textarea sotto, facile da non notare a uno sguardo veloce. **Soluzione proposta, NON implementata** (come richiesto esplicitamente per un problema di visibilità, non di funzionalità): una didascalia breve sopra i due bottoni, es. `"Applica un tono predefinito:"` (`text-xs text-gray-500`, coerente con lo stile delle altre didascalie già in uso nella stessa sezione, es. quella dei segnaposto) — intervento minimo, un solo paragrafo di JSX, nessuna logica toccata. In attesa di conferma dell'utente prima di implementarla. **5) Report — campi attuali di Referenza (nessuna modifica di schema)**: verificato lo schema DB reale via introspezione OpenAPI diretta su Supabase Cloud (non solo le migration) — la tabella `referenza` ha esattamente 8 colonne: `id` (PK), `artigiano_id` (FK→`artigiano.id`), `categoria_id` (FK→`categoria_acquisto.id`), `descrizione`, `colore_finitura` (opz.), `ultimo_prezzo` (opz.), `created_at`, `attiva` (soft delete, 14/8). Nessun campo tipo codice prodotto/unità di misura/note — coerente con le migration `0048`+`0051`, nessuna migration intermedia mai toccata questa tabella oltre a quelle due. **Verificato end-to-end** (Supabase locale, porte offset +1000, riusato un ambiente con dati di test reali da sessioni precedenti — 18 artigiani/20 lavori già presenti da un backup, incluso l'utente `test-unificazione@example.com` con Acquisti di test già pronti — GRANT manuali rifatte da zero, `SMTP_CREDENZIALI_KEY` rigenerata e SMTP dell'artigiano di test ripuntato a Mailpit locale via UI, dev server + 3 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di sia `.env.local` sia `config.toml`): 11 controlli su modale Acquisto (Data editabile con asterisco, testo allegati esatto incluso l'apostrofo corretto, testo checkbox esatto, pillole+wrapper `pb-24` dopo una modifica) + 19 controlli su un invio ordine reale via Mailpit (documento HTML completo, media query di swap presente, ordine/stato di riposo dei due `<img>` invariato, **`margin:0 auto` presente su entrambe le varianti**, **`align="center"` sulla cella banner**, banner verificato posizionato dopo il Congedo, tabella con sole Descrizione/Quantità — nessun valore in euro nel corpo mail, nessun segnaposto residuo) — **ispezione visiva via screenshot della vista Mailpit reale a due viewport** (700px: banner orizzontale centrato sotto il Congedo; 420px: swap confermato, banner impilato centrato, nessun overlap) — più screenshot della modale Acquisto e della sezione Formale/Informale per il punto 4. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). Nessuna migration. |
| 2026-08-19 | **Sessione di correzione — sovrapposizione visiva Acquisto, decimali mai implementati (Quantità), tono mail (didascalia+stato attivo), nuovo campo Referenza "Codice"**. Quattro punti indipendenti, migration 0052. **1) Sovrapposizione visiva riga Allegati/Flag in Acquisto**: verificato sistematicamente su TUTTI gli altri satelliti con entrambi gli elementi (Acconto, Campione, Costruzione, Montaggio, Noleggio, Preventivo, Progetto, SpesaNonPreventivata) — nessuno affetto, checkbox e Allegati sono da sempre figli diretti dello stesso `space-y-3` che avvolge l'intero form. **Acquisto era l'unico caso** (non un bug del componente condiviso `AllegatoTrigger`/`AllegatoLista`, mai toccato): i due blocchi vivevano fuori da qualunque `space-y-*` condiviso, ciascuno con un `mb-2` indipendente — il distacco reale, ridotto dal margin collapsing tra div vuoti/senza padding, finiva sotto la soglia percepita come "riga separata". **Fix**: entrambi i blocchi (+ "Ordine inviato"/errore invio) spostati dentro un unico `<div className="space-y-3">`, stesso pattern di ogni altro satellite — **ordine invertito** (checkbox ora PRIMA di Allegati, Acquisto era l'unico con l'ordine opposto, nessun'altra ragione lo giustificava). **2) Decimali Quantità — bug reale trovato, non "mai implementato" come descritto**: verificato che Prezzo (`InputValuta` con `decimali={1}`, dalla sessione 2026-08-15) funzionasse già correttamente prima di modificare qualunque cosa — confermato. Il vero difetto era **solo su Quantità**: `filtraQuantita()` normalizza correttamente la virgola digitata a un punto interno (coerente con `Number()`), ma il campo mostrava quello stato interno grezzo invece di riformattarlo per la visualizzazione — digitare "1,5" faceva apparire "1.5" nel campo (il valore salvato era comunque corretto, ma sembrava che la virgola "non venisse accettata", da cui la segnalazione). Fix: `value={riga.quantita.replace('.', ',')}` nel campo di modifica, stessa conversione già usata nella vista di sola lettura sottostante — nessun riposizionamento del cursore necessario (sostituzione di un carattere, la lunghezza non cambia, a differenza della mascheratura di InputValuta). **3) Selettore Formale/Informale**: implementata la proposta della sessione precedente, approvata implicitamente includendola in questa richiesta — didascalia `"Applica un tono predefinito:"` sopra i due bottoni (`text-xs text-gray-500`); stato "attivo" **derivato** da `fields` ad ogni render (non un flag separato salvato a parte, `tonoDaValori()`) — risulta "formale"/"informale" solo quando Apertura+Congedo corrispondono ESATTAMENTE al preset, si accende dopo un click e si spegne da solo se l'utente modifica manualmente il testo (evita un'indicazione rimasta "appesa" e non più corrispondente al contenuto reale — rischio concreto con un booleano indipendente impostato solo al click). Stesso pattern visivo già in uso da `FiltroLavoriChip` (`bg-gray-900 text-white` attivo / `bg-gray-100 text-gray-600` inattivo), `aria-pressed` aggiunto per accessibilità. **4) Nuovo campo Referenza "Codice"**: testo libero opzionale, non per-fornitore (coerente con la decisione del 14/8 — "Districo non traccia codici per-fornitore"). Migration `0052` (`alter table referenza add column codice text`, nullable, nessun default). **Design — letto sempre live dal Catalogo, MAI copiato riga per riga** su `lavoro_satellite_articolo` (a differenza di descrizione/colore_finitura, snapshot al momento della scelta): coerente col fatto che un Codice, a differenza del Prezzo, non ha motivo di "congelarsi" per singolo Acquisto — se cambia nel Catalogo, ogni Acquisto che referenzia quella Referenza deve rifletterlo. **Nessun embed PostgREST usato** (`select('*, referenza(codice)')`, scartato dopo verifica): il progetto non ne aveva mai usato uno in nessun punto, e `database.types.ts` (mantenuto a mano, non generato) ha `Relationships: []` per ogni tabella — la tipizzazione di un embed ne avrebbe richiesto la popolazione corretta per restare affidabile lato client Supabase-JS. Sostituito con lo stesso pattern già in uso in tutto il progetto: query separata per gli id di Referenza coinvolti + merge lato JS (`codicePerReferenzaId`), sia in `dettaglio-lavoro-data.ts` (Acquisto) sia in `ordini-email.ts` (mail) — nessun filtro `attiva` su questa lookup, un Codice storico deve restare visibile anche se la Referenza collegata è nel frattempo stata disattivata (stesso principio già seguito per descrizione/colore_finitura). **UI**: `CatalogoReferenzeForm` — nuovo campo testo "Codice (opz.)" nel form di creazione e nel form di modifica inline, mostrato nella lista come `Cod. XXX` (stile `text-gray-400`, tra colore/finitura e prezzo). **Modale Acquisto**: Codice mostrato in sola lettura accanto alla Referenza selezionata (`· Cod. XXX`), sia nella card "riga selezionata" in modifica sia nel riepilogo interamente di sola lettura (satellite già ordinato) — assente per righe storiche senza Referenza collegata o senza Codice compilato. **Mail d'ordine**: nuova colonna "Codice" in `tabellaReferenzeHtml()` (`ordine-mail-html.ts`), posizionata PRIMA di Descrizione (convenzione comune per un elenco d'ordine — il fornitore cerca prima il codice) — cella vuota (non un trattino, a differenza del prezzo mancante della revisione precedente: qui l'assenza è normale/attesa, non un dato storico mancante da segnalare) quando la Referenza non ha Codice o la riga è storica. `SatelliteArticolo.referenza` (nuovo campo opzionale sul tipo, `satelliti-meta.ts` — un oggetto `{codice}` con codice stringa o nullo, o l'intero campo assente/nullo se la riga non ha una Referenza collegata) e `ReferenzaOption.codice`/`CampiReferenza.codice` (`lib/acquisti/referenze.ts`) estesi coerentemente. **Verificato end-to-end** (Supabase locale, porte offset +1000, riusato l'ambiente con dati di test reali delle sessioni precedenti — `test-unificazione@example.com`, SMTP ripuntato a Mailpit via UI, `SMTP_CREDENZIALI_KEY` rigenerata — dev server + 5 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di sia `.env.local` sia `config.toml`): 11 controlli su spacing/tono (gap ≥8px tra checkbox e Allegati confermato — 20px reale —, nessuna sovrapposizione di bounding box, ordine checkbox-poi-Allegati; didascalia presente, `aria-pressed`/classe attiva corretti su entrambi i bottoni, stato attivo che si azzera dopo modifica manuale); 5 controlli su Catalogo/Acquisto in modifica (campo Codice nel form di creazione, referenza creata con codice mostrata in lista, Codice mostrato accanto alla Referenza scelta nella modale Acquisto); Quantità/Prezzo — digitazione "2,5"/"1,50" mostra correttamente la virgola in entrambi i campi durante l'editing (bug del punto 2 riprodotto e confermato risolto), **salvataggio verificato a query diretta** (`quantita=2.500`, `prezzo_unitario=1.50`, `valore_complessivo=3.75` — 2,5 × 1,5 esatto, sia mostrato live sia persistito); 7 controlli su un invio ordine reale via Mailpit (colonna "Codice" in intestazione, riga con Referenza mostra il codice corretto, riga storica senza Referenza mostra cella vuota — **ispezionato anche visivamente via screenshot della vista Mailpit reale**, nessun trattino inventato — nessun prezzo nel corpo/regressione dalla sessione precedente, banner ancora in fondo/centrato). 23 controlli totali, zero errori console/pageerror. `tsc --noEmit`/`eslint` (scoped `app`/`components`/`lib`, escludendo il rumore noto di `supabase/.temp/` già documentato il 16/8)/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. **Migration `0052` applicata dall'utente su Supabase Cloud** nello stesso turno, verificata indipendentemente via introspezione OpenAPI diretta (`referenza.codice` presente, `200`) prima di procedere — poi **deploy eseguito** (stesso accesso SSH `root@178.105.199.29`): `git pull`+`docker compose build`+`up -d`, container ricreato, verificato `200` su `/login` e `307`→`/login` su `/catalogo` (nessuna sessione, atteso), log del container puliti. |
| 2026-08-19 | **Bug fix — InputValuta, il punto come separatore decimale veniva sempre scartato anche quando non ambiguo**. Segnalato dall'utente subito dopo il deploy della sessione precedente: "non riesco ad attribuire un prezzo da € 0,5". **Riprodotto esattamente prima di correggere** (Playwright, Catalogo Referenze e modale Acquisto, stesso componente `InputValuta` condiviso): digitare "0.5" con il punto — il tasto decimale che quasi ogni tastierino numerico/`inputMode="decimal"` offre, non la virgola — produceva silenziosamente "€ 5" (il punto veniva scartato come rumore incondizionatamente, per design deliberato del 15/8); digitare "1.50" produceva "€ 150", ancora più fuorviante. **Causa**: la protezione del 15/8 (mai scartare la virgola, sempre scartare il punto) era stata pensata per l'unico caso realmente ambiguo — un punto delle migliaia già iniettato dalla formattazione (es. "1.234") che un Backspace sulla virgola poteva far reinterpretare come decimale — ma applicata incondizionatamente a QUALSIASI valore, anche quelli sotto le 1000 unità dove un separatore delle migliaia non può mai essere presente nel testo mostrato: lì il punto non è mai ambiguo, scartarlo era solo un falso positivo che rompeva il caso d'uso più comune (un prezzo sotto l'euro/le decine di euro). **Fix mirato**: `handleChangeDecimale()` ora calcola `puntoAmbiguo` dalla parte intera del valore GIÀ noto prima del tasto corrente (`Number(value) >= 1000`) — sotto quella soglia il punto è trattato esattamente come la virgola (stesso separatore, stessa posizione), sopra resta rumore scartato come da comportamento originale. **Verificato che la protezione originale resta intatta**: riprodotta di nuovo la sequenza esatta del 15/8 (digita "1234,7" → Backspace × 2) — torna correttamente a "€ 1.234", non al bug "€ 1,2"; verificato anche che digitare "1234.5" (punto, valore ≥1000) continui a scartarlo come rumore ("€ 12.345", non un decimale); verificato che "seleziona tutto e digita" resti funzionante. Un solo file toccato (`components/input-valuta.tsx`) — nessuna modifica a `filtraQuantita()` (Quantità in Acquisto), mai stata affetta: quel campo non ha mai avuto una mascheratura con separatore delle migliaia, accetta punto e virgola senza ambiguità fin dal 15/8. **Segnalato nello stesso messaggio, verificato ma non un bug**: "non vedo il selettore Formale/Informale" — controllato il codice sorgente deployato sul container di produzione (`docker exec districo grep -rl "Applica un tono predefinito" /app/.next`): la stringa è presente sia nei chunk server sia in quelli client, il componente lo renderizza in modo incondizionato (nessun `if`/dato mancante può nasconderlo, verificato leggendo `app/(app)/profilo/impostazioni/page.tsx` per intero). Deploy confermato coerente con l'ultimo commit (`git log -1` sul server allineato a `main`). Spiegazione più probabile: cache del browser lato utente (bundle JS precedente al deploy) — nessuna azione di codice necessaria, segnalato all'utente di provare un refresh forzato/finestra anonima; da riaprire come bug reale solo se persiste dopo quella verifica. **Verificato end-to-end** (Supabase locale, porte offset +1000, stesso ambiente di test riusato, dev server + 3 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di sia `.env.local` sia `config.toml`): riproduzione esatta del bug (prima del fix, entrambe le posizioni — Catalogo e Acquisto — mostravano "€ 5"/"€ 150"); dopo il fix, "0.5"→"€ 0,5" e "1.50"→"€ 1,5" in entrambe le posizioni; 4 controlli di regressione sulla protezione del 15/8, tutti superati. `tsc --noEmit`/`eslint` (scoped)/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`, verificato `200` su `/login`). Nessuna migration. |
| 2026-08-19 | **Cambio di design — tono mail (Formale/Informale) scelto al momento dell'invio, non più un default globale in Impostazioni**. Segnalato dall'utente subito dopo aver trovato il selettore in Impostazioni: "il tono utilizzato dipende dal fornitore a cui sto scrivendo" — corregge il design della sessione del 17/8, che trattava Apertura/Congedo come UNA coppia di testo personalizzabile per l'artigiano, con Formale/Informale come semplice prefill una tantum. **Due domande poste esplicitamente prima di implementare, entrambe risolte con l'utente** (proposte con anteprima, entrambe le opzioni raccomandate scelte): (1) dove si sceglie il tono per un invio specifico — **al momento dell'invio** (selettore nel pannello "Invia ordine", accanto al destinatario), non come preferenza salvata per fornitore/sede (opzione scartata, avrebbe richiesto uno schema più complesso senza reale necessità dichiarata); (2) il testo personalizzato in Impostazioni — **due varianti indipendenti**, una per tono, entrambe sempre modificabili fianco a fianco (non un solo testo con fallback al preset fisso di un tono non personalizzato). **Migration `0053`**: droppate `mail_ordine_apertura`/`mail_ordine_congedo` (0051, 17/8) **senza backfill** — verificato su Supabase Cloud prima di scrivere la migration che ZERO artigiani reali le avessero mai valorizzate (funzionalità viva da 2 giorni, mai personalizzata da nessuno prima di questa sessione) — a differenza di altri casi in questo progetto con dati reali da preservare (es. `chiusura_acconti`, mai droppata), qui non c'era nulla da perdere. Sostituite da 4 colonne indipendenti: `mail_ordine_apertura_formale`/`mail_ordine_congedo_formale`/`mail_ordine_apertura_informale`/`mail_ordine_congedo_informale`, tutte nullable (null = usa il preset applicativo di quel tono, stesso principio di prima). **`components/profilo-testo-mail-form.tsx` riscritto**: due sezioni sempre visibili ("Formale"/"Informale"), ciascuna con Apertura+Congedo propri — rimossi interamente `applicaTono()`/`tonoAttivo`/i due bottoni pillola con didascalia (la sessione precedente li aveva appena aggiunti, ora superflui: non c'è più un singolo testo da precompilare, ci sono due testi sempre editabili in parallelo). Ogni textarea vuota mostra il preset applicativo come `placeholder` (testo grigio, non un valore precompilato) — coerente con "vuoto = non personalizzato" già in uso per gli altri campi opzionali dell'app. **`lib/profilo/actions.ts`** (`aggiornaTestoMail`): `TestoMailFields` da 2 a 4 campi, scrive le 4 colonne — stessa cautela `.trim()` del 17/8 mantenuta (il valore salvato non viene troncato, solo la decisione "vuoto→null" lo usa). **`lib/lavori/ordini-email.ts`** (`inviaOrdineSatellite`): nuovo parametro `tono: 'formale' | 'informale'`, passato dal chiamante — seleziona quale coppia di colonne leggere (con fallback al `DEFAULT_APERTURA_*`/`DEFAULT_CONGEDO_*` di quello stesso tono, mai più un fallback fisso "informale" indipendentemente dalla scelta). **`components/satellite-ordine.tsx`**: pannello "Invia ordine" esteso con un selettore Tono (stesso stile pillola bg-gray-900/bg-gray-100 già in uso per il selettore in Impostazioni e per `FiltroLavoriChip`), stato locale `tonoScelto` preselezionato `'informale'` (stesso comportamento di fallback impostazione di prima quando nulla era personalizzato — nessuna sorpresa per chi non tocca il selettore), sempre cambiabile prima di "Conferma invio". **Verificato end-to-end** (Supabase locale, porte offset +1000, riusato l'ambiente con dati di test reali delle sessioni precedenti — SMTP ripuntato a Mailpit via UI, dev server + 3 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di sia `.env.local` sia `config.toml`): 14 controlli sul primo invio (due sezioni "Formale"/"Informale" presenti, 4 textarea, nessun residuo del vecchio prefill; testo personalizzato salvato e persistito dopo reload per entrambi i toni; selettore Tono presente nel pannello invio, "Informale" preselezionato di default, "Formale" diventa attivo al click; **invio reale via Mailpit con tono Formale**: corpo mail ricevuto contiene esattamente il testo Formale personalizzato, non quello Informale) + 3 controlli su un secondo invio con "Informale" lasciato di default (corpo mail con il testo Informale personalizzato, non quello Formale) — **ispezione visiva via screenshot** sia della sezione Impostazioni (due gruppi ben distinti) sia del pannello "Invia ordine" (selettore Tono chiaramente visibile sopra "Conferma invio"). 17 controlli totali, zero errori console/pageerror. `tsc --noEmit`/`eslint` (scoped)/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. **Migration `0053` applicata dall'utente su Supabase Cloud** nello stesso turno, verificata indipendentemente via introspezione OpenAPI diretta (le 4 nuove colonne presenti, le 2 vecchie sparite) prima di procedere — poi **deploy eseguito** (stesso accesso SSH `root@178.105.199.29`): `git pull`+`docker compose build`+`up -d`, container ricreato, verificato `200` su `/login` e `307`→`/login` su `/profilo/impostazioni` (nessuna sessione, atteso), log del container puliti. |
| 2026-08-19 | **Tariffe orarie e costo manodopera (Costruzione/Montaggio) + congelamento alla chiusura**. Migration `0054`. **1) Impostazioni**: nuova sezione "Tariffe orarie" (`ProfiloTariffeForm`, tra "Testo mail ordine" e "Obiettivi" — quest'ultima esplicitamente "senza più alcun effetto visibile", le tariffe invece alimentano un calcolo reale, non bundlate lì) — due campi `InputValuta decimali={1}` (stesso componente/precisione già in uso per Prezzo di Acquisto, una tariffa oraria può ragionevolmente avere un decimale), "Costo orario Costruzione" default **50 €/h**, "Costo orario Montaggio" default **30 €/h**. Colonne `artigiano.tariffa_oraria_costruzione`/`tariffa_oraria_montaggio numeric(10,2) NOT NULL DEFAULT` (a differenza di `mail_ordine_*`, dove null="non personalizzato" ha senso per un testo — qui il valore entra in un calcolo aritmetico ovunque venga letto, un default a schema evita di ripetere lo stesso fallback in ogni punto di lettura). **2) Numero di persone per sessione**: `SessioneLavoro` (tipo condiviso Costruzione/Montaggio, `satelliti-meta.ts`) guadagna `persone: number` — colonna jsonb, nessuna migration di schema per questo campo specifico (fa parte del payload libero già esistente). Nuovo controllo "Persone" nella card di ogni sessione (`type="number" min="1" step="1"`, stesso pattern già in uso per gli Obiettivi — non il testo filtrato di Quantità/Acquisto, qui non c'è mascheratura con separatore delle migliaia da cui distinguere le frecce native, il rischio di variazioni accidentali su un contatore a step 1 è marginale), default "1" per una sessione nuova. Sessioni salvate prima di questa sessione non hanno `persone` nel JSON storico — **ogni punto di lettura tratta il campo come possibilmente assente** (`s.persone ?? 1`), verificato empiricamente con una sessione legacy sintetica (nessun crash, default "1" mostrato correttamente, costo calcolato di conseguenza). **3) Calcolo costo manodopera**: nuove funzioni condivise in `satelliti-meta.ts` (leaf module, no rischio di import circolare) — `arrotondaSuA30Min`/`calcolaTotaleMinutiSessioni` (estratte da satellite-costruzione.tsx/satellite-montaggio.tsx, dove erano duplicate identiche dal 12/8: "duplicazione deliberata" riguardava la STRUTTURA dei due componenti — audit 13/8 — non un divieto di condividere funzioni pure quando la stessa formula serve anche lato server) e la nuova `calcolaCostoManodopera(sessioni, tariffaOraria)` — per ogni sessione, durata arrotondata ai 30 minuti (stessa regola di "Totale ore", sessione aperta conta come tempo trascorso finora) × persone × tariffa, sommato su tutte le sessioni, arrotondato a 2 decimali (stessa cautela già in uso per Prezzo×Quantità di Acquisto). **4) "Costo manodopera stimato" nelle modali**: nuova riga sola lettura accanto a "Totale ore" in entrambi i satelliti, `formattaValuta(x, 2)`. Design dei prop (`tariffaOraria`+`costoManodoperaStimato`, non un solo numero): il ramo editabile (`isOwner=true`, che implica sempre Lavoro non completato) ricalcola dal vivo ad ogni render con la tariffa VIVA — nessun congelamento in gioco mentre si sta ancora modificando, dato che editing e "Lavoro chiuso" sono mutuamente esclusivi; il ramo di sola lettura mostra invece `costoManodoperaStimato`, già risolto server-side (congelato se il Lavoro è chiuso). **5) Chiusura Lavoro**: due nuove righe sola lettura "Costo manodopera Costruzione"/"Costo manodopera Montaggio" (tra Spese complessive e Margine, `formattaValuta(x, 2)` — non lo 0 decimali di default delle altre righe, per mostrare lo stesso numero identico alla riga "Costo manodopera stimato" dei due satelliti). **Margine ridefinito**: Valore complessivo − Spese complessive − Costo manodopera Costruzione − Costo manodopera Montaggio. **6) Congelamento alla chiusura — cuore della sessione**. Migration `0054`: `lavoro_satellite.chiusura_costo_manodopera_costruzione`/`_montaggio numeric(12,2)`, nullable (null finché mai chiuso). **Design scelto (non ambiguo, nessun bisogno di fermarsi)**: si congela il COSTO GIÀ CALCOLATO (non la tariffa applicata) — dato che il Lavoro completato rende l'intero satellite Costruzione/Montaggio sola lettura (`isOwnerEffettivo = isOwner && !completato`, invariato), le sessioni non possono più cambiare dopo la chiusura: ricalcolare da sessioni-frozen × tariffa-frozen darebbe lo stesso numero di un costo salvato direttamente, ma con più complessità (due valori da congelare invece di uno, doppia lettura). **Tariffa applicata determinata dall'owner del Lavoro** (`lavoro_artigiani.ruolo='owner'`), non da chi clicca "Salva" — scelta deterministica che evita l'ambiguità "tariffa di quale dei due artigiani" su un Lavoro "a quattro mani" (funzionalità priva di un punto di ingresso in UI dal 18/7, ma la scelta resta corretta anche se oggi nessun Lavoro reale ne ha davvero due) — stesso principio riusato sia per il congelamento (`calcolaCostiManodoperaPerLavoro()`, nuovo helper in `satelliti.ts`) sia per il calcolo LIVE pre-chiusura (`dettaglio-lavoro-data.ts`, non la tariffa di chi sta guardando la pagina). **Scrittura**: in `aggiornaChiusuraFlags()`, quando `fields.conclusa===true` — **ricalcolata e sovrascritta OGNI volta** che si chiude (non solo la prima, a differenza di `chiusura_data`): se il Lavoro viene riaperto e richiuso, sessioni/tariffa possono essere legittimamente cambiate nel frattempo, il nuovo congelamento deve riflettere lo stato reale di QUESTA chiusura. Best-effort (se l'owner o le sue tariffe non si trovano, i due campi restano assenti invece di bloccare la chiusura). **Lettura**: in `dettaglio-lavoro-data.ts`, il blocco `chiusuraSatellite`/reset-automatico (già esistente dal 13/8) è stato **spostato prima** del calcolo di Margine/costo manodopera — il congelamento deve guardare il valore FINALE di `chiusura_conclusa` (post reset-automatico), non quello letto a inizio funzione, altrimenti un Lavoro appena "sbloccato" dal reset mostrerebbe per un istante un costo congelato ormai scaduto. Se chiuso E la colonna congelata non è null → quella è la fonte di verità; altrimenti calcolo dal vivo. **Riapertura** (`riapriLavoro()`, `lib/lavori/actions.ts`) **non toccata**: i due campi congelati restano nel DB invariati quando si riapre (stessi principio già in uso per `chiusura_data`) — semplicemente ignorati finché `chiusura_conclusa` è di nuovo `false` (si torna al calcolo dal vivo), verificato esplicitamente che tornino ad essere quelli di riferimento se richiuso senza ulteriori modifiche. **5 Lavori reali già chiusi prima di questa sessione**: verificato su Supabase Cloud prima di scrivere la migration — nessuna tariffa storica ricostruibile per loro (mai tracciata prima d'ora, 2 dei 5 con sessioni Costruzione reali). **Backfill applicativo one-off** (script Node, non versionato — stesso principio già in uso per altri script one-off in questo progetto), eseguito subito dopo il deploy: per ognuno, tariffa ATTUALE dell'owner al momento del backfill come miglior approssimazione disponibile (non la tariffa storica reale, irrecuperabile) — da quel momento restano comunque protetti da futuri cambi di tariffa, coerente con l'obiettivo della sessione anche se il valore non riflette esattamente quanto sarebbe stato calcolato alla loro chiusura reale (passata). **Verificato end-to-end** (Supabase locale, porte offset +1000, riusato l'ambiente con dati di test reali — Lavoro "Bagno B" attrezzato ad hoc con Costruzione (2h, 1 persona), Montaggio (1h, 2 persone), Preventivo accettato (€6000), Acquisto ordinato (€3,75), tutte le altre attività portate a verde per sbloccare la chiusura — dev server + 5 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di sia `.env.local` sia `config.toml`): tariffe default "€ 50"/"€ 30" mostrate correttamente; campo Persone presente, Totale ore invariato (2h), **Costo manodopera stimato = €100 (2h×1×€50)** corretto; **persone=3 dal vivo → €300** (2h×3×€50, ricalcolo reattivo confermato); **sessione aperta aggiunta → costo aumenta** (verificato dinamicamente contro l'ora reale di esecuzione, non un valore fisso); Chiusura — le due nuove righe mostrano €100/€60 (1h×2×€30), **Margine = €5.836** (6000−3,75−100−60=5836,25, arrotondato a 0 decimali come le altre righe, convenzione preesistente invariata); **chiusura reale** (Incassato+Chiuso, Salva) → verificato a query diretta `chiusura_costo_manodopera_costruzione=100.00`/`_montaggio=60.00`, `lavoro.stato='completato'`; **congelamento**: cambiata la tariffa Costruzione a €999 in Impostazioni → il Lavoro chiuso mostra ANCORA €100 (non ricalcolato), **un Lavoro diverso ancora aperto con una sessione Costruzione da 1h mostra invece €999** (1h×1×€999, riflette la nuova tariffa dal vivo) — la coppia di verifiche che dimostra sia il congelamento sia la non-rottura del calcolo live altrove; **riapertura**: dopo "Riapri lavoro", la vista Chiusura torna a mostrare il costo LIVE (€1.998 = 2h×1×€999, non più €100) — **i due campi congelati nel DB restano invariati** (100.00/60.00, verificato a query diretta), pronti a essere sovrascritti solo a una richiusura reale; sessione legacy sintetica senza `persone` nel JSON → nessun crash, default "1" corretto, costo coerente. Backfill: dry-run e run reali contro l'ambiente locale (4 Lavori di test già `completato` senza Costruzione/Montaggio → €0/€0 scritti correttamente, verificato a query diretta) prima di considerarlo pronto per la produzione. 25 controlli totali su 5 script, zero errori console/pageerror. `tsc --noEmit`/`eslint` (scoped)/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. **Migration `0054` da applicare su Supabase Cloud prima del deploy** (nessuna credenziale di connessione diretta Postgres disponibile, stesso limite di sempre) — deploy e backfill in produzione sospesi finché non confermata. **Nota per chi riprende**: questa voce aveva portato CLAUDE.md a 157,5K caratteri, oltre la soglia dei 150K — sessione di riordino dedicata eseguita subito dopo (stesso giorno, vedi header di questo file e CLAUDE-ARCHIVIO.md per il dettaglio del travaso). |
| 2026-08-19 | **Riorganizzazione Profilo/Impostazioni — due punti di accesso distinti, nuova pagina Profilo (anagrafica + immagine), Impostazioni a tab, rimozione Obiettivi**. Migration `0055`. **Verifica preliminare richiesta esplicitamente, prima di scrivere codice**: cosa raccoglie realmente la registrazione oggi, e se il problema per i campi mancanti (segnalati dall'audit "Inventario Impostazioni" dello stesso giorno) fosse "non richiesti" o "richiesti ma non salvati". Letto `registrazione-form.tsx` + il trigger `handle_new_artigiano()` (migration `0008`, "registrazione minimale") — **due esiti diversi per due gruppi di campi, non uno solo**: (1) **`telefono` è correttamente raccolto E salvato** — l'audit del mattino aveva sbagliato a raggrupparlo con gli altri come "mai popolato", **correzione esplicita** verificata anche su dati reali (tutti e 3 gli artigiani reali hanno un telefono valorizzato); (2) `ragione_sociale`/`partita_iva`/`codice_fiscale`/`via`/`civico`/`cap`/`localita`/`provincia` **non sono mai richiesti dal form** — non un bug, una **decisione deliberata già scritta nella `0008`**: "indirizzo completo, ragione sociale e partita IVA vengono rimandati al completamento profilo (non più raccolti in fase di signup)" — il trigger li legge già da `raw_user_meta_data` con `nullif(...,'')`, pronto a riceverli, semplicemente nessuno li ha mai inviati perché la schermata "completamento profilo" a cui quella migration rimandava non è mai stata costruita fino a questa sessione. Nessuna modifica al flusso di registrazione (fuori scope, confermato) — questa sessione è quel completamento. **Struttura**: due voci distinte in navigazione (`components/app-nav.tsx`) al posto della sola icona ingranaggio — **avatar** (nuovo `components/avatar.tsx`, iniziali nome+cognome su sfondo `bg-gray-900` se nessuna immagine, "come da decisione originale mai implementata" — la colonna `immagine_profilo` esisteva a schema dal luglio ma non era mai stata né letta né scritta) → `/profilo` (nuova pagina); **ingranaggio**, posizione invariata → `/profilo/impostazioni`. **Bug potenziale trovato e corretto durante l'implementazione, non nel codice originale**: `voceAttiva()` (match a prefisso, usato per `/lavori/[id]` ecc.) avrebbe acceso ENTRAMBE le icone quando attivi su `/profilo/impostazioni`, dato che quel path inizia per "/profilo/" — sostituito con un confronto esatto (`pathname === '/profilo'`) per queste due voci specifiche, unico punto dell'app con questa ambiguità (nessun'altra voce di menu è prefisso di un'altra). **Nuova pagina `/profilo`** (`app/(app)/profilo/page.tsx`): form anagrafica completo (`components/profilo-anagrafica-form.tsx`) — Nome/Cognome/Specializzazione/Telefono (obbligatori, stesso vincolo NOT NULL a schema della registrazione) + Ragione sociale/Partita IVA/Codice fiscale (opzionali, ma **Codice fiscale obbligatorio se Partita IVA è valorizzata** — stesso CHECK a schema `artigiano_codice_fiscale_se_partita_iva`, validazione client+server duplicata per un messaggio leggibile invece del testo grezzo di una violazione CHECK) + Indirizzo completo, **riusa `CampiIndirizzo`** (componente condiviso dal 13/8, mai esteso ad Artigiano prima — il suo stesso commento di testa anticipava già questo caso) mappando i nomi generici del componente (indirizzo/citta/siglaProvincia/nazione) sui nomi reali delle colonne `artigiano` (via/localita/provincia/paese, diversi da quelli di Lavoro/Sede Fornitore) — stesso ordine Città→Provincia→Nazione garantito automaticamente, nessun rischio di far ripresentare il bug corretto due volte l'anno scorso. Specializzazione: stesso pattern "Altro..." + testo libero della registrazione, con un **nuovo INSERT lato applicativo** su `specializzazione` (upsert con `ignoreDuplicates`, equivalente lato client dell'`on conflict do nothing` del trigger SQL) — richiede una **nuova policy RLS** (`inserimento specializzazioni da autenticati`, migration 0055): l'unica INSERT esistente su quella tabella passava finora sempre dal trigger `security definer`, mai da un client autenticato normale. **Email — flusso separato, non un update diretto della colonna**: cambiare `artigiano.email` da sola avrebbe disallineato silenziosamente l'email di LOGIN (`auth.users.email`, mai toccata) da quella mostrata/usata come mittente — un utente avrebbe continuato ad accedere con la vecchia email credendo di averla già cambiata. Usa invece `supabase.auth.updateUser({ email })` (client Supabase standard lato browser — l'API di cambio email vive lì, non in un Server Action) che innesca il flusso di conferma nativo di Supabase (`double_confirm_changes=true` in questo progetto: un'email di conferma parte sia verso il vecchio sia verso il nuovo indirizzo, il cambio diventa effettivo solo dopo aver confermato entrambi). **Nuovo trigger `on_auth_user_email_updated`** (migration 0055, `after update on auth.users`, `security definer` — stesso pattern di `handle_new_artigiano`, che copre solo l'INSERT iniziale): sincronizza `artigiano.email` automaticamente a conferma avvenuta, indipendentemente da quando/come la conferma arriva (il completamento del flusso non passa dalla sessione dell'utente che ha avviato il cambio). **Verificato end-to-end nel modo più diretto possibile**: cliccati per davvero entrambi i link di conferma ricevuti via Mailpit locale — `auth.users.email` e `artigiano.email` risultati sincronizzati correttamente a query diretta dopo la conferma, `artigiano.email` verificato INVARIATO subito dopo l'invio (prima della conferma). **Immagine profilo — upload con crop**: nuova dipendenza `react-easy-crop` (`components/profilo-avatar-upload.tsx`) — unica libreria nuova aggiunta in questa sessione, motivata: un crop interattivo drag/zoom vero (richiesto esplicitamente, "standard da avatar") non è replicabile in poche righe come le maschere numeriche già in uso altrove nel progetto (InputValuta, filtraQuantita), a differenza di quei casi dove una libreria era stata scartata per eccesso. Crop quadrato/circolare (`aspect={1} cropShape="round"`) fatto per intero nel browser via canvas, solo il quadrato già ritagliato (JPEG, 800px) viene caricato — `caricaImmagineProfilo()` (server action, `lib/profilo/actions.ts`) applica comunque un resize/crop 512×512 `fit:'cover'` **difensivo** con `sharp` (mai fidarsi ciecamente del client), stesso toolchain/stessa cartella (`uploads/`, non Supabase Storage) già in uso per gli allegati satellite (`lib/lavori/allegati.ts`) — coerente con l'infrastruttura esistente, nessuna nuova integrazione di storage. **Sostituisce l'immagine precedente** (richiesto esplicitamente): il file vecchio viene rimosso solo DOPO che il nuovo è già salvato e collegato con successo, mai prima — se qualcosa fallisse a metà, l'artigiano resterebbe con l'immagine precedente invece di restare senza nessuna. **Nuova route `app/api/avatar/[artigianoId]/route.ts`** (stesso pattern di `app/api/allegati/satellite/[id]/route.ts`, file su disco) — **unica differenza architetturale rilevante**: la RLS su `artigiano` (`"artigiano vede solo se stesso"`) blocca la lettura dell'avatar di un ALTRO artigiano, a differenza degli allegati (dove RLS deriva l'accesso da una tabella satellite/lavoro condivisa) — la route usa quindi il client autenticato solo per verificare che il richiedente sia loggato (blocca gli anonimi), poi il client **admin** (stesso già in uso per la lista specializzazioni di registrazione/invito) per la lettura vera e propria, bypassando deliberatamente lo scoping "solo sé stesso": un avatar è pensato visibile a chiunque sia autenticato in Districo (es. un futuro collaboratore "a quattro mani"), nessun dato sensibile in un'immagine profilo. **Cache-busting**: la route è keyed sull'id dell'ARTIGIANO (stabile), non sul file — un `?v=<suffisso dello storage_path>` (`lib/profilo/avatar.ts`) sfrutta l'UUID già fresco a ogni upload (stesso schema di naming degli allegati satellite) per forzare il browser a scaricare la nuova immagine dopo una sostituzione, senza bisogno di un contatore di versione a parte. **Impostazioni riorganizzata a tab** (`components/impostazioni-tabs.tsx`, sostituisce l'unico form lineare con 4 gruppi impilati): stile "chip" segmentato, stesso pattern già in uso per `FiltroLavoriChip` (`/lavori`) e per il selettore Tono dentro Acquisto — non un tab bar con sottolineatura, coerente con l'unico altro selettore "vista tra alternative" già esistente invece di introdurne uno nuovo. 4 tab: Credenziali email (invariata), Testo mail ordine (invariata, tono scelto all'invio dal 19/8 mattina), Tariffe orarie (invariata), **Statistiche** (nuova, un solo campo). Array `TABS` ordinato apposta per reggere l'aggiunta futura di "Abbonamento/Fatturazione" (Stripe, non pianificata — vedi "Prossimi passi aperti") senza ristrutturare il layout. **Rimozione Obiettivi**: i 4 campi "giorni" (Tempo di preventivazione/progetto/produzione, Durata montaggio), confermati inerti dall'"Inventario Impostazioni" dello stesso giorno (nessuna query li leggeva più dal Sprint E, 3/8) — **verificato di nuovo, non solo fidandosi del report precedente**, che tutti e 3 gli artigiani reali avessero ancora esattamente i valori di default (10/7/60/7): nessuna personalizzazione reale persa, colonne droppate dalla migration 0055. `kpi_finestra_mesi` — l'unico campo del vecchio gruppo ancora letto da `kpi_dashboard()` — **sopravvive**, nuova sotto-sezione "Statistiche" a sé (`ProfiloStatisticheForm`, nuova azione `aggiornaStatistiche()` sostituisce `aggiornaObiettiviKpi()`). `ProfiloObiettiviForm` eliminato. **Chiusura di un loop lasciato aperto dalla `0008`**: quella migration aveva reso il CHECK `artigiano_codice_fiscale_se_partita_iva` `NOT VALID` con un commento esplicito — "una VALIDATE CONSTRAINT successiva, una volta che la schermata Profilo permetterà di completare il codice fiscale, potrà rendere il controllo retroattivo" — questa sessione è quel momento: verificato che tutti gli artigiani reali avessero partita_iva E codice_fiscale entrambi NULL (nessuna violazione possibile) prima di eseguire `validate constraint` nella migration 0055, ora pienamente enforced. **PEC/Codice Destinatario SDI — segnalato, non aggiunto**: richiesto esplicitamente di verificare se mancasse qualcosa di ovviamente necessario per la fatturazione elettronica italiana rispetto ai 3 campi fiscali già presenti — manca, aggiunto come nota in "Prossimi passi aperti" legata al futuro Stripe, non implementato di iniziativa. **Nessuna ambiguità reale emersa che richiedesse di fermarsi** (l'unico punto esplicitamente segnalato come possibile blocco, il flusso di registrazione, si è risolto in una verifica netta senza necessità di scegliere tra alternative). **Verificato end-to-end** (Supabase locale, porte offset +1000, riusato l'ambiente con dati reali — `test-unificazione@example.com` — dev server + 6 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di sia `.env.local` sia `config.toml`): 15 controlli su navigazione/struttura (avatar→/profilo, ingranaggio→/profilo/impostazioni, 4 tab presenti, nessun campo Obiettivi residuo, contenuto di ogni tab corretto); 14 controlli sull'anagrafica (precompilazione da registrazione, Ragione sociale/Via correttamente vuote — mai raccolte —, validazione Partita IVA senza Codice fiscale bloccata sia client sia server, salvataggio e persistenza di tutti i campi verificati dopo reload, ordine Città→Provincia→Nazione confermato nel DOM); 8 controlli su upload/crop/rimozione avatar (fallback iniziali "TU" corretto, modale di crop con overlay circolare e slider zoom, upload→immagine sostituisce le iniziali, "Rimuovi"→torna alle iniziali, **ispezionato anche visivamente via screenshot** — crop UI, avatar risultante nella pagina Profilo, icona avatar 36×36 nella navigazione desktop, quest'ultima verificata via bounding box/naturalWidth non solo a occhio dopo un primo screenshot fuorviante a bassa risoluzione); 4 controlli sul cambio email (messaggio "email attuale" in sola lettura, conferma di invio mostrata, `artigiano.email` invariata prima della conferma, messaggio ricevuto in Mailpit) **+ verifica end-to-end della sincronizzazione reale** (entrambi i link di conferma cliccati per davvero, `auth.users.email`/`artigiano.email` risultati sincronizzati a query diretta). 41 controlli totali su 6 script, zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Migration `0055` applicata dall'utente su Supabase Cloud durante la sessione stessa**, verificata indipendentemente via introspezione OpenAPI diretta (le 4 colonne `target_*` risultate assenti, `immagine_profilo`/`kpi_finestra_mesi` ancora presenti) prima di procedere al deploy. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). |
| 2026-08-19 | **Codice SDI/PEC (fatturazione elettronica) + cambio password in Profilo**. Migration `0056`. **Parte 1 — Codice SDI/PEC**: due nuove colonne `artigiano.codice_sdi`/`.pec` (entrambe `text`, nullable, nessun default) — segnalate come mancanti nella sessione precedente ("riorganizzazione Profilo/Impostazioni", 19/8 mattina) e non implementate allora su richiesta esplicita di limitarsi a segnalarle. Aggiunte nella sezione "Dati fiscali" del form anagrafica (`components/profilo-anagrafica-form.tsx`), accanto a Ragione sociale/Partita IVA/Codice fiscale — stesso raggruppamento tematico, non una sezione a parte. **Nessun vincolo di obbligatorietà incrociata** (né tra i due campi, né con partita IVA/codice fiscale), come richiesto esplicitamente: sono campi di preparazione, la regola reale ("almeno uno dei due obbligatorio per la fatturazione elettronica") resta rimandata a quando l'integrazione Stripe/fatturazione sarà pianificata concretamente. Unica validazione applicata: formato email standard sulla PEC (stessa regex già in uso in `CambioEmail`, duplicata lato client per un feedback immediato e ripetuta lato server in `aggiornaAnagraficaArtigiano()` come guardia autoritativa — nessun vincolo a schema, stesso principio già seguito per `artigiano.email`/`cliente.email` in questo progetto, mai vincolate a livello di colonna). Nota "Prossimi passi aperti" su PEC/Codice SDI aggiornata: i due campi ora esistono, resta aperto solo il collegamento con Stripe/fatturazione elettronica vera e propria. **Parte 2 — cambio password**: nuova sezione "Password" in Profilo, subito sotto "Email di accesso" (stesso file/pattern di `CambioEmail`, nuovo componente `CambioPassword`). **Verifica dell'identità con la password attuale, richiesta esplicitamente** (non ci si affida alla sola sessione già autenticata): nessuna action server-side (stesso motivo del cambio email, vedi commento dedicato in `lib/profilo/actions.ts`) — implementata componendo due chiamate native Supabase Auth lato client, non una gestione custom delle password. `signInWithPassword({email, password: quella dichiarata attuale})` verifica prima l'identità (se fallisce, "Password attuale errata", `updateUser` non viene nemmeno chiamato); solo se riesce, `updateUser({password: quella nuova})` applica il cambio. **Alternativa nativa scartata dopo verifica**: `supabase.auth.reauthenticate()` esiste ma richiede di abilitare "Secure password change" nel progetto (`secure_password_change=false` oggi, config.toml) — cambierebbe il comportamento per OGNI aggiornamento password, incluso il flusso "password dimenticata" già esistente e stabile, e introduce un passaggio OTP via email (un codice da inserire) non richiesto qui: la richiesta era verificare la password attuale, non generare/validare un codice. Validazione: password attuale non vuota; nuova password **almeno 8 caratteri** (stessa soglia già in uso in registrazione e in `reimposta-password`, coerente con `minimum_password_length=6` di config.toml — il vincolo applicativo è più severo di quello DB, non il contrario, nessuna incoerenza da segnalare); nuova password e conferma coincidenti. Messaggi d'errore distinti per ciascun caso (password attuale errata / nuove password non coincidenti / password troppo corta) più un messaggio di successo "Password aggiornata.", campi svuotati dopo un salvataggio riuscito. Riusato `PasswordInput` (mostra/nascondi, già in uso in registrazione/login/reimposta-password), nessun nuovo componente di base. **Verificato end-to-end** (Supabase locale, porte offset +1000, nuovo artigiano di test registrato via UI — l'utente di test riusato nelle sessioni precedenti aveva nel frattempo cambiato email a `nuova-email-test@example.com` per un test reale della sessione precedente, credenziali non note per questa sessione, non riutilizzato — dev server + 3 script Playwright, ambiente smontato a fine sessione con verifica hash SHA-256 di sia `.env.local` sia `config.toml`, entrambi risultati IDENTICI al backup pre-sessione): 13 controlli su Codice SDI/PEC (campi presenti e vuoti inizialmente; salvataggio con solo Codice SDI compilato e PEC vuota riuscito — **nessun vincolo incrociato confermato**; persistenza dopo reload; aggiunta di una PEC valida riuscita e persistita; PEC malformata bloccata con messaggio, **verificato a query diretta che il valore precedente valido non sia stato sovrascritto dal tentativo fallito**; svuotamento di entrambi i campi riuscito, tornano `null` in DB, nessun blocco); 8 controlli sul cambio password (password attuale errata bloccata con messaggio dedicato; nuove password non coincidenti bloccate; nuova password troppo corta bloccata; cambio riuscito con la password attuale corretta, messaggio di successo, campi svuotati; **verifica reale via login**: uscita esplicita — `/login` reindirizza a `/lavori` se già autenticati, verificato — poi login con la VECCHIA password fallisce, login con la NUOVA password riesce) — 21 controlli totali, zero errori console/pageerror. Screenshot della sezione Profilo completa (Dati fiscali con Codice SDI/PEC, sezione Password) ispezionato visivamente, layout coerente col resto del form. `tsc --noEmit`/`eslint` (scoped `app`/`components`/`lib`)/`npm run build` (con `.env.local` di produzione, ripristino verificato) puliti. **Committato su `main`**. **Migration `0056` da applicare su Supabase Cloud prima del deploy** (nessuna credenziale di connessione diretta Postgres disponibile, stesso limite di sempre). |
