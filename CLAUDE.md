# Districo — Contesto di progetto

Questo file va aggiornato a ogni decisione importante presa nel progetto (nome, funzionalità, architettura, scelte di design, ecc.), così che chiunque riprenda il progetto — umano o assistente — abbia il contesto aggiornato. Aggiungere una riga alla tabella "Decisioni prese" per ogni scelta fatta, senza cancellare la cronologia precedente.


> **Per la cronologia completa delle decisioni storiche, vedi [CLAUDE-ARCHIVIO.md](CLAUDE-ARCHIVIO.md).** Questo file contiene solo lo stato attivo/corrente del progetto e le decisioni più recenti/rilevanti per chi riprende oggi il lavoro. In archivio: le decisioni e gli sprint dal 16/7 al 19/7 (primo modello dati Attività/Fasi e prima versione del modello "a stella", superati dalla revisione strutturale del 25/7); l'intera revisione strutturale del 25/7 (Sprint A: lo schema satellite di allora — stati testuali, `RevisionabileChain`, ecc. — è stato riscritto quasi per intero dalla "Revisione satelliti"/Sprint "fondamenta" di agosto); i round di implementazione/test/fix del 26/7–31/7 (Sprint B/C/D originali, KPI a target/semaforo poi sostituiti dai KPI dello Sprint E, redesign Dashboard/Header/Footer/Fornitore/Clienti, vari "Key learnings" infrastrutturali); la gran parte delle voci della tabella "Decisioni prese" dal 2/8 al 6/8 che documentano passaggi intermedi poi superati nello stesso periodo (es. stati testuali di Acquisto poi sostituiti dal flag `ordinato`, creazione automatica limitata al solo Briefing poi estesa a Preventivo/Chiusura); e le voci dal 29/7 all'8/8 spostate nello split del 13/8 (Revisione satelliti Fase 1, Sprint "fondamenta"/B/"allegati modale"/C/D, rimozione trigger post-accettazione, Sprint E, Combobox, flag `ordinato`, prima versione del restyling modale Appuntamento — poi diventata il template UI-4 e infine superata dal nuovo template Briefing dell'10/8 — Sprint UI-1/UI-2/UI-3/UI-4, audit iOS/Modal di test, sessioni di affinamento UI Android) — **nessun contenuto è stato perso o modificato, solo riorganizzato**. Split più recente eseguito il 2026-08-13 (il file aveva superato 300K caratteri); lo split precedente (2026-08-06) copriva fino a 275K caratteri. Stesso giorno, un secondo taglio più piccolo ha spostato altre 4 righe non più citate altrove (indicatore "acconto incassato" Dashboard, caselle testo Verifica misure, nota testuale Noleggio, prima versione delle classi Data+Ora poi sostituita nella sessione immediatamente successiva) per fare di nuovo spazio sotto i 150K. Il 2026-08-14 spostate altre 2 righe non più citate altrove (verifica/allineamento dimensioni Modal condivisa, mascheratura valuta) per lo stesso motivo. Il 2026-08-16 spostate altre 9 righe (il file aveva superato di nuovo i 150K, a 173,5K): origine del template Briefing/`PilloleSalvaAnnulla` e fix scrollbar/contenitore (10/8), restyling Preventivo/Progetto (11/8), nuova Attività "Acconto" (11/8), restyling Campionatura (12/8), restyling Costruzione con `sessioni_lavoro` (12/8), proporzioni Data/Ora 60/40 (12/8), promozione di Montaggio a tipo autonomo (12/8) — tutte sessioni di restyling/propagazione-template ormai completate e stabili, il loro esito resta riassunto in "Prossimi passi aperti"; il Refactor Parallel/Intercepting Routes e la "ripetibilità Attività, elenco consolidato" (entrambi 12/8) restano invece qui, ancora attivamente citati/di riferimento (dettaglio del travaso in CLAUDE-ARCHIVIO.md). Il 2026-08-17 spostate altre 5 righe, stavolta non per anzianità ma perché rese fattualmente obsolete da sessioni successive: bug fix "provenienza Dettaglio Lavoro" (13/8) e metà della sessione "origine navigazione + icona Visualizza" (14/8, l'icona resta viva e invariata) — il meccanismo "sezione di origine" che descrivono è stato rimosso per intero il 16/8; "ordinamento liste Dashboard e Conclusi" (13/8) — la sua logica è stata interamente sostituita dalla sessione di unificazione del 16/8; restyling Acquisto+catalogo Referenze e la sua gestione standalone in Impostazioni (entrambe 14/8) — corrette esplicitamente dalla sessione del 17/8 su due punti (creazione al volo rimossa, Categorie/Referenze spostate da Impostazioni a una sezione "Catalogo" dedicata); il resto di quelle due sessioni (schema, migration 0048) resta valido e non duplicato altrove (dettaglio in CLAUDE-ARCHIVIO.md). Il 2026-08-19 spostate altre 7 righe (il file aveva raggiunto 157,5K dopo la sessione "tariffe orarie e costo manodopera"), tutte del 13/8 — bug fix "Aggiungi attività"/Dashboard, rifinitura visiva grassetto Chiusura Lavoro, bug fix ordine campi indirizzo Sede Fornitore, sessione di audit completo, correzione due fix dal Livello 2 dell'audit, "Correzione di rotta" `lavoro_pronto_per_montaggio()` (il principio che stabilisce resta comunque qui, già duplicato per intero in "Principi architetturali"), cinque problemi emersi da test manuali post-audit (il punto sugli allegati è anch'esso già duplicato in "Principi architetturali") — tutti fix/rifiniture ormai stabili, mai più citati per nome da una sessione successiva (verificato con una ricerca mirata prima di spostarli, dettaglio del travaso in CLAUDE-ARCHIVIO.md). Stesso giorno, un secondo taglio più piccolo (dopo la sessione "Codice SDI/PEC + cambio password", il file aveva superato di nuovo i 150K, a 153,8K) ha spostato altre 2 righe non più citate altrove per nome (correzione posizione "Attività non preventivate" nell'elenco, 13/8; unificazione bottoni Modifica/Riapri lavoro su Lavoro concluso, 15/8 — quest'ultima non va confusa con l'unificazione Dashboard+Conclusi del 16/8, un refactor distinto e non toccato). Ancora lo stesso giorno, un terzo taglio (dopo la sessione "restyling Aggiungi attività — griglia di icone", il file aveva di nuovo superato i 150K) ha spostato 1 riga non più citata altrove per nome (overflow mobile tabella Attività + etichette Acquisto numerazione/parentesi, 16/8 — non va confusa con l'Unificazione Dashboard+Conclusi dello stesso giorno, sessione distinta e non toccata). Un quarto taglio, sempre lo stesso giorno (dopo la sessione "altezza modale Aggiungi attività"), ha spostato 1 riga in più (vincolo su "Contrassegna il lavoro come chiuso", 13/8 — le due funzioni condivise che introduce, `attivitaRilevantiPerChiusura`/`coloreQualsiasiSatellite`, restano citate per nome da una sessione successiva ma sono già autodocumentate nel codice sorgente, `satelliti-meta.ts`, nessun bisogno di consultare la narrativa completa qui spostata). Il 2026-08-17 (dopo la sessione "Scheda di lavoro PDF", il file aveva raggiunto 156,7K) spostate le ultime 2 righe rimaste del 12/8 — il Refactor Parallel/Intercepting Routes e la "ripetibilità Attività, elenco consolidato" — tenute nello split del 16/8 perché allora "ancora attivamente citate/di riferimento": a un mese di distanza, verificato con una ricerca mirata che non lo fossero più (solo due menzioni brevi e autosufficienti restano, una in "Prossimi passi aperti" e questa stessa nota), l'architettura che descrivono è stabile da allora e resta comunque autodocumentata nel codice sorgente. Il 2026-08-19 sera (dopo la sessione "screenshot 'Come funziona'", il file aveva raggiunto 158,5K) spostata 1 riga in più — "Attività non preventivate" + restyling calcoli economici/ciclo di vita di Chiusura Lavoro (13/8, oltre 10K caratteri da sola, la riga più vecchia ancora presente) — verificato con una ricerca mirata che i pochi riferimenti superstiti (`aggiornaChiusuraFlags()`/`chiusura_conclusa`, citati dalla sessione "Tariffe orarie" del 19/8) siano puntatori brevi e autosufficienti, non un bisogno reale della narrativa completa. Ancora lo stesso giorno, un sesto taglio (dopo aver documentato la sessione "landing coming soon", il file aveva di nuovo superato i 150K) ha spostato 1 riga in più (modale Acquisto, decimale Quantità/Prezzo, 15/8 — le due sessioni successive che la citano per data si autospiegano per intero, il comportamento che introduce resta autodocumentato nei commenti di `input-valuta.tsx`/`satellite-ordine.tsx`). Il 2026-08-21 (dopo la sessione "Landing definitiva", il file aveva raggiunto 156,7K) spostate 5 righe del blocco landing del 19/8 ("Landing page pubblica", "routing root/logout + selettore mestiere + colore CTA", "screenshot reali in 'Come funziona'", "aggiornamento asset nuovi screenshot", "coming soon — flag") — non per anzianità ma perché fattualmente superate: la sessione "Landing definitiva" (21/8, resta qui) dichiara esplicitamente di sostituire sia la modalità "coming soon" sia l'intera landing "definitiva" del 19/8, e ogni componente che quelle 5 righe descrivono è stato eliminato dal repository nella stessa sessione — non solo superseduto, cancellato. Non spostata "Coming soon: hero responsive desktop/mobile..." (stessa mattina del 21/8): il suo contenuto (breakpoint, posizionamento testo, compressione immagini) resta il perché della Hero attuale, che la sessione successiva riusa "esattamente" senza ripeterne il ragionamento. Il 2026-08-21 sera (dopo la sessione "Integrazione Stripe", il file aveva raggiunto 153,2K) spostate le 4 righe più vecchie ancora presenti (16/8 "Unificazione Dashboard + Conclusi", 17/8 "Rinomina filtro Conclusi→Completati + fix conteggio", 17/8 "Catalogo Referenze standalone + revisione Acquisto", 17/8 "Mail d'ordine HTML con banner") — verificato con una ricerca mirata (grep su ogni citazione "16/8"/"17/8" nel file) che i pochi riferimenti superstiti da sessioni successive siano puntatori brevi e autosufficienti, non un bisogno reale della narrativa completa qui spostata; l'architettura che descrivono resta comunque autodocumentata nel codice sorgente (dettaglio del travaso in CLAUDE-ARCHIVIO.md). Il 2026-08-22 (a fine sessione "Forum beta tester", su richiesta esplicita di riordino, il file aveva raggiunto 149,8K) spostate le 4 righe più vecchie ancora presenti (18/8 "Sessione di correzione — Acquisto allineato allo standard", 19/8 "Sessione di correzione — sovrapposizione visiva Acquisto/decimali/tono mail/campo Codice", 19/8 "Bug fix — InputValuta, il punto come separatore decimale", 19/8 "Cambio di design — tono mail scelto al momento dell'invio") — verificato con una ricerca mirata (grep su "18/8"/"19/8" e sui termini tecnici distintivi di ciascuna riga) che i pochi riferimenti superstiti siano puntatori brevi e autosufficienti; il comportamento che descrivono resta comunque quello attualmente in uso, solo la cronaca dettagliata si sposta (dettaglio del travaso in CLAUDE-ARCHIVIO.md). Il 2026-08-22 sera (su richiesta esplicita "riordina claude-md", dopo la sessione "voce menu Admin + post Info + fix tasto Accedi", il file aveva raggiunto 148,5K) spostate le sei righe più vecchie ancora presenti (17/8 "Scheda di lavoro PDF"; 19/8 "Tariffe orarie e costo manodopera", "Riorganizzazione Profilo/Impostazioni", "Codice SDI/PEC + cambio password", "Restyling Aggiungi attività — griglia di icone", "Correzione altezza modale Aggiungi attività") — verificato con una ricerca mirata (grep sui termini tecnici distintivi di ciascuna riga) che i pochi riferimenti superstiti da sessioni successive siano puntatori brevi e autosufficienti, non un bisogno reale della narrativa completa; il comportamento che descrivono resta comunque quello attualmente in uso (dettaglio del travaso in CLAUDE-ARCHIVIO.md).

## Cos'è

Districo è un'app gestionale pensata per gli artigiani, per seguire un singolo lavoro dall'inizio alla fine attraverso tutte le sue fasi. Pensata per artigiani di mestieri diversi (non un solo settore), con un ruolo di amministratore della piattaforma che non entra nel merito del lavoro dei singoli ma dispone di statistiche aggregate.

## Funzionalità principali (dal brief iniziale)

- Anagrafica utenti: ogni artigiano vede i propri lavori; due artigiani possono condividere un lavoro svolto "a quattro mani".
- Anagrafica clienti: ogni lavoro è associato a un cliente specifico.
- Anagrafica fornitori: per acquisti di materiali e lavorazioni esterne.
- Percorso di svolgimento del lavoro: trattativa (attività libere) → esecuzione (fasi, personalizzabili, vedi sotto).

## Principi architetturali

- **Nessun vincolo di prerequisito reale tra Attività** (esplicitato il 2026-08-13, dopo aver rimosso `lavoro_pronto_per_montaggio()`, l'unico tentativo di gate simile mai esistito nell'app — dettaglio della rimozione in CLAUDE-ARCHIVIO.md): Districo non impone un ordine né blocca un'Attività in base allo stato di un'altra. L'artigiano decide — può fare un preventivo senza misure confermate, acquistare materiale senza acconti incassati, procedere senza consenso esplicito su una modifica — l'app si limita a **tracciare** cosa è stato fatto e cosa resta da fare (i semafori rosso/giallo/verde), mai a **imporre**. L'unico vero gate di tutta l'app resta quello di Chiusura Lavoro (tutte le Attività verdi per poter chiudere il Lavoro) — deliberato, non un pattern da estendere per analogia ad altre Attività.
- **Noleggio = noleggio di un furgone/mezzo di trasporto** (chiarito il 2026-08-13, mai specificato esplicitamente prima): per gli artigiani privi di un mezzo idoneo sempre disponibile — non un'attività di noleggio di materiale/attrezzatura del lavoro stesso.
- **Gli allegati vivono fuori dal ciclo bozza/Salva/Annulla** (chiarito il 2026-08-13, per design fin dallo Sprint "allegati modale" del 2/8, mai esplicitato prima): un file è già fisicamente scritto su disco/storage nel momento in cui viene caricato, non una bozza in memoria — trattarlo come "non salvato" fino a un click su Salva creerebbe un'ambiguità reale su cosa fare di un file già scritto se l'utente preme Annulla. Upload/eliminazione di un allegato restano quindi sempre immediati e auto-salvanti su tutti gli 8 satelliti, indipendentemente dal dirty-state degli altri campi — non un'incoerenza da correggere, un confine deliberato.
- **Ruolo admin — `artigiano.is_admin`, un solo bit, mai un sistema di permessi granulare** (esistente dalla migration iniziale, 2026-07-25 — mai documentato qui esplicitamente fino al 2026-08-22, quando la sessione "pagina admin utenti" lo ha scoperto/corretto/esteso, vedi tabella sotto per il dettaglio completo). Un artigiano è admin o non lo è, niente ruoli intermedi. Guard in **`app/admin/layout.tsx`** (Server Component, eseguito prima di qualunque pagina sotto `/admin/*`): redirect a `/login` se non autenticato, a `/lavori` se autenticato ma `is_admin` non è `true` — un non-admin non vede mai un errore che riveli l'esistenza di una pagina admin, solo un redirect silenzioso. Due pagine oggi: **`/admin/dashboard`** (statistiche aggregate — riempie il placeholder "da implementare" presente dal 25/7, implementato il 2026-08-22 sera: artigiani totali, nuove iscrizioni ultimi 7gg, lavori totali e per stato — `in corso`=opportunita+accettato/`completati`/`rifiutati`, 3 bucket non 2 perché `lavoro.stato` ha 4 valori reali e un lavoro rifiutato non è né l'uno né l'altro — distribuzione `stato_abbonamento`, beta tester attivi; tutto via **`admin_statistiche_aggregate()`**, migration `0061`, stesso pattern SECURITY DEFINER — SOLO conteggi, mai un id/nome/titolo di un singolo Cliente/Fornitore/Lavoro) e **`/admin/utenti`** (anagrafica + stato abbonamento di tutti gli artigiani, due toggle indipendenti — `beta_tester`: marcatura manuale di chi ha aderito al programma beta della landing, migration `0058`, 2026-08-22; **`accesso_gratuito`**: deroga manuale permanente indipendente da Stripe, migration `0060`, 2026-08-22 sera — quando `true` `app/(app)/layout.tsx` salta del tutto il controllo `stato_abbonamento`/`trial_fine`, copre anche `'nessuno'` non solo `'canceled'`/trial scaduto, unico modo per dare accesso gratuito senza toccare l'account Stripe dell'artigiano). Protezione **su due livelli indipendenti**, non solo la route: ogni lettura/scrittura che coinvolge righe di ALTRI artigiani (o conteggi aggregati su tabelle operative come `lavoro`) passa da funzioni SQL `SECURITY DEFINER` (`admin_lista_artigiani()`/`admin_imposta_beta_tester()`/`admin_imposta_accesso_gratuito()`/`admin_statistiche_aggregate()`, migration `0058`+fix `0059`+`0060`+`0061`) che verificano `is_admin` **internamente** (`raise exception` se il chiamante non è admin) — la RLS su `artigiano` resta "vede solo se stesso" (0001), MAI un accesso diretto più ampio; anche una chiamata RPC che bypassa del tutto Next.js viene rifiutata. Stesso principio già scritto sotto "Note tecniche" ("Admin RLS: solo funzioni/view SQL con SECURITY DEFINER"), qui il primo uso reale. **Nota tecnica**: cambiare il tipo di ritorno di `admin_lista_artigiani()` (aggiungere una colonna a `returns table`) ha richiesto un `drop function` esplicito prima del `create` — Postgres rifiuta un `create or replace` che cambia la shape delle OUT columns (`42P13 cannot change return type of existing function`), e il drop cancella anche i GRANT/REVOKE già impostati: vanno sempre riapplicati nella stessa migration dopo il drop+create, non solo su una funzione nuova.
  **Attenzione per chi tocca questa cartella in futuro — `app/admin/` deve restare un segmento URL reale, MAI un route group `app/(admin)/`**: le parentesi di un route group Next.js non contribuiscono all'URL — `app/(admin)/utenti/page.tsx` risolverebbe silenziosamente a `/utenti`, non `/admin/utenti`. Esattamente questo bug è esistito per un mese dal setup iniziale del progetto (25/7 → 22/8) senza che nessuno se ne accorgesse, per una ragione precisa: un utente non autenticato o non-admin viene comunque reindirizzato dal middleware in base al solo pathname (che esista o meno una pagina reale a quel path), mascherando il 404 sulla route sbagliata sottostante — il bug era invisibile fino a quando non è esistito un vero account con `is_admin=true` da cui osservarlo. Corretto il 2026-08-22 rinominando la cartella (`git mv app/(admin) app/admin`), nessuna riscrittura del guard.
- **FIX SICUREZZA — `artigiano` scrivibile ora solo per colonna, non più per intera riga** (scoperto e corretto il 2026-08-22 sera, migration `0064` — vedi tabella sotto per il dettaglio completo). La policy RLS "artigiano aggiorna solo se stesso" (0001) ha SEMPRE ristretto solo la RIGA (`id = auth.uid()`), mai le colonne — verificato empiricamente (non ipotizzato) che qualunque artigiano autenticato potesse auto-impostare `is_admin=true`, `beta_tester=true`, `accesso_gratuito=true`, `stato_abbonamento` ecc. con una singola PATCH REST diretta sulla propria riga, bypassando tutte le funzioni admin `SECURITY DEFINER` pensate per proteggere esattamente quei campi. Corretto con privilegi UPDATE **a livello di colonna** (`revoke update on artigiano from authenticated, anon` + `grant update (lista esplicita di campi) on artigiano to authenticated`) — si combinano con la RLS esistente, che resta invariata e continua a decidere QUALE riga; i privilegi di colonna decidono QUALI CAMPI. **Attenzione per chi aggiunge in futuro un nuovo campo self-writable su `artigiano`** (una preferenza personale che l'artigiano deve poter modificare su se stesso): va aggiunto esplicitamente alla lista nel `grant update (...)` — altrimenti l'`UPDATE` fallisce con `42501 permission denied for table artigiano`, anche se la RLS lo permetterebbe. Non rientra invece nella lista (resta scrivibile solo da funzioni `SECURITY DEFINER`/trigger/service role, mai da un `UPDATE` diretto del client) qualunque campo che rappresenti un ruolo, un diritto o uno stato controllato dall'esterno: `is_admin`, `beta_tester`, `accesso_gratuito`, `stato_abbonamento`/`piano_abbonamento`/`trial_fine`/`stripe_subscription_id` (Stripe/webhook), `email` (trigger dedicato), `id`/`created_at`. `stripe_customer_id` è un caso a parte: era scritto dal client in `avviaCheckout()`, spostato in una funzione dedicata (`imposta_stripe_customer_id()`, `where ... and stripe_customer_id is null` — scrivibile una volta sola) invece di rientrare nella lista, essendo un identificativo verso un servizio di pagamento esterno, più sensibile di una preferenza personale.
- **Forum beta tester — `post_beta`/`messaggio_beta`, un caso NUOVO rispetto al resto dell'app** (2026-08-22, vedi tabella sotto per il dettaglio completo): a differenza di `artigiano` (RLS deliberatamente "vede solo se stesso", admin escluso da un accesso diretto e instradato su funzioni SECURITY DEFINER) qui l'admin è un **partecipante di prima classe** della community, non un supervisore esterno — le sue regole (vede/modera tutto, risponde a qualunque post) sono RLS dirette su queste due tabelle (migration `0062`), non funzioni admin dedicate. Community condivisa, non thread privati a coppie: un beta tester vede tutti i post non nascosti di tutti; solo l'autore originale scrive follow-up sul proprio post, solo l'admin risponde a QUALSIASI post — e solo se `stato='aperto'` (un post chiuso blocca la scrittura per chiunque, admin incluso, finché non viene riaperto esplicitamente — mai una scrittura "attraverso" la chiusura). Moderazione sempre soft-hide (`nascosto` su post E su singolo messaggio, mai una DELETE) — un post nascosto porta con sé tutti i suoi messaggi anche se non marcati singolarmente uno per uno. Pagina `/beta` (lista + `/beta/[id]` + `/beta/nuovo`), voce di menu condizionale (visibile solo a `beta_tester` o `is_admin`), badge admin-only nel menu (post aperti la cui ultima risposta è di un beta tester, `beta_notifiche_admin_count()`). **Lezione tecnica riusabile, non specifica di questa sola funzione**: una funzione `SECURITY INVOKER` che joina `artigiano` per leggere un campo (qui `is_admin`) di un utente DIVERSO dal chiamante viene silenziosamente azzerata dalla RLS self-only di quella tabella — non un errore, un JOIN che restituisce zero righe. Scoperto proprio su `beta_notifiche_admin_count()` (0062, corretta in `0063` aggiungendo `security definer` solo a questa funzione): il badge tornava sempre 0 anche con dati reali, nessun errore visibile in nessun log — il sintomo è "il numero è sempre sbagliato", non un'eccezione.

## Modello dati — dove trovarlo

Non esiste più un unico schizzo statico del modello dati: dopo la "Revisione satelliti" di agosto (vedi tabella sotto, a partire dal 2026-08-01) lo schema delle Attività (ex "satelliti") è stato riscritto più volte in sprint successivi, ciascuno documentato dove è stato deciso. Per capire lo stato **attuale** dello schema, la fonte primaria sono le migration in `supabase/migrations/` (l'ultima applicata fa fede) insieme alle voci più recenti della tabella "Decisioni prese" qui sotto, in particolare: Revisione satelliti Fase 1 (2026-08-01, base di preventivo/progetto/acquisti/campione), Sprint "fondamenta" (creazione automatica, ordine delle attività), Sprint B/C/D di agosto (Appuntamento, Progetto, Preventivo, Campionatura, Acquisto, Costruzione, Noleggio), e "Nuova attività Chiusura Lavoro" (2026-08-03, l'attuale — e unico — meccanismo di chiusura del Lavoro). Il vecchio modello Attività/Fasi e la prima versione del modello satellite (25/7) sono in CLAUDE-ARCHIVIO.md e **non riflettono più lo schema reale**.

## UI / Stile — linee guida

- Mobile-first, totalmente responsive, navigazione via menu hamburger a scomparsa.
- Palette: bianco/nero/grigio come base; colori "a LED" (rosso/giallo/verde) riservati agli stati; palette bottoni limitata e uniforme in tutta l'app.
- Tipografia: serif (stile logo) solo nel logo/marchio; sans-serif ovunque nell'interfaccia.
- Schermata più critica da disegnare per prima: dettaglio Lavoro, deve rendere evidente "cosa manca per andare avanti" (Attività aperte/bloccate/SLA superati, Fasi non concluse).

### Note tecniche emerse in fase di implementazione

- `sla_attivita`: PostgreSQL non ammette colonne nullable in una PRIMARY KEY, neanche con COALESCE nella definizione. Soluzione adottata: `id UUID PRIMARY KEY` surrogato + `CREATE UNIQUE INDEX` con espressione `COALESCE(artigiano_id, '00000000-...')` — funziona perché gli expression index supportano COALESCE, le PK no.
- `NEXT_PUBLIC_SUPABASE_URL` deve essere la base dell'URL senza path (es. `https://xxx.supabase.co`), non includere `/rest/v1/`.
- Admin RLS: nessun accesso diretto alle tabelle operative, solo funzioni/view SQL con `SECURITY DEFINER` — dettaglio completo del ruolo admin (`is_admin`, guard, le due funzioni, il bug del route group) in "Principi architetturali" sopra.
- **Ambiente dev**: Node.js 18 non è compatibile con Next.js 15/16 (richiede ≥20). Installato Node.js 20 via nvm (`nvm use 20`). Dev server: `npm run dev -- --port 3456`.
- **Fix Tailwind/Turbopack**: `@tailwindcss/oxide` non trova il binding nativo in modalità Turbopack. Fix: copiare `node_modules/@tailwindcss/oxide-linux-x64-gnu/tailwindcss-oxide.linux-x64-gnu.node` dentro `node_modules/@tailwindcss/oxide/`. Va rifatto se si cancella `node_modules`.
- Nessuna credenziale di connessione diretta Postgres (connection string) salvata in locale: le migration vengono applicate a mano via SQL Editor Supabase, non con `supabase db push`.

### 4) Convenzione asterisco sui campi obbligatori
Rimosse tutte le etichette testuali "(opz.)" (21 occorrenze in 11 file)
in favore di un **asterisco rosso** (`<span className="text-red-500">*</span>`)
accanto all'etichetta dei soli campi **obbligatori**; i facoltativi
restano impliciti per assenza di marcatura. Applicata in modo coerente
a tutti i form dell'app: registrazione, invito "a quattro mani", login,
password dimenticata/reset, Cliente, Fornitore/Sede/Contatto, Lavoro
(nuovo/standalone/modifica), e tutti i satelliti (Appuntamento,
Progetto/Preventivo/Campione, Ordine, Costruzione, Noleggio).

**Colore scelto (rosso, non un grigio neutro)**: coerente con l'uso
già esistente del rosso per i messaggi di errore nei form
(`text-red-600`), non introduce quindi una terza semantica di colore —
resta comunque distinto dal rosso "a LED" riservato agli stati dei
satelliti (un asterisco è un simbolo tipografico accanto a un'etichetta,
non un pallino di stato, il rischio di confusione è marginale e diverso
da quello già accettato per il logo/brand nella decisione del 19/7).

**Criterio usato per stabilire obbligatorio/facoltativo**: campi con
validazione client esplicita che blocca il submit se vuoti (o vincoli
`NOT NULL` a DB senza fallback) → asterisco; campi liberi/nullable →
nessun marcatore. **Select con un valore di default sempre presente**
(es. Nazione, Prefisso telefono, Sicurezza SMTP) non hanno ricevuto
asterisco: non possono essere lasciati vuoti dall'utente, quindi non
c'è ambiguità da segnalare. **Eccezione**: il campo Specializzazione
(registrazione e invito) **ha** l'asterisco pur essendo una `<select>`,
perché la sua opzione di default è "Seleziona..." (valore vuoto), non
un valore valido — può essere lasciato vuoto e la validazione lo
richiede esplicitamente.

### Note tecniche / infrastrutturali — riferimento rapido (dettaglio completo in CLAUDE-ARCHIVIO.md)

- **Cloudflare (proxy attivo su districo.it) blocca falsamente le Server Actions su `/lavori/`**: una regola WAF gestita per CVE-2025-55183 genera falsi positivi contro richieste legittime (Districo non è vulnerabile, gira su Next.js oltre la versione patchata). Fix applicato lato Cloudflare (non nel codice): Custom Rule "Skip - All managed rules" per i path contenenti `/lavori/`. Se un'azione server fallisce in produzione in modo intermittente e senza traccia nei log del container/Nginx, sospettare per primo un blocco a livello Cloudflare/CDN.
- **Porta SMTP 465 bloccata in uscita da apphub** (Hetzner o rete a monte, non l'OS del VPS) — usare sempre 587/STARTTLS, mai 465/SSL diretto, per SMTP di sistema o credenziali personali degli artigiani.
- **Docker Compose interpreta `$$` → `$` anche nei valori letti da `env_file:`** (non solo in `environment:`): una password con caratteri `$` letterali va scritta raddoppiando ogni `$` (`$` singolo → `$$`) per sopravvivere intatta a runtime.
- **`ignoreTLS` necessario per l'opzione "Nessuna cifratura" SMTP**: `secure:false`+`requireTLS:false` da soli non disabilitano lo STARTTLS opportunistico di nodemailer — se il server lo annuncia con un certificato non valido, l'invio fallisce comunque. `lib/email/send-email-personale.ts` imposta `ignoreTLS` esplicito quando l'utente sceglie "Nessuna". Preset provider comuni (Aruba, Gmail/Workspace, Register.it, Microsoft 365, Libero, hosting generico) precompilano host/porta/sicurezza in Profilo/Impostazioni.
- **Upload allegati**: due limiti Next.js indipendenti da alzare insieme (`experimental.serverActions.bodySizeLimit` e `proxyClientMaxBodySize`, quest'ultimo per via di `middleware.ts` attivo su ogni richiesta) — entrambi a 20MB, coerenti con `client_max_body_size` di Nginx. Le immagini caricate vengono ridimensionate server-side con `sharp` (lato massimo 1920px, qualità 82, EXIF applicato e poi rimosso) — i PDF restano invariati byte-per-byte.
- **Le note "non ancora applicata a Supabase Cloud" in questo file possono diventare stale**: se l'utente esegue una migration manualmente dallo SQL Editor senza che questo venga registrato in una sessione successiva, la nota scritta qui non riflette più la realtà — verificare lo stato reale del DB (query REST con la service role key) prima di trattare una migration come non ancora live, specialmente se questo condiziona *come* si scrive un fix (editare una migration già applicata è sbagliato: se ne aggiunge una nuova che corregge la precedente).
- `cloneElement`/`isValidElement` su un elemento passato da un Server Component a un Client Component **non è affidabile** (Next.js può serializzarlo come riferimento "lazy" non risolto) — se serve mostrare la stessa istanza di un componente in due modalità diverse (es. sola lettura vs modifica) a seconda di un'interazione client-side, costruire entrambe le varianti lato server invece di un override a runtime.

## Prossimi passi aperti

- **Landing page — riscritta da zero il 2026-08-21, Pricing+Beta+FAQ aggiunte lo stesso giorno** (vedi tabella sotto): sostituisce sia la modalità "coming soon" sia l'intera landing "definitiva" del 19/8 (framework narrativo incompatibile, il selettore "che artigiano sei" del 19/8 è stato esplicitamente abbandonato). 9 sezioni implementate (Hero, Problem, Agitation, Solution, Tecnico "Come funziona", Tecnico "Funzioni per fase", Pricing — prima CTA principale della pagina, Beta testing, FAQ) — **restano da costruire, non ancora pronte**: Pratico, Recensioni, Footer dedicato (oggi la pagina usa comunque il `SiteFooter` globale, invariato). Un componente per sezione già in `app/page.tsx`: aggiungerle significa importare un nuovo componente, non toccare quelli esistenti.
- **Due funzionalità promesse dalla FAQ (2026-08-21) NON ancora implementate nel codice, segnalate esplicitamente per pianificarle a parte**: (1) limite di "100 lavori attivi contemporaneamente" — nessun conteggio/vincolo del genere esiste oggi in nessun punto dell'app, il numero vive solo nel testo della FAQ; (2) "esportare ogni lavoro in PDF, con tutti i suoi dettagli" — esiste già la Scheda di lavoro PDF (17/8), ma è deliberatamente priva di ogni dato economico (pensata come foglio di appunti per l'artigiano, non un export completo) — non copre alla lettera quanto promesso dalla FAQ. Testo FAQ pubblicato comunque così com'è su richiesta esplicita, entrambi i gap segnalati invece di bloccare la pubblicazione.
- **CLAUDE.md sopra i 150K caratteri — RISOLTO il 2026-08-21**: sessione di riordino eseguita su richiesta esplicita (5 righe del blocco landing 19/8 spostate in CLAUDE-ARCHIVIO.md, ormai fattualmente superate dalla sessione "Landing definitiva" dello stesso giorno), file tornato a 135,5K caratteri.
- **Ordine delle Attività di trattativa personalizzabile per mestiere**: oggi fisso in codice (`lib/lavori/attivita-ordine.ts`), uguale per tutti gli artigiani — da affrontare solo se emergerà un bisogno reale (non ipotetico), riusando lo stesso pattern già previsto per `Fase_Template` nel brief originale (vedi riga 2026-08-03 "Nota architetturale" nella tabella sotto per il dettaglio).
- **Propagazione del template Briefing (2026-08-10) COMPLETA**: tutti e 9 i satelliti sono ora su `PilloleSalvaAnnulla` — Preventivo e Progetto restylati l'11/8, Campionatura e Costruzione il 12/8, Chiusura Lavoro il 13/8 (effetto collaterale del restyling calcoli economici), Montaggio e "Attività non preventivate" nati direttamente sul nuovo standard, Noleggio migrato durante l'audit del 13/8, **Acquisto restylato il 14/8** (vedi tabella sotto, sessione "restyling Acquisto + catalogo Referenze") — era l'ultimo rimasto su `SalvaFlottante`. Le dimensioni/margini della Modal condivisa (`components/modal.tsx`) restano allineate alla Modal di test dall'11/8, nessun intervento necessario per nessuno di questi restyling. **Anche la Modal di test** (`components/test/modal-test.tsx`) resta indietro su un punto specifico: ha solo una pillola "Salva" (nessun "Annulla" affiancato) — notato e lasciato intoccato come richiesto esplicitamente in più sessioni, non è nello scope di una sessione che restyla un satellite reale.
- **Gestione standalone del catalogo Referenze — COMPLETATA il 14/8** (vedi tabella sotto, stessa giornata): non più un gap aperto.
- Fix tastiera mobile (Modal di test, 7/8): verificato finora solo su Android, da verificare anche su iOS prima di considerarlo definitivo — invariato da questa sessione.
- **"Aggiungi attività" non route-ificata nel refactor Parallel/Intercepting Routes del 12/8** (vedi tabella sotto): decisione autonoma per contenere lo scope, resta stato locale come prima — il tasto Back non la chiude (nessuna regressione, non la chiudeva nemmeno prima). Follow-up naturale a basso rischio se emergerà il bisogno: stesso pattern già pronto (`AttivitaModaleRoute`/`bloccaBackConModifiche`), ma qui non serve alcuna protezione dirty-state (è un selettore di creazione, non un form).
- **Revisioni del Preventivo non raggiungibili dalla UI**: la modale Preventivo restylata (11/8, sul template Briefing) non espone alcuna interfaccia per creare o consultare nuove revisioni — `revisione_di` esiste ancora a schema e `costruisciCatena()` mostra eventuali catene storiche precedenti alla revisione satelliti dell'1/8, ma non c'è alcun modo per l'utente di generarne una nuova dall'interfaccia attuale. Da affrontare in una sessione dedicata quando si deciderà come gestire le revisioni del preventivo in interfaccia (non ancora pianificato).
- **Integrazione Stripe — IMPLEMENTATA il 2026-08-21 sera, modalità test/sandbox** (vedi tabella sotto per il dettaglio completo): abbonamenti trial (**30gg**, ridotto da 60 il 2026-08-22 — vedi tabella sotto) + ricorrenti (mensile €5/annuale €48), checkout, webhook, gate "trial scaduto" tutti funzionanti e verificati end-to-end con carta di test reale. **Resta esplicitamente fuori scope, da pianificare a parte quando si passerà a produzione/pagamenti reali**: passaggio da chiavi `sk_test_`/`pk_test_` a chiavi live; nessun Customer Portal Stripe self-service (cancellazione/cambio piano autonomi — oggi solo lettura in Impostazioni, nessun link); nessuna prevenzione di trial ripetuti per lo stesso artigiano che annulla e ri-sottoscrive (Stripe concede un nuovo `trial_period_days: 30` ad ogni checkout, per design non essendo stato richiesto di impedirlo); un artigiano "ospite" (a quattro mani) non ha un trattamento distinto dal proprietario del Lavoro — il gate si applica a ogni artigiano individualmente, ma la funzionalità "a quattro mani" resta comunque priva di un punto di ingresso UI (invariato dal 18/7); regola di obbligatorietà "almeno uno tra Codice SDI/PEC" per la fatturazione elettronica, ancora non implementata (campi liberi, nessun vincolo incrociato, invariato dal 19/8).
- Per lo storico dei passi ormai completati (migration da applicare, deploy in sospeso, credenziali da aggiornare — tutti risolti nel frattempo), vedi CLAUDE-ARCHIVIO.md.

## Decisioni prese

| Data | Decisione |
|---|---|
| 2026-08-21 | **Coming soon: hero responsive desktop/mobile + riposizionamento testo sulla zona di cielo + ottimizzazione foto Personaggi**. Tre asset nuovi forniti dall'utente: `public/landing/hero/desktop.webp` (16:9, 5 artigiani, sostituisce `HeroCover.webp`), `public/landing/hero/mobile.jpg` (9:16, 3 artigiani, zona cielo alta lasciata pulita apposta per il testo) e le 5 nuove foto "Personaggi" fotorealistiche (`public/landing/personaggi/c_*.jpg`, sostituiscono le vecchie illustrazioni flat — rimosse dal server prima di questa sessione, non ancora reintegrate nel markup, la sezione "Solution" resta inattiva dietro il flag coming-soon). **Hero responsive**: `<picture>` con `<source media="(min-width: 640px)">` per desktop.webp, `<img>` mobile.webp come fallback/sotto soglia — breakpoint 640px scelto perché è il già lo standard "sm:" di Tailwind usato in tutto il resto dell'app (Modal condivisa, griglia icone "Aggiungi attività", 19/8) per lo switch mobile/desktop, nessuna ambiguità reale da porre. **Compressione**: `mobile.jpg` (3,5MB) convertito in webp con `sharp` (ridimensionato a 800px di larghezza, quality 75) → 147KB, in linea con `desktop.webp` (152KB) come richiesto; le 5 foto Personaggi (3-3,3MB ciascuna) convertite allo stesso modo (900px, quality 75) → 65-124KB ciascuna, stesso ordine di grandezza. Nessun tool CLI (`cwebp`/`imagemagick`) disponibile nell'ambiente — usato `sharp` via script Node one-off (non versionato, stesso principio già in uso per altri script one-off in questo progetto), scelta obbligata più che preferita ma coerente col toolchain già in uso nel progetto per lo stesso tipo di operazione (resize/compressione asset). File originali (`mobile.jpg`, i 5 `c_*.jpg`) rimossi dopo la conversione, non tenuti in parallelo al webp. **Riposizionamento testo**: il blocco titolo/payoff/"Coming soon" era centrato nel viewport (flex `items-center`) e finiva sovrapposto al gruppo di persone invece che sulla fascia di cielo — sezione passata a `items-start` con il blocco testo posizionato in alto via padding-top in `svh` (`pt-[19svh] sm:pt-[13svh]`, offset diversi per mobile/desktop perché la proporzione della zona di cielo pulita differisce tra le due foto — verticale 9:16 su mobile ha una fascia più alta prima delle teste rispetto all'orizzontale 16:9 desktop, misurato a occhio sulle foto reali poi affinato via screenshot). Gradiente di contrasto in testa esteso da 30svh a 46svh mobile/34svh desktop per coprire la nuova posizione del testo (non solo dietro l'header come nella versione precedente), stesso principio già in uso — `drop-shadow` su ogni riga di testo invariato. **Verificato end-to-end** (dev server locale, script Playwright con Chromium via `playwright-core`, 4 viewport — 390×844, 412×915, 1440×816, 1920×1080): sorgente `<picture>` corretta per breakpoint (`currentSrc` verificato sotto/sopra i 640px), testo sempre leggibile e mai sovrapposto ai volti in tutti e 4 i viewport — **ispezionato visivamente via screenshot**, zero errori console/pageerror/request falliti, tutti i webp validi (`file` conferma VP8/dimensioni attese). `tsc --noEmit`/`eslint`/`npm run build` puliti. Nessuna migration. |
| 2026-08-21 | **Landing definitiva — sostituisce sia la modalità "coming soon" sia l'intera landing "definitiva" precedente (19/8), riscritta da zero su un nuovo framework Problem/Agitation/Solution + due sezioni tecniche**. 6 sezioni confermate in questa sessione (Hero, Problem, Agitation, Solution, Tecnico "Come funziona", Tecnico "Funzioni per fase"), un componente per sezione — le sezioni future del brief (Pratico, Commerciale, FAQ, Recensioni, Footer) non ancora pronte, aggiungerle in seguito significa importare un nuovo componente in `app/page.tsx`, non toccare quelli già scritti. **Selettore "che artigiano sei" (19/8) ABBANDONATO su indicazione esplicita**: messaggio ora universale, non differenziato per mestiere — rimossi `MestiereProvider`/`mestiere-context.tsx`, `landing-selettore-mestiere.tsx` e ogni sezione che ne dipendeva. **Pulizia radicale del vecchio impianto**: dato che il nuovo framework narrativo (PAS) è incompatibile col precedente (Il caos/Filo logico/Personaggi illustrati/Beta/Pricing/FAQ/CTA finale — un copy completamente diverso, non solo una sezione in più), eliminati per intero — non lasciati come codice morto dietro flag — `landing-header.tsx`, `landing-caos.tsx`, `landing-filo-logico.tsx`, `landing-personaggi.tsx` (vecchia versione a griglia 5 mestieri), `landing-come-funziona.tsx`/`landing-funzioni-fase.tsx` (vecchia versione con esempi per mestiere), `landing-beta/pricing/faq/cta-finale.tsx`, `landing-coming-soon.tsx` + `lib/landing/coming-soon.ts` (il flag "coming soon" stesso: la sua ragion d'essere — sostituire temporaneamente la landing "vera" non ancora pronta — è finita, nessuna ambiguità nel rimuoverlo insieme al resto), `lib/landing/mestieri.ts`/`funzioni-fase.ts`/`cta.ts` (zero chiamanti dopo la pulizia, verificato con grep mirato prima di cancellare ciascuno — stesso principio già seguito ripetutamente in questo progetto per il codice diventato morto). **Sezione 1 (Hero)**: riusa esattamente il markup/immagine/posizionamento della Hero "coming soon" (`<picture>` responsive, testo ancorato sulla fascia di cielo, header trasparente overlay con logo+Accedi) — tolta solo la riga "Coming soon", il resto del copy richiesto per questa sessione era già lo stesso testo. **Nessun header sticky separato**: l'header vive dentro la Hero (overlay sull'immagine) — un secondo header sopra duplicherebbe logo/login; le sezioni successive restano quindi senza logo/accesso visibile finché non si torna in cima, accettato perché la richiesta era esplicita ("riusa la Hero così com'è", "Accedi resta dove già posizionato"). **Sezione 2 (Problem)**: 4 card icona+titolo+testo (Come/Quanto costa/Cosa serve/Quanto tempo) — 4 icone nuove disegnate a mano nello stesso stile stroke-based del progetto (non `ICONA_ATTIVITA`: quelle rappresentano i tipi di Attività dell'app, un concetto diverso da queste 4 domande generiche), cerchio icona `bg-gray-900`/testo bianco, nessun colore fuori dalla palette bianco/nero/grigio. **Sezione 3 (Agitation)**: sfondo `bg-gray-900` + contenitore stretto (`CONTENITORE_STRETTO`), paragrafi ravvicinati e lista puntata invece delle card ordinate di Problem — come richiesto esplicitamente ("testo che si accumula visivamente"), nessuna icona/griglia. **Sezione 4 (Solution)**: una delle 5 foto "Personaggi" (già convertite in webp/ottimizzate il 21/8 mattina, non ancora usate nel markup fino ad ora) scelta a caso al caricamento pagina. **Scelta tecnica non banale**: il primo tentativo (`useState(FOTO[0])` + `useEffect(() => setFoto(random))`) violava la regola ESLint del progetto `react-hooks/set-state-in-effect` (errore, non warning) — sostituito con `useSyncExternalStore` (`getSnapshot` calcola e mette in cache la scelta random una sola volta a livello di modulo, `getServerSnapshot` sempre `FOTO[0]` deterministico per SSR/primo render client, nessun mismatch di idratazione, nessun setState in un effect) — verificato con script Playwright che pagine caricate in browser context separati ottengano foto diverse. **Sezione 5 (Tecnico "Come funziona")**: 4 passi con i 4 screenshot già esistenti (`public/landing/screenshots/`), copy aggiornato secondo il brief di questa sessione (diverso dal copy della vecchia sezione "Come funziona" del 19/8, eliminata). **Predisposto per la sostituzione futura con video**, come richiesto esplicitamente: `media` è un'unione discriminata (`{tipo:'img', src, alt} | {tipo:'video', src}`), un componente `MediaPasso` risolve già entrambi i casi — sostituire un passo con un video sarà un cambio di un solo oggetto dati, nessuna modifica al resto del componente. **Sezione 6 (Tecnico "Funzioni per fase")**: icone/etichette riusate esattamente da `ICONA_ATTIVITA`/`LABEL_ATTIVITA` (`icone-attivita.tsx`/`attivita-ordine.ts`, la mappatura icona↔tipo già in uso nella griglia "Aggiungi attività" dell'app) — solo icona+nome, nessuna descrizione, come richiesto. **Ordine/raggruppamento Trattativa/Esecuzione preso alla lettera dal brief di questa sessione, non da `ORDINE_ATTIVITA`**: quell'ordine serve l'app operativa, la classificazione narrativa qui richiesta è diversa (es. Progetto raggruppato sotto "Esecuzione", non subito dopo Briefing in trattativa) — rispettata così com'è, non "corretta" per coerenza con l'app. Chiusura Lavoro non compare in nessuna delle due righe (arriva sempre per ultima e in automatico, citata solo nel testo di chiusura), coerente con `RIPETIBILE_ATTIVITA.chiusura === false`. **`components/app-nav.tsx`**: commento ormai stale su `landing-header.tsx` (eliminato) aggiornato per puntare al nuovo header integrato nella Hero — nessuna modifica di comportamento, `PAGINE_PUBBLICHE`/il gate su `pathname==='/'` restano invariati. **Verificato end-to-end** (dev server locale riusato, script Playwright con Chromium via `playwright-core`, viewport 390×844 e 1440×900 — screenshot completi di pagina intera più ritagli ravvicinati sulle sezioni Problem e Funzioni per fase per la leggibilità delle icone): nessun overflow orizzontale, zero errori console/pageerror/request falliti su entrambi i viewport, selezione random della foto Solution verificata su 4 contesti browser separati (esiti diversi, nessuna foto sempre identica) — **ispezionato visivamente via screenshot**. `tsc --noEmit`/`eslint`/`npm run build` puliti. Nessuna migration (solo application code + asset già presenti). **Nota per chi riprende**: questa voce aveva portato CLAUDE.md sopra i 150K caratteri (156,7K) — segnalato all'utente, sessione di riordino eseguita su richiesta esplicita subito dopo (stesso giorno, vedi header di questo file e CLAUDE-ARCHIVIO.md per il dettaglio del travaso). |
| 2026-08-21 | **Landing: Sezione 7 (Pricing) e Sezione 8 (Beta testing)**, aggiunte subito dopo "Funzioni per fase". **Pricing — prima CTA principale della pagina** (le sezioni precedenti non ne avevano una, per decisione esplicita della sessione Hero del 19/8): copy del trial (60gg, carta registrata ma addebito solo a scadenza, mail di promemoria, disdetta libera) più due card prezzo (€5/mese, €48/anno con badge "Risparmi rispetto al mensile" — testo letterale dato dal brief, nessuna percentuale calcolata/inventata) e il testo sul confronto mensile→annuale nella mail di promemoria. CTA "Inizia la prova gratuita" → `/registrazione`, lo stesso e unico flusso di iscrizione esistente — nessuna raccolta carta reale implementata (fuori scope di una sessione di landing/copy, coerente con "Futuro: app a pagamento con Stripe" ancora non pianificato). **Colore CTA — riusato il token esistente, non un nuovo colore**: `bg-sky-500`/`hover:bg-sky-600`, lo stesso di `PILLOLA_CLASSI_PRIMARIA` (`pillola-flottante.tsx`, bottoni "Salva" in tutta l'app) — nuovo `lib/landing/cta.ts` (`CTA_PRINCIPALE_CLASSI`/`CTA_SECONDARIA_CLASSI`, la stessa coppia già esistita ed eliminata nella sessione precedente perché senza chiamanti, ricreata ora che la sezione Commerciale la rende di nuovo necessaria). **Contrasto WCAG AA verificato con rendering reale, non a calcolo**: script Playwright con conversione canvas (`fillStyle`+`getImageData`) dei colori restituiti da `getComputedStyle` — Tailwind v4 li espone in `lab()`/`oklab()`, non rgb letterale, un parsing testuale ingenuo avrebbe prodotto numeri falsati (infatti il primo tentativo di verifica li ha prodotti, scartato). Risultati: `bg-sky-500`+testo bianco (il testo della pillola originale) è **2.71:1**, sotto soglia AA — usato invece `text-gray-900` su `bg-sky-500`, **6.56:1**; nessuno scurimento del fondo in hover (solo `hover:shadow-xl`), stesso 6.56:1 in entrambi gli stati, verificato con un hover reale nello script. **Sezione Beta**: titolo+intro+elenco puntato (2 voci, grassetto su "6 mesi di accesso gratuito" e su "gratis per sempre" nella seconda, come richiesto) + CTA secondaria "Richiedi un posto" → `mailto:info@districo.it?subject=...` (stesso indirizzo del Footer, nessun form/backend dedicato). **CTA secondaria — stesso trattamento cromatico azzurro ma a contorno**, come richiesto esplicitamente: `border-2 border-sky-500 text-sky-700` su sfondo bianco — stessa distinzione strutturale primaria/riempita vs secondaria/bordata già in uso in `pillola-flottante.tsx` (lì bordo grigio neutro, qui ricolorato in azzurro per assecondare la richiesta). Contrasto verificato con lo stesso script: **5.86:1** a riposo, **5.49:1** in hover (`hover:bg-sky-50`) — entrambi sopra soglia. **Flag "programma chiuso"**: `lib/landing/beta.ts` (`BETA_PROGRAMMA_APERTO`, booleano semplice come richiesto esplicitamente — "per ora non serve un meccanismo automatico") — quando `false`, nasconde titolo/testo/elenco/CTA e mostra SOLO la nota conclusiva in corsivo ("I posti sono limitati…", sempre presente in entrambi i rami, mai condizionata dal flag). **Verificato end-to-end** (dev server locale riusato, script Playwright con Chromium via `playwright-core`): screenshot mirati sulle due sezioni a 390px e 1440px, nessun overflow orizzontale, zero errori console/pageerror; **flag testato dal vivo** — `BETA_PROGRAMMA_APERTO` temporaneamente impostato a `false`, verificato che la sezione mostri solo la nota conclusiva (screenshot di conferma), poi ripristinato a `true`. `tsc --noEmit`/`eslint`/`npm run build` puliti. Nessuna migration (solo application code). |
| 2026-08-21 | **Landing: Sezione 9 (FAQ) + componente "trial scaduto" (non agganciato)**. **FAQ** (`components/landing/landing-faq.tsx`, subito dopo Beta testing): accordion via `<details>`/`<summary>` nativi — nessun JS/Client Component necessario, coerente col principio "niente dipendenze pesanti" già seguito altrove nel progetto — marcatore nativo nascosto (Chrome/Firefox via `marker:content-none`, Safari via `[&::-webkit-details-marker]:hidden`) sostituito da una chevron SVG a mano nello stesso stile stroke-based del resto del progetto, ruotata via `group-open:rotate-180`. 8 domande/risposte, testo fornito dall'utente parola per parola, pubblicate così come date. **Due gap segnalati come richiesto, non implementati** (dettaglio in "Prossimi passi aperti"): il limite "100 lavori attivi" citato dalla FAQ non esiste in nessun punto del codice (verificato con grep mirato, zero riscontri fuori dal testo FAQ stesso); "esportare ogni lavoro in PDF con tutti i dettagli" è coperto solo in parte dalla Scheda di lavoro PDF esistente (17/8), che esclude deliberatamente ogni dato economico — non un vero export completo. **Componente "trial scaduto"** (`components/trial-scaduto.tsx`): per un utente che tenta di accedere dopo la scadenza del trial — la richiesta originale chiedeva di individuare il punto giusto nel flusso di autenticazione (probabilmente login) e di fermarsi se la logica trial/abbonamento non fosse ancora implementabile lato backend invece di improvvisare; verificato che non esiste alcuno stato trial/abbonamento a schema, **su indicazione esplicita dell'utente si è proceduto con la sola costruzione del componente presentazionale** (titolo "Il tuo periodo di prova è terminato.", testo forniti dall'utente parola per parola), **senza alcun aggancio reale** — nessuna colonna DB, nessun redirect da login/middleware. Riusa `AuthCard` (stesso contenitore di Login/Registrazione, il contesto in cui un utente incontrerebbe questo messaggio) e `CTA_PRINCIPALE_CLASSI` (stessa CTA azzurra della landing, riusata invece di introdurne una simile) verso `/#prezzi`, l'unico punto di scelta piano esistente oggi. Il collegamento vero (verifica stato trial + redirect) resta esplicitamente rimandato a quando la logica trial/abbonamento e l'integrazione Stripe saranno pianificate (vedi "Prossimi passi aperti", stessa nota di SDI/PEC). **Verificato**: `tsc --noEmit`/`eslint`/`npm run build` puliti; FAQ testata visivamente via screenshot mobile (390px) e desktop (1440px) — accordion apre/chiude correttamente, nessun overflow orizzontale, zero errori console/pageerror. Il componente "trial scaduto" non è raggiungibile da alcuna route (nessun test visivo end-to-end nell'app, per costruzione — verificato solo che compili). **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). Nessuna migration (solo application code). |
| 2026-08-21 | **Integrazione Stripe — abbonamenti trial (60gg) + ricorrenti, modalità test/sandbox**. Migration `0057`. Chiavi Stripe (`STRIPE_SECRET_KEY`/`STRIPE_PUBLISHABLE_KEY`/`STRIPE_PRICE_ID_MENSILE`/`STRIPE_PRICE_ID_ANNUALE`) fornite dall'utente già in `.env` locale — **scoperto durante la sessione che l'`.env` di produzione (apphub) non le aveva affatto**, nonostante l'istruzione iniziale dicesse il contrario: l'utente intendeva l'`.env` locale. **1) Checkout** (`lib/stripe/actions.ts`, Server Action `avviaCheckout(piano)`): crea/riusa un Customer Stripe (email dell'artigiano, `metadata.artigiano_id`), apre una Checkout Session hosted (`mode:'subscription'`, `trial_period_days:60`, `success_url`→`/lavori?abbonamento=attivato`, `cancel_url`→`/profilo/impostazioni`) — nessun uso di Stripe.js/Elements lato client, solo redirect all'URL restituito da Stripe. Guardia server-side contro un doppio abbonamento (stato già `active`/`past_due`/`trialing`-non-scaduto → errore esplicito). **Nessun collegamento sincrono dalla registrazione**: la conferma email è obbligatoria in produzione (template "Confirm signup" personalizzato, 19/7) — al momento del `signUp()` non esiste ancora una sessione autenticata da cui creare una Checkout Session; la scelta del piano avviene sempre DOPO il primo login, non durante il form di registrazione (il brief condizionava questo hook a "se esiste già un passaggio dedicato nella registrazione" — non esiste, nessuna modifica lì). **Due punti di ingresso**, stesso componente condiviso `components/scegli-piano-abbonamento.tsx` (due `<form>`, uno per piano, argomento legato via `.bind(null, piano)` — pattern standard Next.js per passare un valore a una Server Action usata come form action, `useFormStatus` per lo stato "Reindirizzamento a Stripe…"): (a) dentro `TrialScaduto` (sostituisce l'unica CTA generica precedente verso `/#prezzi`, irraggiungibile da loggato); (b) nuovo tab **"Abbonamento"** in Impostazioni (`components/profilo-abbonamento-form.tsx`, `ImpostazioniTabs` — lo slot che il codice aveva già anticipato dal 19/8), mostra stato/piano/data fine trial in sola lettura e i bottoni piano solo quando `stato ∈ {nessuno, canceled}` — punto di ingresso indispensabile per un artigiano `'nessuno'` (mai bloccato dal gate, altrimenti senza alcun modo di sottoscrivere un piano dall'app). **2) Webhook** (`app/api/stripe/webhook/route.ts`, URL `https://www.districo.it/api/stripe/webhook`): verifica firma HMAC con `STRIPE_WEBHOOK_SECRET` (`stripe.webhooks.constructEvent`, richiede il body RAW — `request.text()`, nessuna configurazione aggiuntiva necessaria nei Route Handler App Router a differenza delle vecchie Pages API routes). `customer.subscription.created/updated/deleted` trattati identicamente (`deleted` arriva comunque con `status:'canceled'`) — `sincronizzaAbbonamento()` mappa lo stato Stripe sui 5 valori del CHECK a schema (`trialing`/`active`→diretti, `past_due`/`unpaid`/`incomplete`→`past_due`, `canceled`/`incomplete_expired`/`paused`→`canceled`), ricava il piano dal `price.id` della prima riga (confrontato con le due env var), converte `trial_end` (unix seconds) in ISO — lookup dell'artigiano per `metadata.artigiano_id` (impostato alla creazione della Checkout Session) con fallback su `stripe_customer_id` (indice univoco parziale, migration 0057). `invoice.paid` solo loggato (conferma addebito, nessuna scrittura aggiuntiva richiesta). `customer.subscription.trial_will_end`: **nessun invio mail** come richiesto esplicitamente, solo un log — gancio predisposto per un futuro promemoria (riuserebbe `lib/email/send-email-personale.ts`). **Bug reale trovato e corretto testando il flusso end-to-end**: `middleware.ts` intercettava anche `/api/stripe/webhook` (nessuna eccezione in `PUBLIC_PATHS`) e reindirizzava a `/login` qualunque chiamata senza sessione — cioè SEMPRE, dato che Stripe chiama da server a server, senza cookie; il webhook non avrebbe mai funzionato in produzione. Aggiunto `/api/stripe/webhook` a `PUBLIC_PATHS` (l'autenticazione della richiesta resta comunque garantita, non dal middleware ma dalla verifica della firma HMAC dentro il route handler). **3) Modello dati**: nuove colonne su `artigiano` (non una tabella satellite — un artigiano ha un solo abbonamento corrente, nessuno storico da tracciare oltre quello che Stripe mantiene lato suo, stesso approccio già usato per `codice_sdi`/`pec`, 0056) — `stripe_customer_id`/`stripe_subscription_id` (`text`, nullable), `stato_abbonamento` (`text not null default 'nessuno'`, CHECK 5 valori), `piano_abbonamento` (`text`, CHECK `mensile`/`annuale`/null), `trial_fine` (`timestamptz`). Indice univoco parziale su `stripe_customer_id` (nullable, stesso pattern di `sla_attivita`). **4) Gate "trial scaduto" agganciato** — CORREGGE la sessione precedente (stesso giorno, componente presentazionale non agganciato): `app/(app)/layout.tsx` legge `stato_abbonamento`/`trial_fine` e sostituisce **tutto** il contenuto della sezione autenticata con `<TrialScaduto/>` (non solo un singolo redirect — l'intera app dietro il nav resta inaccessibile) quando `abbonamentoBloccato()` è vero: `stato==='canceled'` OPPURE (`stato==='trialing'` E `trial_fine` nel passato — controllo sulla data, non ci si fida ciecamente dello stato in caso il webhook non sia ancora arrivato). **`stato==='nessuno'` (mai avviato un checkout — quindi tutti gli artigiani reali oggi) NON è bloccante**, né lo è `past_due` (periodo di grazia implicito, non citato tra le condizioni di blocco nel brief — pratica comune mentre Stripe ritenta l'addebito). Nav (logout incluso) resta sempre visibile sopra il blocco, essendo nel root layout, non in quello `(app)`. **Verificato end-to-end con Stripe CLI reale** (installato ad-hoc, `~/.local/bin/stripe`, nessun `stripe login` interattivo — autenticato via `--api-key` dalla secret key locale): endpoint webhook di produzione creato per davvero via API (`stripe webhook_endpoints create`, modalità test, `livemode:false`) con i 5 eventi richiesti; per il test locale, `stripe listen --forward-to localhost:3456/api/stripe/webhook` (secret temporaneo separato da quello di produzione). Artigiano di test creato via Admin API REST diretta (email pre-confermata — l'SDK `@supabase/supabase-js` fallisce su Node 20 per `WebSocket` nativo mancante in `realtime-js`, aggirato con `fetch` diretto invece del client, unico impatto pratico dell'incompatibilità Node 20/22 già nota). **Flusso reale completo via Playwright + Chromium** (pacchetto `playwright` globale con browser già scaricati, nessuna nuova dipendenza nel progetto): login → Impostazioni → tab Abbonamento → click "Mensile" → redirect reale a `checkout.stripe.com` (verificato "60 days free / Then €5.00 per month", email precompilata) → compilata carta `4242 4242 4242 4242` → submit → **webhook ricevuto realmente** (`invoice.paid` + `customer.subscription.created`, entrambi `200` nel log di `stripe listen`) → **verificato a query diretta**: `stato_abbonamento='trialing'`, `piano_abbonamento='mensile'`, `trial_fine` a esattamente 60 giorni da ora, `stripe_customer_id`/`stripe_subscription_id` entrambi valorizzati. **Gate testato in tutti e 5 gli stati** (via PATCH diretto REST + reload, stesso artigiano di test): `trialing` valido→non bloccato + tab Abbonamento mostra "Periodo di prova in corso"/piano/data corretti con i bottoni piano nascosti; `trialing` con `trial_fine` nel passato→bloccato, bottoni piano visibili nel blocco stesso; `canceled`→bloccato; `active`→non bloccato; `past_due`→non bloccato; `nessuno`→non bloccato — 7 controlli, tutti con l'esito atteso. **Ispezionato anche visivamente via screenshot mobile** (420px): schermata "trial scaduto" con nav/logout/footer intatti sopra il blocco, tab Abbonamento con stato/piano/data leggibili. Zero errori nel log del dev server durante l'intero test. **Pulizia end-to-end**: subscription Stripe di test cancellata via CLI, utente/artigiano di test eliminato via Admin API (cascade confermato), processi locali (`next dev`, `stripe listen`) terminati, script temporanei rimossi. `tsc --noEmit`/`eslint` (scoped)/`npm run build` puliti. **Fuori scope, segnalato in "Prossimi passi aperti"**: passaggio a chiavi live, Customer Portal self-service, prevenzione trial ripetuti, trattamento "a quattro mani", obbligatorietà SDI/PEC. **Chiavi di produzione**: le 4 chiavi Stripe + `STRIPE_WEBHOOK_SECRET` (generato dall'endpoint webhook creato via API in questa sessione) aggiunte dall'utente a mano sull'`.env` di produzione (mai scritte direttamente da Claude — un tentativo di farlo via SSH è stato bloccato dal sistema di permessi, corretto: l'utente le ha aggiunte lui stesso, verificate poi via confronto hash SHA-256 prima di procedere). **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`) dopo la verifica end-to-end locale. **Nota per chi riprende**: questa voce ha portato CLAUDE.md sopra i 150K caratteri (153K) — da riordinare in una sessione dedicata quando richiesto esplicitamente, stesso principio già seguito più volte in questo file (vedi header). |
| 2026-08-22 | **Pagina admin `/admin/utenti` — anagrafica e stato abbonamento di tutti gli artigiani, toggle beta tester**. Migration `0058`+`0059` (fix). **Ambiguità reale posta esplicitamente prima di scrivere codice, come richiesto**: la richiesta chiedeva un nuovo campo `ruolo` enum ('artigiano'/'admin') — verificato PRIMA di implementare che esisteva già `artigiano.is_admin` (boolean, dalla migration iniziale) con un guard funzionante in `app/(admin)/layout.tsx` (redirect non-admin→`/lavori`, non autenticato→`/login`) e una pagina placeholder `app/(admin)/dashboard/page.tsx` — mai citati nella richiesta. Un nuovo `ruolo` avrebbe duplicato `is_admin` con lo stesso significato, rischio concreto di disallineamento. **Chiesto e confermato dall'utente**: riusare `is_admin`, nessun nuovo campo ruolo — solo `beta_tester boolean not null default false` aggiunto ad `artigiano` (marcatura manuale dell'admin per chi ha aderito al programma beta della landing, nessuna automazione/collegamento con Stripe). **Bug reale preesistente scoperto e corretto durante il testing, non introdotto da questa sessione**: `app/(admin)/` era una route *group* di Next.js — le parentesi non contribuiscono all'URL, quindi `app/(admin)/dashboard/page.tsx` risolveva a `/dashboard`, non `/admin/dashboard` (stesso per `/utenti`). Invisibile per un mese (unico commit su quella cartella: il setup iniziale del progetto) perché nessun artigiano aveva mai `is_admin=true` — un utente non autenticato/non-admin viene comunque reindirizzato dal middleware in base al solo pathname, mascherando un 404 sulla route sbagliata sottostante. Riprodotto e diagnosticato con un debug mirato (log assente nel layout → la route non veniva proprio raggiunta; build di produzione confermava `/dashboard`/`/utenti` invece di `/admin/*` nell'elenco route). **Fix**: cartella rinominata `app/(admin)` → `app/admin` (segmento URL reale, via `git mv` per preservare la history) — nessuna riscrittura del guard, stesso comportamento, ora al path corretto. **Protezione a due livelli, come richiesto esplicitamente** ("non solo a livello di route"): (1) route — `app/admin/layout.tsx` invariato, redirect prima di render; (2) DB — `admin_lista_artigiani()`/`admin_imposta_beta_tester(p_artigiano_id, p_valore)` (migration 0058), entrambe `SECURITY DEFINER` con verifica `is_admin` **interna** (`raise exception 'Accesso negato'` se il chiamante non è admin) — primo uso reale del principio già scritto in CLAUDE.md ("Admin RLS: solo funzioni/view SQL con SECURITY DEFINER"), applicato qui all'anagrafica invece che a metriche aggregate. RLS su `artigiano` resta "vede solo se stesso" (0001), INVARIATA — le due funzioni sono l'unico varco per leggere/modificare righe di altri artigiani, verificato che una chiamata RPC diretta (bypassando del tutto Next.js, con l'access token di un non-admin) restituisca `Accesso negato` sia per la lista sia per il toggle. **Bug reale trovato nella migration 0058 stessa, corretto con `0059` (non editata `0058`, già applicata in produzione — per convenzione del progetto)**: `returns table (id uuid, ...)` dichiara implicitamente `id`/`nome`/... come variabili plpgsql nello scope della funzione — il controllo `where id = auth.uid()` diventava ambiguo (variabile OUT vs colonna `artigiano.id`), Postgres rifiutava con `42702 column reference "id" is ambiguous`. Fix: alias esplicito sulla tabella nel controllo (`where art.id = auth.uid()`). **Contenuto pagina**: Nome e cognome, Email, Data di registrazione, Stato abbonamento e Piano (etichette riusate da `lib/abbonamento/labels.ts`, nuovo — estratte da `ProfiloAbbonamentoForm` per non duplicarle, unica fonte di verità), toggle Beta tester (`components/admin/beta-tester-toggle.tsx`, aggiornamento ottimistico con rollback su errore, chiama la Server Action `impostaBetaTester()` → RPC `admin_imposta_beta_tester`). **Nessun dato su Clienti/Fornitori/Lavori** (principio già in CLAUDE.md), ordinata per `created_at desc` (via la funzione SQL, nessun sort lato client), nessuna paginazione (lista semplice, come richiesto). Tabella con `overflow-x-auto` su mobile (nessuna variante a card — "non serve che sia elaborata", coerente con le altre scelte di scope minimale di questa sessione). Nav minimale aggiunta dentro `app/admin/layout.tsx` (Dashboard | Utenti, ora che le pagine admin sono due) — nessuna voce nel menu pubblico `AppNav` (un solo admin, accesso diretto via URL, per design). **Verificato end-to-end** (Supabase Cloud diretto — nessun ambiente locale isolato in questa sessione, artigiani di test creati/eliminati via Admin API REST diretta, is_admin impostato via service role, dev server + script Playwright + verifiche RPC dirette via curl, ambiente smontato a fine sessione — utenti di test cancellati, verificato che restassero solo i 3 artigiani reali): anonimo su `/admin/utenti`→`/login`; non-admin su `/admin/utenti` e `/admin/dashboard`→`/lavori`; **RPC diretta di un non-admin (bypassando Next.js) → `Accesso negato`** sia per `admin_lista_artigiani` sia per `admin_imposta_beta_tester` (anche con un `p_artigiano_id` che tenta di modificare l'account admin stesso); admin vede la pagina, tabella con tutte le righe attese (3 reali + 2 test), nessuna menzione di Clienti/Fornitori/Lavori; toggle beta tester invertito al click e persistito dopo reload — **ispezionato anche visivamente via screenshot** desktop (1280px, tabella completa e leggibile) e mobile (390px, scroll orizzontale funzionante). Zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` puliti (build di produzione usata anche per confermare `/admin/dashboard`+`/admin/utenti` nell'elenco route, prima e dopo il fix del path). **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). **Migration `0058` e `0059` applicate dall'utente su Supabase Cloud durante la sessione**, verificate indipendentemente (colonna `beta_tester` e le due funzioni RPC presenti via introspezione OpenAPI; la funzione corretta testata via chiamata RPC diretta prima di procedere). |
| 2026-08-22 | **Accesso gratuito permanente — deroga manuale admin indipendente da Stripe**. Migration `0060`. Nuova colonna `artigiano.accesso_gratuito` (boolean, default `false`) — override COMPLETO di `app/(app)/layout.tsx`: quando `true` il gate abbonamento (`abbonamentoBloccato()`, invariato) non viene nemmeno interrogato, copre anche `stato_abbonamento='nessuno'` (mai sottoscritto), non solo `'canceled'`/trial scaduto come richiesto esplicitamente. Terza colonna/toggle in `/admin/utenti`, accanto a Beta tester — stessa funzione SQL `SECURITY DEFINER` **dedicata** (`admin_imposta_accesso_gratuito()`, non una versione parametrica generica su nome colonna: con solo due flag da gestire una funzione esplicita per campo resta più semplice/sicura di un `update ... set %I` costruito a runtime, coerente con lo stile già in uso in tutte le migration precedenti). **Componente toggle generalizzato** (non duplicato): `BetaTesterToggle` rinominato `ToggleAdmin` (`components/admin/toggle-admin.tsx`) — accetta una `azione: (valore) => Promise<void>` invece di essere legato a un solo campo, la Server Action viene già legata all'id riga via `.bind(null, a.id)` prima di passarla come prop (stesso pattern di `avviaCheckout.bind(null, piano)` in `ScegliPianoAbbonamento`). **Bug reale scoperto applicando la migration, non nel codice TypeScript**: `create or replace function admin_lista_artigiani()` con una colonna in più in `returns table` è stato rifiutato da Postgres (`42P13 cannot change return type of existing function` — la vecchia definizione va rimossa esplicitamente prima). Corretto con `drop function if exists` prima del `create` nella stessa migration — **e riapplicati i GRANT/REVOKE** (il drop li cancella insieme alla funzione, dettaglio non ovvio: senza riapplicarli la funzione ricreata sarebbe tornata eseguibile da `public`/`anon`, una regressione di sicurezza silenziosa). **Verificato end-to-end su Supabase Cloud** (artigiano di test isolato via Admin API, creato/reso admin/eliminato a fine sessione): RPC `admin_lista_artigiani` restituisce `accesso_gratuito` per ogni riga; toggle diretto via RPC funziona e persiste; **RPC diretta con l'artigiano reso non-admin → `Accesso negato`** (stessa protezione DB delle altre due funzioni); **gate testato nei due casi rilevanti** — `stato_abbonamento='canceled'` + `accesso_gratuito=true` → utente NON bloccato, vede `/lavori` normalmente; stesso stato con `accesso_gratuito=false` → bloccato come prima (nessuna regressione sul gate esistente); toggle "Accesso gratuito" nella UI di `/admin/utenti` verificato via Playwright (invertito al click, persistito dopo reload) — **ispezionato anche visivamente via screenshot** (terza colonna ben allineata accanto a Beta tester). Zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` puliti (route `/admin/*` invariate nell'elenco). **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). **Migration `0060` applicata dall'utente su Supabase Cloud** — primo tentativo fallito per il bug del `create or replace` sopra, corretto nel file prima del secondo tentativo riuscito, verificato indipendentemente via introspezione OpenAPI (colonna e le 3 funzioni RPC presenti) prima di procedere. |
| 2026-08-22 | **Durata trial ridotta da 60 a 30 giorni**, ovunque compaia — codice e copy pubblico. `lib/stripe/actions.ts`: `trial_period_days` 60→30 (unico punto che lo imposta davvero, nelle Checkout Session create da `avviaCheckout()`). Copy landing: Sezione Pricing (`landing-pricing.tsx`, "60 giorni per provare Districo..." e "allo scadere dei 60 giorni..." nello stesso paragrafo) e Sezione FAQ (`landing-faq.tsx`, prima domanda "Devo pagare subito per iniziare?") — entrambe aggiornate a "30 giorni". **Ricerca dedicata per non lasciare residui** (richiesta esplicitamente): nessun'altra occorrenza di "60" legata al trial in nessun altro componente (`trial-scaduto.tsx` non cita mai un numero di giorni esplicito, invariato) né altrove nel codice applicativo. **Voci storiche della tabella sotto (21/8) NON modificate deliberatamente** — descrivono correttamente cosa era vero ed è stato verificato in quella sessione (60 giorni, incluso lo screenshot Stripe reale "60 days free"), riscriverle con "30" sarebbe stato falsificare la cronologia; solo la voce di stato in "Prossimi passi aperti" sopra, che descrive lo stato ATTUALE, è stata aggiornata. `tsc --noEmit`/`eslint`/`npm run build` puliti, ricerca testuale finale (`grep -rn "60 giorni\|trial_period_days: 60"`) confermata pulita prima del commit. **Committato e deployato su apphub**. Nessuna migration (solo application code + copy). |
| 2026-08-22 | **Statistiche aggregate admin — `/admin/dashboard`**. Migration `0061`. Riempie il placeholder "da implementare" presente dal 25/7 (stesso path già linkato dalla nav condivisa di `app/admin/layout.tsx`, nessuna nuova route/nessuna ambiguità sul path: la richiesta lasciava discrezione, "Dashboard" era già lì in attesa). `admin_statistiche_aggregate()` (SECURITY DEFINER, verifica `is_admin` interna, stesso pattern di `admin_lista_artigiani()`) — **una sola RPC per tutti i 6 conteggi richiesti** invece di 6 query separate: artigiani totali, nuove iscrizioni ultimi 7gg, lavori totali, lavori per stato, distribuzione stato_abbonamento, beta tester attivi. **Lavori in 3 bucket, non 2** ("in corso" vs "completati" come nella richiesta letterale) — non un'ambiguità da porre, una correzione silenziosa motivata: `lavoro.stato` ha 4 valori reali (`opportunita`/`accettato`/`rifiutato`/`completato`, verificato su `database.types.ts` — la migration 0001 originale ne aveva 3 diversi, superata dalla revisione strutturale di agosto), un Lavoro rifiutato non è né in corso né completato — mostrare solo 2 numeri li avrebbe fatti sommare a un totale sbagliato accanto al conteggio "Lavori totali" mostrato nella stessa pagina. `in_corso` = `opportunita`+`accettato` (stesso raggruppamento già in uso in `lavori_dashboard()` per il filtro "in corso" di `/lavori`), `completati` = `stato='completato'`, `rifiutati` = `stato='rifiutato'` — i tre sommano esattamente al totale, verificato. **UI**: griglia di card (`CardStat` per un numero singolo, `CardRipartizione` per una lista di sotto-valori dentro la stessa card — "Lavori per stato"/"Abbonamenti per stato" restano comunque UNA card per metrica come richiesto, non spezzate in card separate per sotto-valore), etichette stato abbonamento riusate da `lib/abbonamento/labels.ts` (stessa fonte di verità di `/admin/utenti` e del tab Impostazioni). Nav bidirezionale già presente per gratis (`app/admin/layout.tsx`, "Dashboard | Utenti" condiviso da entrambe le pagine dal 22/8 mattina), nessuna modifica necessaria lì. **SOLO conteggi aggregati** (principio esplicito della richiesta, già scritto in CLAUDE.md): nessuna riga della funzione contiene un id/nome/titolo di un singolo Cliente/Fornitore/Lavoro/artigiano, verificato anche a runtime (nessun testo riconducibile a un singolo utente nella pagina). **Verificato end-to-end su Supabase Cloud**: numeri della RPC confrontati contro query dirette sulle tabelle `artigiano`/`lavoro` prima di introdurre qualunque dato di test (3 artigiani reali, 19 Lavori reali — 9 accettato/5 completato/4 opportunita/1 rifiutato) — corrispondenza esatta; artigiano di test isolato creato via Admin API, reso admin, RPC richiamata con un JWT admin reale → numeri aggiornati correttamente (+1 su artigiani/nuove iscrizioni/nessuno); reso non-admin → RPC diretta rifiutata (`Accesso negato`), stessa protezione DB delle altre funzioni; nav bidirezionale Dashboard↔Utenti verificata via click reale; **ispezionato anche visivamente via screenshot** desktop (1280px, 6 card ben allineate su 3 colonne) e mobile (390px, impilate a 1 colonna, nessun overflow). Zero errori console/pageerror. Utente di test eliminato a fine sessione, verificato che restassero solo i 3 artigiani reali. `tsc --noEmit`/`eslint`/`npm run build` puliti. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). **Migration `0061` applicata dall'utente su Supabase Cloud**, verificata indipendentemente via introspezione OpenAPI prima di procedere. |
| 2026-08-22 | **Forum beta tester — `/beta`, community a thread tra beta tester e admin**. Migration `0062` + fix `0063`. **Modello dati**: `post_beta` (id, artigiano_id, titolo, stato `aperto`/`chiuso`, created_at, chiuso_at, nascosto) + `messaggio_beta` (id, post_id, autore_id, testo, created_at, nascosto) — l'autore di un messaggio può essere sia un beta tester sia l'admin, distinti al bisogno tramite `artigiano.is_admin`, nessun campo separato (come richiesto). **Decisione architetturale**: a differenza di `artigiano` (dove l'admin non ha mai un accesso diretto più ampio, solo funzioni SECURITY DEFINER), qui l'admin è un partecipante della community, non un supervisore esterno — le regole di accesso sono **RLS dirette** su `post_beta`/`messaggio_beta`, non funzioni admin: SELECT (beta tester vede i non nascosti, admin vede tutto), INSERT (beta tester apre un post proprio; su un messaggio, solo l'autore del post o l'admin, solo se `stato='aperto'`), UPDATE (solo admin — chiudi/riapri/nascondi/mostra). Nessuna policy DELETE (moderazione sempre soft-hide via `nascosto`, mai una cancellazione fisica, richiesto esplicitamente per tracciabilità). **Due funzioni SQL per l'atomicità, non per bypassare la RLS** (`SECURITY INVOKER`, nessun `security definer` — la RLS sopra concede già a ciascun chiamante i diritti giusti): `beta_crea_post(titolo, testo)` (post + primo messaggio in un'unica transazione implicita — il "titolo" è solo l'oggetto, il contenuto vero vive come messaggio #1, pattern forum classico) e `beta_chiudi_post_con_risposta(post_id, testo)` (inserisce la risposta di chiusura POI aggiorna `stato='chiuso'` — l'ordine conta: l'INSERT deve avvenire mentre il post è ancora `aperto` per rispettare la propria policy). Riaprire/nascondere restano `update` diretti via Server Action (RLS admin-only li protegge già, nessuna funzione dedicata necessaria). **Bug reale scoperto testando il badge admin, corretto in `0063`**: `beta_notifiche_admin_count()` era `SECURITY INVOKER` per scelta deliberata (non serve bypassare la RLS di post_beta/messaggio_beta) ma joina anche `artigiano` per leggere `is_admin` dell'autore dell'ULTIMO messaggio — quando quell'autore non è l'admin chiamante, la RLS "artigiano vede solo se stesso" (0001) nasconde silenziosamente quella riga, il JOIN produce zero righe e il conteggio torna sempre 0 indipendentemente dal contenuto reale (nessun errore, solo un numero sbagliato — dettaglio completo anche in "Principi architetturali" sopra, lezione riusabile per future funzioni simili). Fix: `security definer` aggiunto solo a questa funzione. **Nomi autori — nuovo problema mai incontrato prima**: un beta tester deve vedere nome/cognome di ALTRI beta tester (community condivisa) ma la RLS di `artigiano` lo vieta (self-only) — risolto con `beta_nomi_autori(p_ids)` (SECURITY DEFINER, verifica `beta_tester OR is_admin` del chiamante, espone SOLO id/nome/cognome, mai l'intera riga artigiano — stesso principio di minima esposizione già usato per i conteggi aggregati admin, qui applicato a un lookup di nomi). **Pagine**: `/beta` (lista, ordinata per ULTIMA ATTIVITÀ — ultimo messaggio non nascosto, non data di creazione, come richiesto — query post/messaggi/nomi separate + merge lato JS, nessun embed PostgREST mai usato in questo progetto), `/beta/nuovo` (form titolo+testo), `/beta/[id]` (thread — messaggi cronologici, form di risposta condizionale su chi può scrivere ORA, bottone "Chiudi con questa risposta" solo admin, controlli nascondi/mostra post e singoli messaggi solo admin). Guard aggiuntivo in `app/(app)/beta/layout.tsx` (oltre a quello già in `app/(app)/layout.tsx`): `beta_tester OR is_admin`, altrimenti redirect silenzioso a `/lavori` — l'admin accede sempre, anche se il suo account non è marcato beta tester, come richiesto esplicitamente. **Nav**: voce "Beta Tester" condizionale in `components/app-nav.tsx` (mai renderizzata per chi non ha accesso, non solo nascosta dietro il guard di route) con badge — SOLO per l'admin — dal conteggio di `beta_notifiche_admin_count()`, calcolato in `app/layout.tsx` solo se `is_admin` (nessuna chiamata superflua per gli altri). **Firma admin**: nessuna logica speciale, il nome mostrato è sempre quello reale risolto da `beta_nomi_autori()` — mai un'etichetta generica "Admin"/"Districo", soddisfatto per costruzione senza codice dedicato. **Verificato end-to-end su Supabase Cloud** (4 artigiani di test isolati via Admin API — admin, due beta tester A/B, un artigiano "plain" né beta né admin — dev server + script Playwright, cascade `on delete` verificato alla pulizia finale, ambiente smontato a fine sessione): 24 controlli, tutti superati — plain bloccato dal menu E dalla route; A crea un post; B lo vede in lista/thread ma non ha il form (non è l'autore); admin vede il form (può rispondere a chiunque) e il badge=1; admin risponde (non chiude) → badge=0; firma admin = nome reale; A fa un follow-up → badge=1; admin CHIUDE con una risposta → stato Chiuso, form sparito anche per l'admin; A tenta di scrivere → nessun form, messaggio "Questo post è chiuso", **e una chiamata RLS diretta con il JWT reale di A (bypassando del tutto l'app) viene comunque rifiutata**; admin riapre → stato Aperto, form di nuovo visibile a A; admin nasconde un messaggio → sparisce per B, resta visibile (marcato) per admin; admin nasconde l'intero post → sparisce dalla lista di B, accesso diretto al thread → 404, resta visibile (marcato) in lista per l'admin. **Ispezionato anche visivamente via screenshot** desktop e mobile (lista con badge "nascosto", thread con messaggio nascosto tratteggiato e bottoni nascondi/mostra per riga, pulsanti Rispondi/Chiudi ben distinti). Zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` puliti (route `/beta`, `/beta/[id]`, `/beta/nuovo` confermate nell'elenco). **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). **Migration `0062` e il fix `0063` applicate dall'utente su Supabase Cloud durante la sessione**, verificate indipendentemente via introspezione OpenAPI e chiamata RPC diretta prima di procedere ai test. |
| 2026-08-22 | **`/beta` diventa il punto di ingresso unico al programma beta + FIX SICUREZZA scoperto nel farlo**. Migration `0064`. **Scoperta non pianificata, prima di scrivere qualunque codice applicativo**: per aggiungere `richiesta_beta_at` come nuovo campo self-writable su `artigiano` (stesso meccanismo generico già usato per anagrafica/preferenze), verificato — non ipotizzato, testato empiricamente con un artigiano di test isolato — che la policy RLS "artigiano aggiorna solo se stesso" (0001) non avesse MAI ristretto le colonne, solo la riga: qualunque artigiano autenticato poteva auto-impostare `is_admin=true` con una singola PATCH REST diretta. **Fermato il lavoro, segnalato esplicitamente all'utente prima di procedere** (due opzioni proposte: correggere subito o solo segnalare) — **confermato "correggi subito, poi procedi col task"**. **Fix**: privilegi UPDATE a livello di colonna (`revoke update on artigiano from authenticated, anon` + `grant update (lista esplicita) to authenticated`) — la RLS resta invariata (decide la riga), i privilegi di colonna decidono i campi. **Audit completo di ogni scrittura reale** (grep mirato su `.from('artigiano').update(...)` in tutto `app`/`components`/`lib`, nessuna congettura) prima di scrivere la lista: 29 colonne restano self-writable (anagrafica, indirizzo, SMTP, testo mail, tariffe, `kpi_finestra_mesi`, `immagine_profilo`, ora anche `richiesta_beta_at`); `is_admin`/`beta_tester`/`accesso_gratuito`/`stato_abbonamento`/`piano_abbonamento`/`trial_fine`/`stripe_subscription_id`/`email`/`id`/`created_at` restano SOLO da funzioni SECURITY DEFINER/trigger/service role. **`stripe_customer_id` — unico caso spostato invece di re-includerlo**: era scritto direttamente dal client in `avviaCheckout()` (`lib/stripe/actions.ts`) — con la REVOKE avrebbe smesso di funzionare; spostato in una nuova funzione `imposta_stripe_customer_id(p_customer_id)` (SECURITY DEFINER, `where ... and stripe_customer_id is null` — scrivibile una volta sola, un identificativo verso un servizio di pagamento esterno è più sensibile di una preferenza personale). **Verificato con un doppio confronto prima/dopo su un artigiano di test reale**: self-promote `is_admin=true` e `beta_tester=true` → `403 permission denied for table artigiano` (prima della migration, entrambi riuscivano); update legittimo (`nome`) e il nuovo `richiesta_beta_at` → `204` riusciti; `imposta_stripe_customer_id()` verificata idempotente (secondo tentativo con un id diverso resta no-op). **Primo tentativo di applicazione fallito** (`42703 column "richiesta_beta_at" of relation "artigiano" does not exist` — il `grant update` citava la colonna prima che la riga `alter table add column` la creasse, ordine sbagliato nel file) — **verificato che il batch si fosse annullato per intero prima di correggere** (nessun self-update rimasto bloccato a metà, il SQL Editor di Supabase esegue uno script multi-statement come una singola transazione implicita), poi riordinato e riapplicato con successo. **2) Mini-sito beta** (contenuto della richiesta originale di questa sessione): `/beta` ora raggiungibile da OGNI artigiano autenticato (voce di menu "Beta Tester" tolta dalla condizione `beta_tester||is_admin`, sempre in `VOCI_ATTIVE`) — chi ha già accesso vede il forum invariato (estratto in una funzione `ForumBetaLista()` interna alla stessa pagina, nessuna riscrittura della logica), chi non ce l'ha vede un mini-sito (`components/beta/mini-sito-beta.tsx`, copy ripresa parola per parola dalla Sezione 8 della landing — "Districo è ancora giovane..."). `/beta/[id]` e `/beta/nuovo` restano riservati (nuovo `lib/beta/guard.ts`, `richiedeAccessoBeta()` — sostituisce il vecchio `app/(app)/beta/layout.tsx`, rimosso: reindirizza a `/beta` non più a `/lavori`, coerente col fatto che `/beta` è ora sempre una destinazione valida per chiunque). **Posti disponibili** = `configurazione_beta.posti_beta_totali` (tabella singleton, PK boolean forzata a `true`, default 10, editabile solo da admin — RLS admin-only, editor in `/admin/dashboard` accanto a "Beta tester attivi") meno `count(beta_tester=true)`, esposto a QUALSIASI artigiano via `beta_posti_disponibili()` (SECURITY DEFINER: deve leggere sia la config admin-only sia contare TUTTI i beta tester, RLS self-only su `artigiano` altrimenti vedrebbe solo il chiamante — stessa classe di bug già corretta in `0063`, qui evitata da subito). **"Richiedi un posto"** (`richiediPostoBeta()`) imposta solo `richiesta_beta_at`, MAI accesso automatico — tre stati mostrati nel mini-sito: bottone (posti disponibili, nessuna richiesta ancora), "Richiesta inviata, ti risponderemo appena possibile" (richiesta già fatta), messaggio "posti esauriti" (nessun bottone). **Richieste pendenti visibili all'admin**: `admin_lista_artigiani()` estesa con `richiesta_beta_at` (stesso drop+create+re-grant già necessario per un cambio di `returns table`, vedi nota tecnica sopra in "Principi architetturali") — badge giallo "Richiesta {data}" accanto al toggle Beta tester in `/admin/utenti` quando `richiesta_beta_at` è valorizzato e `beta_tester` è ancora `false`, sparisce da solo appena il toggle viene attivato (nessuna azione separata per "evadere" la richiesta). **Verificato end-to-end su Supabase Cloud** (3 artigiani di test isolati — admin, due "plain" senza beta_tester — dev server + script Playwright, un bug di TEST scoperto e corretto durante la verifica — non applicativo: un `hasText` a substring aveva ambiguamente matchato "Test Plain" con "Test PlainDue", toggle attivato sull'account sbagliato, individuato confrontando lo stato reale via query diretta e corretto con un match esatto sulla cella): 16 controlli, tutti superati — voce "Beta Tester" sempre in nav; plain vede il mini-sito (non il forum) con "10 posti su 10 disponibili" e il bottone; `/beta/nuovo` diretto reindirizza a `/beta` (non più `/lavori`); dopo la richiesta, "Richiesta inviata..." e bottone sparito; admin vede il badge "Richiesta" nella riga giusta; dopo aver attivato il toggle, lo stesso utente vede ORA il forum (non più il mini-sito); editor "Posti beta totali" presente e funzionante; scenario posti esauriti (`posti_beta_totali=1`, 1 beta tester già attivo) → "0 posti su 1 disponibili", messaggio esauriti, nessun bottone. **Ispezionato anche visivamente via screenshot** desktop/mobile del mini-sito e della pagina `/admin/dashboard` con l'editor posti. Zero errori console/pageerror. Utenti di test eliminati e `posti_beta_totali` ripristinato a 10 a fine sessione, verificato che restassero solo i 3 artigiani reali. `tsc --noEmit`/`eslint`/`npm run build` puliti. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). **Migration `0064` applicata dall'utente su Supabase Cloud** — primo tentativo fallito per l'errore di ordinamento sopra, corretto nel file, verificato indipendentemente (tabella, colonna, 3 funzioni RPC presenti) prima del secondo tentativo riuscito. |
| 2026-08-22 | **Fix bug reale in produzione — l'admin non poteva creare il primo post nel forum beta**. Migration `0065`. **Diagnosi dai log del container di produzione prima di ipotizzare qualunque cosa, come richiesto esplicitamente**: l'errore generico "An error occurred in the Server Components render" (nessun digest visibile in UI nonostante il messaggio lo dichiari — comportamento normale di Next.js in produzione, il digest compare solo nei log server) nascondeva `Error: new row violates row-level security policy for table "post_beta"`. **Non un edge case del "primo post"** come inizialmente sospettato dall'utente — la vera causa: la policy INSERT di `post_beta` (0062) controllava SOLO `beta_tester = true`, mai `is_admin` — l'account admin reale (Nicola) ha `is_admin=true` ma `beta_tester=false` (non è lui stesso marcato beta tester), quindi qualunque tentativo di creare un post da parte sua sarebbe fallito identicamente anche con `post_beta` già popolata. Un gap di design reale, non un bug legato ai dati: il principio "l'admin è un partecipante di prima classe della community" (già applicato correttamente alle RISPOSTE su `messaggio_beta`) non era mai stato esteso alla CREAZIONE di un nuovo post. **Fix**: policy INSERT riscritta per accettare `beta_tester = true` OPPURE `is_admin = true` (drop+create della policy, stesso pattern già in uso per le altre modifiche RLS di questo progetto). **Verificato**: RPC `beta_crea_post()` chiamata con un artigiano di test `is_admin=true`/`beta_tester=false` → `200` (falliva con `42501`/RLS prima del fix); **flusso reale via browser, esattamente lo scenario segnalato** (login admin → `/beta/nuovo` → Oggetto "Benvenuti!" → Pubblica) → redirect al thread, titolo e testo corretti, **e verificato che il post comparisse davvero nella lista `/beta`** (non solo assenza di errore, come richiesto esplicitamente). **2) Fix UI — voce di menu "Beta Tester" andava a capo su due righe**: con 5 voci invece di 4, l'etichetta a due parole non entrava su una riga nella colonna centrale della nav a `md:` (768px). Accorciata a **"Beta"** (il testo completo resta invariato nell'`<h1>`/copy della pagina) + `whitespace-nowrap` aggiunto a tutte le voci della nav desktop come protezione generale (mai più un mid-word wrap, indipendentemente dalla lunghezza futura di un'etichetta). **Verificato visivamente via screenshot** a 1280px e a 768px esatto (il breakpoint più stretto in cui la nav desktop compare, il caso limite reale) — "Beta" su una riga sola in entrambi, nessun overflow. `tsc --noEmit`/`eslint`/`npm run build` puliti. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). **Migration `0065` applicata dall'utente su Supabase Cloud**, verificata indipendentemente via chiamata RPC diretta con un artigiano di test admin-non-beta prima di procedere ai test UI. |
| 2026-08-22 | **Fix bug — orari mostrati sfasati di 2 ore rispetto all'Italia (es. un post delle 12:45 mostrato come 10:45)**. Nessuna migration. **Causa verificata prima di correggere, come richiesto esplicitamente** (non ipotizzata): il container Docker di produzione gira sempre in UTC (`date`/`Intl.DateTimeFormat().resolvedOptions().timeZone` dentro il container confermano entrambi "UTC", verificato via SSH) — ogni `toLocaleDateString`/`toLocaleString('it-IT')` SENZA `timeZone` esplicito eredita il fuso del PROCESSO che esegue il rendering, non quello dell'artigiano. **Scope reale, non "isolato al forum beta"**: audit mirato di tutte le 22 occorrenze del pattern in `app`/`components`/`lib`, classificate una per una per `'use client'` sì/no — i satelliti Lavoro (Preventivo/Progetto/Acconto/Campione/Noleggio/Acquisto/Appuntamento/Costruzione/Montaggio/Spesa non preventivata, `lavoro-info.tsx`) sono TUTTI Client Component: girano nel browser dell'artigiano, già di fatto in Europe/Rome, MAI affetti da questo bug — non toccati, verificato uno per uno prima di escluderli, non per analogia. **4 Server Component realmente affetti** (bug confermato, non solo sospettato): `app/(app)/beta/page.tsx` (ultima attività del thread), `app/(app)/beta/[id]/page.tsx` (data post + data messaggi), `app/admin/utenti/page.tsx` (data registrazione + data/ora richiesta beta), `components/profilo-abbonamento-form.tsx` (data fine trial — nessun `'use client'` in testa, scoperto solo grazie all'audit sistematico, non era tra i punti segnalati dall'utente). `lib/lavori/scheda-lavoro-mapping.ts`/`scheda-lavoro-pdf.tsx` (PDF "Scheda di lavoro", 17/8) erano già corretti — unico altro punto server-side del progetto, avevano già `timeZone: 'Europe/Rome'` esplicito con un commento che descriveva esattamente questo stesso rischio, prima ancora che si materializzasse altrove. **Fix — centralizzato "una volta per tutte" come richiesto**, non 4 patch isolate: nuovo `lib/formato-data.ts` (`formattaData()`/`formattaDataOra()`, `timeZone: 'Europe/Rome'` per NOME IANA — non un offset fisso +1/+2, gestisce da solo il cambio ora legale/solare) — i 4 file adottati, PIÙ `scheda-lavoro-mapping.ts`/`scheda-lavoro-pdf.tsx` migrati sulla stessa utility condivisa al posto delle due implementazioni locali identiche preesistenti (deduplicazione, nessun comportamento cambiato lì). **Verificato end-to-end contro l'ambiente reale**: build di produzione locale avviata con `TZ=UTC` esplicito (stesso identico fuso del container Docker, l'ambiente sandbox di sviluppo è invece già Europe/Rome di suo — avrebbe mascherato il bug se non forzato) puntata su Supabase Cloud di produzione; artigiano di test isolato creato via Admin API (beta_tester poi anche is_admin temporaneo), login reale, pubblicazione reale di un post dalle `13:00:42` (ora italiana) → **mostrato correttamente come "13:00:42" sia nel thread sia nella lista `/beta`** (senza fix sarebbe apparso "11:00:42"); **bonus**: lo stesso passaggio ha mostrato anche il post reale "Benvenuti!" (pubblicato la sessione precedente) ora correttamente a "12:45:42" — la cifra esatta del bug originariamente segnalato dall'utente. Verificato anche il caso limite di confine giorno (un istante UTC che cade dopo mezzanotte italiana, es. 22:30 UTC → 00:30 CEST del giorno dopo) con uno script isolato sotto `TZ=UTC`: mostra correttamente il giorno successivo, non quello UTC — bug latente più sottile che il fix risolve anche se non esplicitamente segnalato. Dati di test rimossi e verificati (`[]` alla query finale). `tsc --noEmit`/`eslint`/`npm run build` puliti. **Committato e deployato su apphub**. |
| 2026-08-22 | **Voce di menu Admin + post "Info" per gli annunci admin nel forum beta**. Migration `0066`. **1) Voce "Admin" nell'AppNav**: aggiunta dinamicamente (`VOCE_ADMIN`, non un elemento statico di `VOCI_ATTIVE` — visibile solo a `isAdmin`), punta a `/admin/dashboard` — `/admin/utenti` resta raggiungibile da lì tramite la nav interna già esistente in `app/admin/layout.tsx` (invariata, solo il commento "nessuna voce di menu pubblica verso /admin, per design" aggiornato perché non più vero). `voceAttiva()` esteso con un caso speciale: la voce Admin si evidenzia su TUTTA la sezione `/admin` (sia `/dashboard` sia `/utenti`), non solo sul sotto-percorso esatto a cui punta il link. **2) Post "Info"**: un post il cui autore è admin al momento della creazione nasce già con `tipo='info'` e `stato='chiuso'` (`chiuso_at = created_at`, stesso istante) — niente più azione di chiusura esplicita richiesta per un annuncio a messaggio singolo come "Benvenuti!". Nuova colonna `post_beta.tipo` (`'discussione'`/`'info'`, default `'discussione'`) e `beta_crea_post()` ridefinita: legge `is_admin` del chiamante, inserisce il post con `tipo` derivato di conseguenza, inserisce il primo messaggio MENTRE il post è ancora `'aperto'` (stesso ordine già usato da `beta_chiudi_post_con_risposta()` — la policy INSERT di `messaggio_beta` richiede `stato='aperto'` al momento dell'inserimento, altrimenti fallirebbe anche per l'admin), solo alla fine chiude il post se il chiamante è admin. **Deduzione automatica da `is_admin` confermata con l'utente** (non un selettore esplicito "Discussione/Info" in UI) — comportamento per i post dei beta tester (non admin) resta ESATTAMENTE invariato: `tipo='discussione'` (default), `stato` resta `'aperto'`, nessun update aggiuntivo. Badge visivo "Info" (`TIPO_POST_BETA_INFO_COLORE`, azzurro `bg-sky-100`/`text-sky-700` — non uno stato "a LED", quelli restano riservati ai semafori delle Attività Lavoro) mostrato accanto al titolo sia nella lista `/beta` sia nell'header del thread `/beta/[id]`, solo quando `tipo==='info'` (nessun badge per il caso normale 'discussione', stesso principio già in uso per il badge "nascosto"). **Il post reale "Benvenuti!" (pubblicato prima di questa sessione) resta `tipo='discussione'`/`stato='aperto'`**, per costruzione: nessuna migration retroattiva sui post esistenti, solo i nuovi post creati da admin da questo momento in poi seguono la nuova logica — segnalato all'utente, nessuna azione presa senza richiesta esplicita. **3) Tasto "Accedi" landing — NON un bug reale**: verificato che il link fosse già presente, visibile e funzionante in produzione (screenshot desktop e mobile, `cache-control: no-store` esclude un problema di cache) prima di modificare qualunque cosa — nessuna azione presa su questo punto, segnalato all'utente per chiarimento invece di introdurre un secondo bottone duplicato "al buio". **Verificato end-to-end su Supabase Cloud** (3 artigiani di test isolati — admin, beta tester, "plain" — dev server + script Playwright, ambiente smontato a fine sessione): 18 controlli, tutti superati — plain non vede la voce Admin né vi accede direttamente (redirect a `/lavori`); admin vede la voce, ci clicca, atterra su `/admin/dashboard`; post creato da admin → `tipo=info`/`stato=chiuso`/`chiuso_at===created_at` (verificato a query diretta), badge "Info"+"Chiuso" in UI, nessun form di risposta residuo; post creato da beta tester → `tipo=discussione`/`stato=aperto`/`chiuso_at=null`, nessun badge "Info", form di risposta presente. **Ispezionato anche visivamente via screenshot** (nav con voce "Admin" attiva su `/admin/dashboard`, lista `/beta` con badge "Info" accanto al post admin e badge "Aperto" invariato su "Benvenuti!"). Zero errori console/pageerror. `tsc --noEmit`/`eslint`/`npm run build` puliti. **Migration `0066` applicata dall'utente su Supabase Cloud durante la sessione**, verificata indipendentemente (colonna `tipo` presente, valore `'discussione'` di default sulla riga esistente) prima di procedere ai test. **Committato e deployato su apphub** (stesso accesso SSH `root@178.105.199.29`). |
| 2026-08-22 | **Fix bug reale — tasto "Accedi" della landing visibile ma non cliccabile**. Nessuna migration. **Chiarito con l'utente prima di agire** (vedi voce precedente, stesso giorno): non un problema di visibilità/cache come inizialmente verificato ("vedo la scritta ma non posso cliccarci" — un problema di funzionalità, non estetico). **Causa reale trovata con `elementFromPoint` sul punto esatto del link, non ipotizzata**: in `LandingHero` (`components/landing/landing-hero.tsx`) il div del titolo/payoff (`h1`+`p`) NON è `absolute` — resta nel flusso flex della `<section>` (`items-start`), quindi il suo BOX (padding-top incluso: `pt-[19svh]`/`pt-[13svh]`, ben più alto dei ~64px dell'header) parte dalla stessa `y=0` dell'header. Stesso `z-index: 10` dell'header ma successivo nel DOM — a parità di z-index vince l'ordine DOM, quindi quest'area di padding, visivamente vuota (nessun figlio la occupa, il testo appare solo più in basso), intercettava comunque i click destinati al link "Accedi" sottostante: verificato con `document.elementFromPoint()` al centro esatto del link in produzione, che restituiva il DIV del titolo (`z-10`, nessun `href`), non il tag `<a>`. Il testo "Accedi" restava visibile perché non c'è nulla di opaco disegnato sopra — un bug di hit-testing puro, non di rendering, motivo per cui uno screenshot da solo non l'aveva rivelato nella verifica precedente. **Fix chirurgico**: `pointer-events-none` sul div contenitore (mai stato interattivo di suo) + `pointer-events-auto` sui suoi due figli diretti (`h1`/`p`, per non perdere la selezionabilità del testo) — nessuna modifica a layout/z-index/ordine DOM, il click ora attraversa l'area vuota e raggiunge l'header sottostante. **Verificato end-to-end**: prima del fix, `elementFromPoint` sul link restituiva il div del titolo e un click reale (Playwright) su `https://districo.it/` falliva (timeout, nessuna navigazione) sia a 1280px sia a 390px; dopo il fix (build locale identica, stesso identico markup), `elementFromPoint` restituisce correttamente `<a href="/login">` e il click naviga a `/login` su entrambi i viewport — confermato anche che il titolo resti presente/leggibile. `tsc --noEmit`/`eslint`/`npm run build` puliti. **Committato e deployato su apphub**. |
